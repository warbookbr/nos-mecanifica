# Melhorias reaproveitáveis pelo NÓS

Este documento acompanha capacidades criadas na Mecanifica que podem voltar ao
repositório original [`brigsd/nos`](https://github.com/brigsd/nos).

Ele não é uma lista de desejos. Uma entrada só avança para “pronta” depois de
implementação, testes e uso em uma peça real.

## Regra de extração

Uma melhoria candidata:

- não importa Three.js;
- não contém conceitos automotivos;
- funciona em modo headless;
- preserva ou migra formatos anteriores explicitamente;
- possui testes independentes da interface;
- fica em commit separado das mudanças de produto;
- documenta arquivos e dependências necessários para cherry-pick.

O remoto Git `source` aponta para o NÓS original. Antes de preparar uma
contribuição, compare a implementação com o estado recente desse remoto e leve a
mudança para um fork verdadeiro de `brigsd/nos`.

## Estados

- **observada:** problema confirmado, sem solução escolhida;
- **em prova:** hipótese sendo exercitada;
- **provada:** usada com sucesso e coberta por testes;
- **pronta para upstream:** isolada, documentada e sem dependências da Mecanifica.

## Escopo de domínio e geometria

O freio a disco foi a prova real desta rodada, mas não é o domínio embutido no
núcleo. Antes de extrair uma capacidade, separe a **ferramenta geral** da
**interpretação mecânica** que a prova usou:

- Identidade estrutural, aliases, parâmetros derivados, posicionamento de
  primitivas, hierarquia de partes, portas semânticas, diagnósticos de referência,
  o gate de ID cru e argumentos estritos não sabem o que é um freio. São
  candidatos gerais para objetos rígidos, orgânicos ou abstratos.
- Caixa por parte, dimensões e componentes conexos também são medidas gerais.
  Elas descrevem geometria; não dizem sozinhas se uma peça está correta.
- A classificação `folga`, `encosta` e `interpenetra` entre caixas é uma régua
  **opt-in para montagem rígida**. Ela foi calibrada para pares com contato
  funcional esperado, como disco/pastilha, pistão/pastilha e suporte/pinça.
  Não é um validador universal de malha nem uma prova de colisão exata.
- Em objetos orgânicos, sobreposição pode ser intencional (pele, folhas, pelos,
  músculos, detalhes encaixados), a forma pode deformar por esqueleto ou animação
  e a continuidade, curvatura e silhueta podem importar mais que uma folga
  positiva. Aplicar essa régua como gate automático produziria falsos defeitos.
  Nesses casos, use-a apenas como relatório, declare pares rígidos específicos ou
  crie métricas próprias de topologia, deformação e aparência.

Portanto, o que não deve subir como capacidade geral é a peça
`freio-disco.js`, suas proporções, a convenção de eixos do veículo e os testes
de contato daquele sistema. O que pode subir é o mecanismo neutro, desde que sua
semântica e seus limites sejam declarados.

## Registro

| ID | Capacidade | Estado | Evidência | Valor para o NÓS |
|---|---|---|---|---|
| UP-001 | Identidade semântica sem dependência de posição do passo | observada | O formato herdado ainda usa blocos `posição × 1000`; uma corrida registrou 234 órfãos | Inserir ou reordenar operações sem quebrar referências |
| UP-002 | Relações espaciais declarativas (`alinhar`, `centralizar`, `encostar`) | observada | A autoria herdada não expressa “encoste A em B”; no freio a disco os 4 contatos viraram soma de espessuras e só um teste guarda a intenção (ATRITOS-AUTORIA A-6) | Compor objetos por intenção, sem calcular coordenadas manualmente; contato deve declarar se é rígido, flexível ou apenas visual |
| UP-003 | Expressões validadas entre parâmetros | provada | `motor/expressoes.js` aceita `'=nome / 2 + outro'` somente em contexto numérico, com números, nomes, parênteses e `+ - * /`; sem `eval`, símbolo/propriedade fora da gramática, nome ausente, ciclo e valor não-finito falham. As 23 derivadas do freio saíram do JS auxiliar e voltaram ao `PARAMS` salvo, sem alterar a malha (ATRITOS-AUTORIA A-5) | Reduzir números duplicados e permitir refinamento paramétrico |
| UP-004 | Verificação de cobertura de nomes agregados | observada | O drone mostrou que um nome pode resolver menos elementos do que promete sem falha técnica | Evitar semântica aparentemente válida, porém incompleta |
| UP-005 | Adaptador neutro para grafos de cena | provada | O drone herdado foi convertido em 23 partes selecionáveis; teste headless e build passaram | Tornar explícita a separação entre autoria e renderizador |
| UP-006 | Gate para faces sem identidade + seleções estruturais no drone | provada | A bancada encontrou 6 faces da lente sem nome e 6 classificadas como pouso; a correção por `origemId` zerou ambas | Impedir que seleções espaciais sobrepostas corrompam nomes sem gerar órfãos |
| UP-007 | Mapa determinístico entre Windows e Linux | provada | Normalização CRLF/LF e ordenação por ponto de código fecharam o CI nos dois ambientes | Evitar mapas diferentes conforme o sistema operacional |
| UP-008 | `origem` estrutural em TODO gerador e portas nomeadas pelo autor | provada, com limite medido | R4 adicionou origem a `chamferBox`, `esfera`, `cone`, `plano` e `inflate`, além de `publicarPorta`/`sel:{porta}`; pinça e suporte do freio usam `chamferBox` com 0 face sem identidade. A fixture NÃO automotiva `prototipos/fps/v3/pecas/_jardineira.js` (jardineira de janela com uma muda) fecha a prova fora do vocabulário mecânico: 6 partes, 351 faces, 350 vértices, 0 face sem identidade, 0 órfão; **8** portas publicadas antes das transformações e resolvidas depois (eram 5 na R4); `tools/mecanifica/jardineira-integridade.test.ts` (22 casos) amarra a contagem de cada parte à FÓRMULA do gerador, reconstrói com outro `TOPO` e prova que inserir um passo no começo da lista renumera a malha inteira sem mudar nada do que a peça afirma. **Limites medidos, não supostos:** `cone`, `plano` e `chamferBox` só citam a primitiva inteira apesar de terem numeração fechada e documentada (A-18); o eixo de uma origem não passa por `st.num`, então não cita parâmetro e reaponta em silêncio quando o `TOPO` muda (A-19); `nucleo()` não devolve as portas, então nenhuma ferramenta as enxerga (A-20). **Os três foram atacados no ciclo Endereços semânticos v1**, e o que sobrou está dito: A-18 e A-19 resolvidos (ver UP-018); o A-20 fechou nas duas metades — `nucleo()` devolve `portas` (Map nome -> `{nome, de, passo}`, ordenado por nome, `de` clonado, fora do `neutroCanonico`) e o módulo neutro `src/autoria/descrever-partes.js` as mede para o `npm run descrever` e para a bancada, mostrando a origem DECLARADA (`cilindro:404 tampa=fundo`), não as faces resolvidas. `adaptarThree` segue sem portas de propósito: ele converte geometria, e porta é contrato de autoria. A prova indireta acabou junto: o teste da fixture tinha 15 leituras de `f.material` como procuração de porta e agora tem 0 — ele lê `neutro.portas` e cita `sel:{porta}` num passo de sonda próprio, montado depois de todas as transformações | Todo gerador fica endereçável sem IDs de runtime; portas separam o nome funcional escolhido pelo autor do nome geométrico local da primitiva. Extração: `CONTRATOS_ORIGEM`, `registraOrigem`, `publicarPorta`/`resolverPorta` e o seletor `sel:{porta}` de `motor/oficina.js` — sem Three.js e sem conceito automotivo. Quem extrair leve os três limites junto: eles dizem onde o contrato para |
| UP-009 | Posicionar/orientar na criação da primitiva | observada | 16 dos 52 passos do freio a disco só transportam primitivas da origem até o lugar (ATRITOS-AUTORIA A-4) | Tirar 30% de burocracia de qualquer montagem com eixo diferente de Y |
| UP-010 | Alias de conjunto resolvido tarde (ou diagnóstico de completude) | em prova | 6 órfãos na 1ª execução do freio: alias citado antes da última primitiva que o compõe existir (ATRITOS-AUTORIA A-7). R2/O-11 implementou o ramo DIAGNÓSTICO em `motor/oficina.js` (`completudeDoAlias`, sem Three.js e sem conceito automotivo): a citação precoce passa a dizer em que passo o alias fecha e o que falta; 3 testes em `tools/oficina/oficina.test.ts`. A resolução TARDE (a outra metade da capacidade) segue não feita | O autor pensa em conjuntos; a ferramenta exige ordem de construção |
| UP-011 | Parâmetro de tipo ponto (e caminho) | observada | 18 dos 61 parâmetros do freio existem só para nomear 6 pontos do caminho da mangueira (ATRITOS-AUTORIA A-8) | Qualquer peça com trajeto — cabo, tubo, correia, trilho |
| UP-012 | Hierarquia pai/filho de partes | observada | O freio expõe 8 partes irmãs e não sabe dizer que a pastilha mora na pinça (ATRITOS-AUTORIA A-11) | Regra 3 do contrato de autoria; navegação de montagem em qualquer projeto |
| UP-013 | Relato de caixa por parte e escala na inspeção headless | em prova | 4 leituras de PNG do freio para responder "o eixo está em X?"; a resposta veio de medição feita fora da bancada (ATRITOS-AUTORIA A-13). R2/O-1 extraiu a medição para `src/autoria/descrever-partes.js` (neutro: sem Three.js, sem conceito automotivo) e publicou `npm run descrever`, que mede os 4 encaixes do freio em número; 12 testes em `tools/mecanifica/descrever-partes.test.ts`. A revisão adversarial (ALTA-1) achou que a relação medida FACE A FACE não alcançava `interpenetra` — face plana tem espessura zero na sua normal —, e a medida passou a ser CORPO A CORPO (componente conexo), com o mesmo classificador de `relacaoEntreCaixas`: uma verdade só, e `folga`/`encosta` nunca escondem invasão. Falta o lado da FOTO — escala px/m, gnômon e régua na imagem | Caixa e escala valem para qualquer inspetor 3D; `folga`/`encosta`/`interpenetra` é interpretação opt-in de montagem rígida, não gate para orgânicos |
| UP-014 | Catraca de id posicional: dívida herdada congelada com nome e número | provada | R2/O-4 fundou a catraca; a revisão adversarial da R2 achou que ela media a coisa errada duas vezes. (a) MEDIA-5: cobria 3 formas de coleção e afirmava que eram todas — o núcleo tem 6 (`faces:[ids]`, `sel:{v}`, `sel:{f}`, `vs:[ids]` do `pesar`, `pontos:[{f}]` do pincel livre, `de:[ids]` do `mescla`, esta última declarada como singular sendo coleção); `_oficina-esqueleto` já tinha 24 ids de vértice invisíveis para o gate. (b) MEDIA-6: contava PASSO, então `faces:[0,1]` e `faces:[0..19]` davam o mesmo número e a dívida podia decuplicar sem sair do teto. Corrigido: 6 formas, contagem de ID, e o inventário travado por teste que varre `a.<chave>` dentro de `OPS` — chave nova no núcleo quebra o teste e obriga a classificar. Medido agora: 8244 ids em 13 das 18 peças com `PASSOS`, contra 0 no freio a disco e 0 na peça de exercício do O-14. `tools/bancadas/id-cru.mjs` (sem Three.js e sem conceito automotivo) reprova peça nova, congela a contagem exata em `id-cru-herdado.json` (lista ORDENADA, não mapa: nome que parece inteiro reordena no motor e no `JSON.parse`, e o arquivo se autoinvalidava — BAIXA-11) e só encolhe; 42 testes em `tools/bancadas/id-cru.test.ts`, ligado ao CI. Fora de escopo, declarado e completo: as formas SINGULARES `face`, `v`, `a`/`b`, `para` (`vira` só aceita `face:<id>`). (c) No fechamento da R4 a catraca errou uma terceira vez, na direção oposta: `publicarPorta` (O-12) passou a ler `de:{op,id}` — origem ESTRUTURAL — na mesma chave que `mescla` usa para `de:[ids]`, e o gate reprovou a primeira peça a usar a op por 5 "ids posicionais" inexistentes (ATRITOS-AUTORIA A-21). O discriminador passou a ser a FORMA, não o nome da op, mantendo o gate op-agnóstico | Toda base herdada tem dívida grande demais para migrar de uma vez; a catraca separa dívida antiga de dívida nova sem migração de risco. E a lição de medição é geral: um gate que conta a unidade errada mente com número, que é a mentira mais convincente |
| UP-015 | Vocabulário DECLARADO de linha de comando: bandeira desconhecida falha | provada | A revisão adversarial da R2 (MEDIA-7) mediu: `--estrit` (uma letra a menos que `--estrito`) saía 0 sem uma linha de aviso e o gate sumia por um typo; `--parte=` imprimia a peça inteira enquanto o autor achava que tinha filtrado; duas peças de uma vez mediam a primeira calado — nos DOIS CLIs irmãos, com validações diferentes. `tools/mecanifica/argumentos.mjs` (sem Three.js e sem conceito automotivo) declara opções, bandeiras e o argumento solto, e recusa nome desconhecido, opção sem valor, bandeira com valor, repetição ambígua e argumento solto a mais, sugerindo o nome certo por distância de edição; 9 testes em `tools/mecanifica/argumentos.test.ts` | A lei do núcleo — referência inválida grita, nunca vira no-op — vale igual na borda: um CLI que engole typo transforma gate em decoração, e o NÓS tem uma dúzia deles em `tools/bancadas/` com o mesmo desenho |
| UP-016 | Repetição radial e linear com identidade semântica por instância | provada, em teste **e em peça** | A roda experimental precisou de cem parâmetros de coordenadas para expressar dez braços em cinco pares; o arquivo terminou com 141 parâmetros, embora a intenção seja quantidade, eixo e abertura do par (ATRITOS-AUTORIA A-17, OFICINA O-13). Entregue em 31/07/2026 como a op `arranja` de `motor/oficina.js` (sem Three.js e sem conceito automotivo), nos modos `radial` e `linear`. Ela é SEMPRE estrutural — `origemId`, `derivaDe` e `sel:{origem:...}` obrigatórios —, então a cópia anônima não depende de disciplina do autor: o passo é recusado sem identidade. A saída publica `{op:'arranja', id, de}` com o eixo `copia`, que reusa as formas de eixo que o núcleo já tinha (ausente, inteiro, nome de parâmetro, `'primeira'`/`'ultima'`, filtro `{passo,fase}`), sem palavra nova no formato salvo. **Prova em peça (fechamento do mesmo ciclo):** duas peças, em duas famílias. `prototipos/fps/v3/pecas/roda-dianteira-realista-experimento.js`, a que originou o item, foi reescrita: 141 parâmetros → 43, os 100 de coordenada → **0**, 66 passos → 47, 20 geradores de instância → 3, e cada um dos dez braços virou uma PARTE nomeada resolvida por `{op:'arranja', id, copia}`, isolável na régua e na bancada. `prototipos/fps/v3/pecas/_cerca-e-flor.js` prova o mesmo contrato fora do vocabulário automotivo, exercitando os DOIS modos: tábuas por arranjo linear, pétalas por arranjo radial. `tools/mecanifica/arranjo-em-peca.test.ts` (13 casos) afirma ângulo/posição derivados do parâmetro, rigidez da cópia, a MÃO da face girada de volta, e que nenhum `arranja` das duas peças pode contar por número digitado — `total` tem de citar um campo de `TOPO`. Cinco mutações medidas, todas mortas. **Limites medidos, ditos na cara:** `arranja` copia UMA origem, e há gerador sem "a primitiva inteira" no contrato — no `cilindro`, `{op,id}` são só as laterais, então copiar um cilindro custa três arranjos e devolve as tampas SOLTAS do tubo (13 corpos no lugar de 5, invisível na foto); a porca da roda teve de virar `lathe` por causa disso (ATRITOS-AUTORIA A-24). A ordem em que a coleção resolve as faces é determinística mas não tem afirmação, porque nenhum consumidor do núcleo a distingue hoje | Arranjos aparecem em rodas, pás, dentes, pétalas, colunas e padrões abstratos; uma operação neutra reduz verbosidade sem transformar as cópias em faces anônimas. Extração: a op `arranja`, `CONTRATOS_ORIGEM.arranja` e os helpers `indiceDeAxi`/`giraPonto`; depende de `validarEixo`/`indiceDeEixo`/`indicesEixo` e de `registraOrigem`, isto é, do UP-018. Três leis gerais para qualquer linguagem de autoria: (1) repetição sem identidade por instância destrói o endereçamento que o resto da linguagem construiu, então a identidade é obrigatória, não opcional; (2) o passo angular tem de ser DERIVADO da contagem (`(k+1)·passo`, sempre a partir da fonte), nunca acumulado — somar N vezes dá outro double e o erro entra no arquivo salvo; (3) duas maneiras legítimas de dizer o espaçamento (arco fechado × passo entre instâncias) não podem ser adivinhadas uma da outra: dar as duas grita e não dar nenhuma grita |
| UP-017 | Editor recusa exportar o que o gate do próprio projeto reprova — no funil, não no botão | provada, com limite medido | A Oficina (`prototipos/fps/v3/oficina.html`, do NÓS herdado) salvava por id posicional e o `id-cru:check` reprovava o arquivo depois (ATRITOS-AUTORIA A-15). A guarda nasceu no ouvinte do clique; `npm run guarda:salvar` (`tools/mecanifica/guarda-salvar-oficina.mjs`, Playwright, sem Three.js e sem conceito automotivo) dirigiu a interface real e mediu que o botão recusava enquanto o gancho `window.__oficina.salvar()` gravava a mesma edição em `pecas/` (2324 → 2354 bytes). Conserto: `diagnosticarExportacaoIncompativel` passa a rodar dentro de `salvarPeca`, antes do `fetch` e antes do `<a download>`, devolvendo `{via:'recusado',incompatibilidades}`; `PASSOS` vira argumento obrigatório para a guarda não virar no-op silencioso em chamador novo. A prova cobre os dois lados (peça limpa salva; `Ctrl+Z` devolve o salvamento) e os dois caminhos de saída, em dois servidores. **Limite medido no fechamento do ciclo e RESOLVIDO em Endereços semânticos v1 (ATRITOS-AUTORIA A-22):** a guarda, o gate e o harness mantinham CÓPIAS SEPARADAS da lista de chaves posicionais, e elas divergiram duas vezes na chave `de` — a Oficina recusava `_jardineira`, peça do repositório que o CI aprova, por 5 "ids posicionais" que eram as 5 portas do `publicarPorta`. O harness não acusou porque sua recontagem "independente" herdou a mesma lista. Conserto: uma regra só em `prototipos/fps/v3/motor/referencia-posicional.js`, importada pelos três, com um teste que reprova quem voltar a declarar a lista de chaves na mão | Extração: `salvarPeca`/`diagnosticarExportacaoIncompativel` de `oficina.html` + o harness inteiro; depende só de `tools/servir.mjs` e do Playwright já usados no NÓS. A capacidade geral é o desenho — **a guarda mora no funil por onde todo chamador passa**, e a prova dirige o botão, não a função —, útil em qualquer editor que grave artefato validado por CI. Quem extrair leve o módulo `motor/referencia-posicional.js` junto, e leve a lição: a regra de "o que é referência posicional" tem de ser UM módulo importado pelo editor, pelo gate e pelo harness. Três cópias divergem, e a terceira cópia faz a prova concordar com o erro em vez de acusá-lo. Cuidado de fronteira que o conserto mediu: o módulo tem de morar onde o EDITOR consegue importá-lo — a página é servida com raiz em `prototipos/fps/v3/`, então um módulo em `src/` 404 no navegador e viraria a quarta cópia |
| UP-018 | Endereço de face pelo eixo que o gerador já tem, e eixo que fala a língua dos parâmetros | provada | Duas metades de uma capacidade só, medidas em `motor/oficina.js` (sem Three.js e sem conceito automotivo). (a) **Endereço:** `cone`, `plano` e `chamferBox` publicavam só a primitiva inteira embora tivessem numeração fechada, documentada linha a linha e travada por teste (ATRITOS-AUTORIA A-18). Cada um passou a citar o eixo que já tinha REUSANDO as fábricas de contrato existentes — `cone` a estrutura do `cilindro` (`lado` + `tampa:'fundo'`; não existe 'topo', o ápice é vértice), `chamferBox` a do `cubo` mais `aresta` (12) e `canto` (8), `plano` a grade `faixa`×`lado` do `loft`. `inflate` fica no contrato mínimo por decisão medida (malha de scan de voxels, sem fórmula fechada), escrita no código. (b) **Eixo:** era o único campo dimensional que não passava por `st.num`, então `faixa: 3` continuava VÁLIDA apontando para outra faixa quando a contagem mudava — referência errada, não inválida (A-19). Passou a aceitar nome de PARAM/TOPO, expressão `=…` e as palavras de extremidade `'primeira'`/`'ultima'`, resolvidas contra a contagem REAL do gerador. 13 casos em `tools/oficina/oficina.test.ts`, conferidos vermelhos contra o arquivo de produção revertido; `gabarito:selecao:check` verde sem regravar (22 peças byte-idênticas). **(c) Prova em peça, não só em teste de núcleo** — a lição do A-21 e do A-15 aplicada de propósito: `prototipos/fps/v3/pecas/_jardineira.js` publica as três portas que a topologia já tinha e o contrato escondia (`chamferBox` aresta, `plano` faixa `'ultima'`, `cone` tampa) e teve a distorção DESFEITA — a meia-volta que só existia para pôr a `faixa: 0` para cima saiu junto com o parâmetro que a governava. Único hash regravado no gabarito, declarado no commit | Extração: `contratoLadoTampa`, `contratoCaixa`, `contratoFaixaLado`, `validarEixo`/`indiceDeEixo`/`indicesEixo` e as chamadas `registraOrigem` das primitivas. Duas leis gerais para qualquer linguagem de autoria: (1) contrato de endereço se ESTENDE reusando a fábrica do gerador irmão, e nome que promete região sem entregar é pior que nome nenhum — por isso `inflate` não ganhou grade; (2) endereço só por literal envelhece em silêncio, que é a falha pior — o endereço precisa citar o mesmo parâmetro que governa a contagem, e "o último" precisa ser resolvido contra a contagem real, não congelado. A aditividade decidiu uma divergência de propósito: `cone` responde a `{op,id}` com a primitiva inteira e `cilindro` com só as laterais, porque mudar o padrão do cone reapontaria toda citação já escrita sem diagnóstico |
| UP-019 | Palavra reservada que grita na colisão, em vez de vencer calada | provada | Medido na revisão adversarial do ciclo anterior (ATRITOS-AUTORIA A-23): num `plano` com `seg: 3` e `PARAMS {ultima: 0}`, escrever `faixa: 'ultima'` devolvia a última linha da grade, não a linha 0. As palavras de extremidade `'primeira'`/`'ultima'` eram testadas antes do dicionário do autor, e a referência resolvia para OUTRA coisa sem diagnóstico — não inválida, ERRADA, que é pior. Conserto em `motor/oficina.js` (sem Three.js e sem conceito automotivo): a palavra continua ganhando, mas a colisão com um `PARAM`/`TOPO` homônimo grita, nomeando a causa e o conserto. Aditivo — nenhuma peça do repositório declara parâmetro com esse nome e as 22 peças ficaram byte-idênticas no gabarito | Vale para qualquer linguagem com vocabulário fechado sobre um dicionário do usuário: reservar palavra é decisão legítima, precedência silenciosa não é. Extração: `EXTREMIDADES_EIXO`/`indiceDeEixo` mais o dicionário da peça viajando no estado (`st.dict`), leitura apenas. A lição de método é a mesma do UP-014: o defeito ficou latente porque nenhuma peça o exercitava, e só apareceu quando alguém foi procurar a colisão de propósito |
| UP-020 | Orientação DECLARADA da seção de um caminho | provada em núcleo **e em peça** | Medido na roda experimental (ATRITOS-AUTORIA A-25, RELATO-RODA-REALISTA "Frame implícito do `loft`"): o `loft` orientava cada anel por TRANSPORTE PARALELO, então quem escolhia o eixo `+u` era o HISTÓRICO do caminho. Uma seção retangular trocava largura por espessura de um braço para o outro, e a peça precisou DETECTAR a troca e REMONTAR cada contorno em código auxiliar. Entregue como a chave `orientacao:[x,y,z]` do `loft` em `motor/oficina.js` (sem Three.js e sem conceito automotivo): a direção do mundo para onde aponta o `+u` de TODA seção, PROJETADA no plano de cada seção — não precisa ser perpendicular, e aceita nome de PARAM como todo ponto do núcleo. Determinismo por construção: cada seção projeta a MESMA referência na PRÓPRIA tangente, sem propagar nada, então não há rotação acumulada ao longo do caminho. 10 casos em `tools/oficina/oficina.test.ts`, e 8 mutações rodadas contra eles, todas mortas — inclusive a que semeia o frame na referência declarada e depois PROPAGA (a diferença só aparece em caminho com TORÇÃO; a primeira versão do teste usava caminho plano e a mutação sobrevivia). Aditiva: `gabarito:selecao:check` verde com 23 peças byte-idênticas, sem regravar nenhuma. Conferida no olho na bancada (isométrica e superior), com quatro lâminas retangulares em direções diferentes: sem a chave, a lâmina do eixo Y sai de perfil enquanto as outras ficam de frente; com a chave, as quatro ficam iguais | Serve a cabo, tubo, corrimão, correia, corda, galho, trilho, pá, dente e lâmina — qualquer perfil NÃO simétrico varrido por um caminho. Extração: a chave `orientacao` dentro da op `loft`, mais `norm3`/`cross3`; ela é independente de `quadroLoft`/`transportaLoft`, que continuam servindo o caso sem a chave. Três leis gerais: (1) grau de liberdade que o gerador escolhe sozinho e o autor sabe nomear vaza para a peça como código auxiliar — publicar a escolha como argumento é mais barato que documentá-la; (2) orientação declarada tem de ser PROJETADA, nunca exigida perpendicular, senão o autor volta a calcular vetor; (3) referência degenerada (paralela à tangente) precisa GRITAR: qualquer desempate interno é a escolha implícita voltando pela janela. **Limite declarado:** só o `loft` tem frame a declarar, e nenhuma PEÇA usa a chave ainda — a roda experimental segue com o contorno remontado em código |
| UP-021 | Abrir vazio: furo cilíndrico com identidade em toda face criada e grito em toda face destruída | provada em núcleo, em peça de exercício **e em peça de produto** | Medido na roda experimental (ATRITOS-AUTORIA A-27, RELATO-RODA-REALISTA "Sem subtração ou corte volumétrico"): a linguagem não tinha NENHUMA subtração, e todo vazio era CONTORNADO — abertura pelo perfil anular, janela deixando o espaço em branco, fixador virando porca sobre o miolo. Entregue como a op `furo` em `motor/oficina.js` (sem Three.js e sem conceito automotivo): furo cilíndrico numa face plana e convexa, PASSANTE (`saida`, a face por onde ele sai) ou CEGO (`profundidade`, onde ele para); `de`/`saida` são origens estruturais que precisam resolver para EXATAMENTE uma face; `centro` é obrigatório e projetado no plano da entrada; `orientacao` declara a fase do anel. Numeração fechada: vértices `b+j` e `b+lados+j`, faces `b+j` borda, `b+lados+j` parede, `b+2·lados+j` saída OU `b+2·lados` fundo. 26 casos em `tools/oficina/oficina.test.ts` e 10 em `tools/mecanifica/prateleira-integridade.test.ts`; 34 mutações rodadas, 3 sobreviventes achadas e mortas com teste novo (saída atrás da entrada, consumo da face de saída, trava de `lados` mínimo). Aditiva: `gabarito:selecao:check` verde, as 23 peças anteriores byte-idênticas. Conferida no olho na bancada em três enquadramentos, mais o furo cego isolado e focado | Serve móvel (parafuso, cavilha), robô (respiro), carroça (eixo), instrumento e carro — furo existe em toda família de objeto. Extração: a op `furo`, os helpers `poligonoPlano`/`convexoCCW`/`margemDentro`/`bordaAnular`/`faceUnicaEstrutural`, o contrato `CONTRATOS_ORIGEM.furo` e o registro `st.consumidas` com `conferirConsumo`/`consumoDe` (que também alteram os quatro contratos de origem existentes). Quatro leis gerais: (1) **não construa a booleana genérica** — ela destrói identidade de face em massa e em silêncio; comece pelos casos reais e escolha a operação mais estreita que os resolve; (2) toda face que a operação cria recebe identidade rastreável até quem a criou; (3) toda face destruída fica REGISTRADA, e a diferença entre REMOVIDA (o autor mandou) e CONSUMIDA (foi substituída) é semântica: a união de faces pode pular a primeira em silêncio, nunca a segunda; (4) o casamento entre a geometria nova e a antiga deve ser ANGULAR (por posição no plano), não por índice — por índice a borda sai com polígonos reflexos que a foto não denuncia. **Limite declarado:** um furo por face (A-26 — PAGO depois, ver UP-023); só cilíndrico, só face plana e convexa, só na direção da normal. A coluna de maturidade desta linha foi promovida DEPOIS, pela rodada "Flange de uma peça só": na entrega original nenhuma peça de produto usava a op, e hoje `freio-disco.js` usa |
| UP-022 | Origem derivada que responde por um RECORTE da fonte, não só pela cópia inteira | provada, em teste e em peça de produto | Medido ao compor as duas capacidades do ciclo "Corte e orientação de seção v1" (ATRITOS-AUTORIA A-28): a origem `{op:'arranja', id, de, copia}` exigia que `de` fosse, chave por chave, a origem declarada em `derivaDe` — e `derivaDe` de um sólido é o sólido inteiro. Como a op `furo` exige que sua face de entrada resolva para EXATAMENTE uma face, as duas capacidades não compunham: um círculo de furos sobre uma coleção arranjada era impossível, e a saída seria declarar cada instância à mão. Correção geral, de duas linhas: o portão deixou de ser a IGUALDADE da origem declarada e passou a ser a PERTINÊNCIA das faces — `de` pode ser qualquer origem cujas faces sejam faces daquela fonte, e o grito de referência estranha passou a nomear a face e a fonte em vez de dizer só "outra seleção estrutural". 6 casos novos em `tools/oficina/arranja-contrato.test.ts` (8 → 14); 2 mutações rodadas, as duas mortas (repor a igualdade derruba 5 casos; trocar o grito por um `continue` silencioso derruba 1). Aditiva por construção — origem igual resolve para todas as faces da fonte, todas no mapa —, e medida: `gabarito:selecao:check` verde com as 24 peças anteriores byte-idênticas | Serve a qualquer op que DERIVE geometria de uma seleção (aqui `arranja`; o `espelha` tem o mesmo portão e continua com a igualdade, de propósito, porque nenhuma prova pediu). Extração: o `resolver` de `CONTRATOS_ORIGEM.arranja` em `motor/oficina.js`, sem Three.js e sem conceito automotivo. Duas leis gerais: (1) endereço derivado que só sabe responder pelo TODO impede a composição com qualquer op que exija uma parte, e isso não aparece até alguém tentar compor; (2) trocar igualdade estrutural por pertinência do resultado é mais permissivo E mais estrito ao mesmo tempo — aceita o recorte legítimo e continua recusando a origem estranha, agora com diagnóstico melhor. **Limite herdado:** o recorte é resolvido contra a malha viva, então face da fonte já consumida por um corte derruba a citação da cópia — quem fura fonte e cópias fura a fonte por último |
| UP-023 | Vários furos na MESMA face num passo só, cada um endereçável sozinho | provada em núcleo, em peça de exercício **e em peça de produto** | Medido em duas peças (ATRITOS-AUTORIA A-26): a op `furo` consome a face de entrada, então o segundo furo na mesma face é impossível — o círculo de parafusos, a fileira de furos da cantoneira e a flange furada só existiam se cada furo caísse numa face diferente. O custo apareceu na régua do flange de `freio-disco.js`: cinco corpos onde deveriam ser dois, uma chapa por prisioneiro. Entregue como a chave `centros` da op `furo` (sem Three.js e sem conceito automotivo), em duas formas — a lista `[[x,y,z], …]` e o círculo `{pivo, distancia, total, volta\|graus}`, com as palavras do `arranja` e o mesmo significado. O círculo nasce no quadro (u,w) da própria face, então nenhum seno ou cosseno entra no formato salvo. A borda de vários anéis é uma triangulação por PONTES e ORELHAS que não cria vértice nenhum (nada de junção em T com as faces vizinhas), contagem fechada `n + M·L + 2M − 2`. Anéis que se cruzam OU se encostam gritam pelo teorema do eixo separador, e o passo aborta com 0 V / 0 F. Identidade: o eixo `furo` da origem endereça cada furo do passo, e a superfície que não toca anel nenhum virou `preenchimento`/`preenchimentoDaSaida`. 30 casos novos em `tools/oficina/oficina.test.ts` e 9 em `tools/mecanifica/tampa-de-caixa-integridade.test.ts`; 16 mutações rodadas, 3 sobreviventes achadas — uma virou teste novo (o bloco de ids trocado entre furos passava por tudo), duas ficaram DECLARADAS no núcleo como provas de estado impossível sem caso que as dispare. Aditiva e medida: `gabarito:selecao:check` verde com as 25 peças anteriores byte-idênticas | Serve placa de móvel, flange, cantoneira, chapa de robô, tampa e cubo de roda — círculo ou fileira de furos existe em toda família de objeto. Extração: a chave `centros` da op `furo`, os helpers `aneisSeSobrepoem`/`triangularComAneis` (com `cruz2`/`dentroEstrito`/`segmentosCruzam`/`pontoNoSegmento`/`areaPoligono`) e o eixo `furo` de `CONTRATOS_ORIGEM.furo`. Três leis gerais: (1) quando uma op passa de UM para VÁRIOS, o plural precisa de identidade PRÓPRIA por instância, senão a segunda instância nasce anônima e o formato salvo fica sem como citá-la; (2) partição que cria vértice no contorno abre junção em T com as faces vizinhas — triangule sobre os cantos que já existem; (3) conferência que não pode falhar é promessa sem afirmação que morra: quando a transformação é AFIM, disjunção na entrada é disjunção na saída, e a segunda conferência deve virar teste, não código. **Prova em peça de PRODUTO, e o defeito que ela achou (rodada "Flange de uma peça só"):** o flange de `freio-disco.js` virou UM disco com os quatro furos num passo — 7 passos → 4, 9 parâmetros → 5, 6 identidades → 2, CORPOS da parte `cubo` 5 → 2, com o envelope idêntico; e `prisioneiros` passou a bastar sozinho (a peça constrói com 3, 5, 6 e 8, sem cosseno no arquivo). Levar a op ao produto ACHOU um defeito na partição: a face é a tampa de um cilindro de 16 lados com 4 anéis de 12 a 90°, e como 16, 12 e 4 são múltiplos de 4 a simetria põe o vértice de um anel EXATAMENTE em cima da aresta de uma orelha de outro (2,2·10⁻¹⁹ contra eps 10⁻¹²). "Em cima" não é "dentro": a orelha era cortada, engolia a lasca do outro lado e invertia o resto do polígono. Corrigido recusando vértice em cima de qualquer das três arestas da orelha, com o `pontoNoSegmento` que a ponte já usava — 17 de 240 combinações de face × furo × total gritavam antes, 0 depois; 10 casos novos, e devolver a conferência a só `dentroEstrito` mata 3. **A conta vale dentro das 240, e isso foi medido depois:** varrendo 14 212 combinações, 10 758 saem inteiras, 1 240 gritam por anéis sobrepostos e 1 165 por anel fora da face (os dois CORRETOS), 904 estouram o bloco de ids do passo (limite declarado), e 37 travam a partição com "nenhuma orelha livre" — face de poucos lados com furo raspando a borda, folga na casa de 5·10⁻⁴. É o A-33, ABERTO. O que já está garantido e fixado em teste: o caso ruim grita e aborta com 0 V / 0 F, nunca sai malha rasgada. LEI GERAL que entra na extração: **triangulação por orelhas em polígono FRACAMENTE simples (o que a ponte para o buraco produz) precisa recusar vértice sobre a aresta, não só dentro do triângulo** — e a geometria simétrica é onde isso aparece, porque a coincidência exata é regra e não acaso. Corolário: uma prova de "estado impossível" sem teste que a dispare pode estar apenas sem entrada conhecida, e a frase "não há entrada aceita que a faça falhar" é uma afirmação sobre o que ninguém tentou. **Limite declarado:** um RAIO por passo (A-30, aberto) — a flange com furo central mais círculo de parafusos ainda não é escrevível |
| UP-024 | A marca de suavização do formato salvo chega ao renderizador, e a face côncava é triangulada por ORELHAS | provada, com o acervo inteiro atravessado | Medido na bancada e no adaptador (ATRITOS-AUTORIA A-31): a borda dos furos serrilhava, e a peça não tinha culpa. Duas causas independentes, as duas no adaptador. (1) A marca `liso`, a mais antiga do formato salvo, NUNCA chegava ao renderizador: `computeVertexNormals()` numa geometria não indexada devolve por definição a normal chapada do triângulo, então das 540 faces do freio as 172 marcadas saíam iguais às outras. (2) O adaptador triangulava todo n-gon em LEQUE a partir do canto 0, e leque só vale em polígono CONVEXO — numa placa com furo de 12 lados, 4 das 12 faces da borda são quads côncavos, e neles o leque emitia triângulo de área NEGATIVA que cobria área fora do polígono. Atingia toda face côncava de qualquer peça. Entregue como: normal POSTA em vez de recalculada, somada por vértice do núcleo e pesada por ÁREA, varrendo SÓ as faces lisas e o estado NEUTRO inteiro (antes de partir em malhas por parte e material, para que troca de material não vire costura); e triangulação por ORELHAS no plano da face, que FALHA ALTO quando o contorno não fecha. Provas: `tools/mecanifica/normais-lisas.test.ts` (15 casos) e `tools/mecanifica/acervo-adaptador.test.ts` (o adaptador atravessa as 40 peças do acervo — antes só cinco chegavam nele em teste, e ele tinha ganhado três caminhos novos que lançam). Mutações: 3 na entrega, todas mortas; e uma que SOBREVIVEU virou conserto — havia uma conferência que endireitava a normal do triângulo contra a do plano, e ela mascarava o leque, deixando a aparência certa com a área errada. Depois da revisão, mais quatro mutações sobreviventes viraram teste: a soma por lote em vez de global, a soma sem peso de área, o vetor nulo virando normal, e o plano da face no lugar da normal do triângulo. Aditiva no formato salvo: `gabarito:selecao:check` verde | Vale para QUALQUER peça de qualquer família: a marca de suavização e a face côncava não têm nada de automotivo, e a segunda causa era um defeito de forma em toda peça com concavidade. Extração: `normaisSuaves`, `triangularPoligono` (orelhas), `normalDePoligono` (Newell) e `prepararFaces` de `src/autoria/adaptar-three.js` — nenhum deles conhece freio, carro ou interface, e só o último toca Three.js. Três leis gerais: (1) marca do formato salvo que o renderizador não lê é promessa sem efeito, e só a foto denuncia; (2) leque é triangulação de polígono CONVEXO — usar em n-gon qualquer é defeito de forma que a foto esconde e a área denuncia; (3) guarda que CONSERTA a aparência de uma geometria errada é o defeito silencioso, e deve ser removida em vez de mantida por segurança. **Limite declarado:** a SILHUETA continua poligonal (A-34, aberto) — normal suave é sombreado, não contorno |
| UP-025 | Raio de concordância (fillet) num contorno 2D — a alça de curva do 3º elemento do ponto, implementada no `lathe`, no `contorno` do `loft` e nos contornos do `inflate` | provada em peça de produto e não automotiva | Ciclo 5 ("Curva e filete v1"): o 3º elemento reservado do ponto 2D vira um raio de concordância — substitui a quina entre dois segmentos por um arco analítico tangente aos dois, sem seno/cosseno no formato salvo (só o número do raio). Um helper único (`expandirConcordancias`/`arcoDeConcordancia` em `motor/oficina.js`) atende os três lugares — cada op continua com seu próprio código de numeração/faces, cego a se um ponto veio direto do autor ou de um arco. Fail-closed em toda validação (raio negativo, ponta de caminho aberto sem os dois vizinhos, tangência maior que o segmento adjacente, duas concordâncias vizinhas disputando o mesmo segmento). Medido: cada ponto amostrado do arco fica a <0,001% do raio declarado do centro analítico (bem dentro do <1% do gate), em duas discretizações (`segmentosCurva` 4 e 16). Provado no `pecas/_vaso.js` (louça, barriga+gargalo+borda, 0 id cru) e no ombro do pneu de `pecas/roda-dianteira.js` (280F/320V -> 520F/560V no pneu, peça inteira 1,49x, envelope idêntico). Aditivo: `gabarito:selecao:check` verde nas 25 peças que não mudaram de propósito. Mutações: 5 testadas, 1 sobrevivente achado (troca cos/sin na amostragem do arco passava no teste de distância-ao-centro, porque cos²+sin²=1 preserva a distância mesmo com x/y invertidos) — morta depois de um teste que fixa a coordenada EXATA dos pontos de tangência, não só a distância | Vale para QUALQUER peça de qualquer família com um contorno/perfil 2D — nada aqui sabe o que é pneu, freio ou carro. Extração: as duas funções são puras (sem Three.js, sem estado externo), então o cherry-pick é direto |
| UP-026 | Filete de UMA aresta escolhida por identidade, sem regenerar a peça | provada em núcleo, em peça de exercício **e em peça de produto** | Medido na crítica visual do ciclo 5: a Oficina só sabia arredondar a CAIXA INTEIRA, com o `chamferBox`. Não havia como cortar uma aresta escolhida, e por isso toda peça saía de bloco. Entregue como a op `filete` (sem Three.js e sem conceito automotivo): `de` endereça a face por origem estrutural, `aresta` é a posição da aresta dentro daquele polígono, `raio` é a profundidade do corte. O que ela cria entra em `CONTRATOS_ORIGEM` com o eixo `painel`. Ela NÃO consome face nenhuma: as quatro faces que toca continuam vivas com o mesmo id. Custo fechado: +2 V e +1 F, seja qual for a peça. **O defeito que o primeiro desenho tinha, e a lei que sai dele:** para não mexer nas pontas da aresta, a primeira versão preservava os vértices originais dentro das duas faces. O neutro continuava FECHADO e a contagem BATIA, então nenhum teste do núcleo caía — mas a face ficava com um canto EM CIMA da aresta seguinte, um pico de área nula. Polígono que se toca. Quem gritou foi o ADAPTADOR, ao triangular em orelhas, e só quando a op chegou numa peça de verdade. LEI GERAL: **malha fechada e contagem certa não provam polígono SIMPLES** — são três propriedades, e um teste de núcleo que só olha as duas primeiras passa verde sobre uma peça que não desenha. O conserto: os vértices da aresta ANDAM para o lado da face de entrada, nascem dois do outro lado, e a terceira face de cada ponta ganha um canto, que é onde a fresta se fecha, sem triângulo avulso, sem junção em T e sem face coplanar sobreposta. Provas: 8 casos em `tools/oficina/oficina.test.ts`, incluindo a afirmação de que toda face do resultado é um polígono simples (nenhum canto cai sobre aresta que não seja a dele), a de 45°/45° na malha, e a varredura do acervo pelo adaptador. `gabarito:selecao:check` verde com 26 das 28 peças byte-idênticas — mudaram só as duas que a rodada reescreveu de propósito | Vale para móvel, ferramenta, máquina, estrutura e carro: aresta viva contra aresta cortada é a diferença entre bloco e peça acabada em toda família de objeto. Extração: a op `filete` e a entrada `filete` de `CONTRATOS_ORIGEM`. **Limites declarados:** UM painel só, então é chanfro e não arredondamento (A-36, aberto); ponta de aresta com mais de uma terceira face GRITA, o que exclui toda peça que já passou por `chamferBox` (A-37, aberto); o raio não é conferido contra o tamanho das faces vizinhas |

| UP-027 | Manifesto derivado de operações que publicam origem | provada em documentação/gate | `OPERACOES_COM_ORIGEM` é derivado de `CONTRATOS_ORIGEM` no núcleo, e o teste da skill compara a marca curta do manual a essa lista. A descoberta veio quando a skill ainda ensinava que `esfera`, `cone`, `plano`, `chamferBox` e `inflate` ignoravam `origemId`, apesar de o núcleo já publicar essas origens | Qualquer autor ou ferramenta pode consultar uma fonte única de capacidade sem duplicar uma tabela que envelhece. Extração: a exportação de `motor/oficina.js` e `tools/bancadas/skill-criar-peca.test.ts`; sem Three.js e sem domínio automotivo |
| UP-028 | Arredondamento real de aresta com canto composto | desenhado, ainda não implementado | O `filete` atual é um chanfro de um painel e recusa corretamente `chamferBox` porque cada ponta chega com duas faces adicionais. `FILETE-V2.md` define operação, identidade, raio e gate de descoberta | É capacidade geral de modelagem; só pode virar contribuição depois de núcleo, testes topológicos, prova não automotiva e compatibilidade com formatos anteriores |

## UP-005 — fronteira de renderização provada

**Problema observado:** `oficina.js` já separava `nucleo()` de `adaptarV3()`,
mas só existia um consumidor real. Isso deixava a independência do núcleo como
uma intenção arquitetural ainda não exercitada por outro motor.

**Solução geral:** `src/autoria/adaptar-three.js` consome apenas o estado neutro
(`V`, `F`, atributos e partes) e produz um grafo de cena. O núcleo herdado não
importa Three.js e não foi alterado.

**Provas:** `tools/mecanifica/adaptar-three.test.ts` verifica preservação de
identidade semântica, ausência de UUID persistido, triangulação e falha antes do
render quando há órfãos. O drone da Fase 4 foi convertido no navegador com 23
partes selecionáveis.

**Como aproveitar no NÓS:** o código Three.js não é candidato direto. A
contribuição útil é transformar a fronteira já existente em contrato explícito
e testável para múltiplos adaptadores. Ainda falta isolar tipos do estado neutro
e documentar quais atributos todo adaptador deve suportar.

**Limites:** a primeira ponte não cobre atlas pintável, suavização compartilhada,
skinning nem animações do adaptador v3. Esses recursos só serão generalizados
quando uma peça real da Mecanifica os exigir.

## UP-006 — semântica do drone provada pela bancada

**Problema observado:** a lente e o pouso eram nomeados por regiões sobrepostas
na origem. O replay permanecia tecnicamente válido, mas seis laterais do
cilindro viravam `pouso`, seis ficavam sem parte e apenas as tampas eram
`lente`.

**Solução geral:** cilindro, cubos e saídas de espelhamento são selecionados por
origem estrutural. O adaptador também publica a contagem de faces sem parte como
diagnóstico explícito.

**Provas:** `tools/mecanifica/drone-semantica.test.ts` conta a cobertura de cada
parte e exige zero faces sem identidade. A bancada reproduziu o defeito,
isolou os lotes contaminados e validou a correção nas mesmas vistas.

**Como aproveitar no NÓS:** a troca das seleções do
`prototipos/fps/v3/pecas/drone-inspecao.js` e o teste de cobertura são
candidatos diretos. O painel Three.js não é necessário para o cherry-pick.

**Limites:** o gate atual detecta ausência de nome; ainda não prova sozinho que
um nome existente significa o conjunto correto. UP-004 continua aberto.

## UP-013 — descrição geométrica com interpretação rígida opcional

**Problema observado:** a inspeção visual do freio não tinha escala nem eixo.
Quatro leituras de PNG foram necessárias para confirmar uma geometria que um
relatório numérico deveria responder diretamente.

**Solução geral:** `src/autoria/descrever-partes.js` calcula, a partir do estado
neutro, caixas, centros, dimensões, faces e componentes conexos por parte. Isso
não importa Three.js, carro ou freio. A relação entre duas partes é derivada da
caixa de cada corpo conectado, não de IDs ou de faces isoladas.

**Escopo:** caixa, dimensão e decomposição em componentes são observações de
geometria. A leitura `folga`/`encosta`/`interpenetra` é uma convenção para corpos
rígidos com portas de montagem declaradas; ela foi provada no freio, não em
malha orgânica deformável.

**Como aproveitar no NÓS:** extraia primeiro a medição neutra e seu
determinismo. Exponha a classificação de relação como consulta ou regra
explicitamente habilitada por par de partes, nunca como reprovação implícita de
qualquer objeto. Um consumidor orgânico pode usar as mesmas caixas para orientar
uma câmera, sem interpretar a sobreposição como defeito.

**Limites:** AABB é uma aproximação conservadora: folga comprovada é segura, mas
interpenetração pode ser apenas das caixas. Não substitui colisão de sólidos,
contato em superfície curva, análise de continuidade, validação de pele ou
deformação por esqueleto. Antes de chamar esta capacidade de pronta para upstream,
prove-a também em uma peça não mecânica e registre quais relações, se houver,
são esperadas.

## Modelo de entrada futura

Ao concluir uma capacidade, acrescente:

```text
ID:
estado:
problema observado:
solução geral:
arquivos:
testes:
compatibilidade:
como extrair:
escopo geométrico:
limites:
```

O histórico detalhado pode viver em um relatório próprio; esta página continua
sendo o índice curto para quem mantém o NÓS.
