# Registro de Decisões — NÓS

> Cada decisão importante entra aqui com o porquê. Antes de re-discutir algo, leia a entrada. Formato: D-nº · data · decisão · motivo.
>
> **Dois níveis** (o arquivo virou paredão, reorganizado): abaixo, o **índice de TODAS** as decisões (D-01…D-66) e o **detalhe das ATIVAS** (a era v3, D-55→). O detalhe histórico de D-01…D-54 vive em [`DECISIONS-ARCHIVE.md`](./DECISIONS-ARCHIVE.md) — nada foi alterado, só movido. Precisa do porquê de uma decisão antiga? Abra o arquivo e busque o ID.

## Índice — todas as decisões

- **D-01** · 2026-07-14 · Nome: NÓS
- **D-02** · 2026-07-14 · Repo público dedicado (`brigsd/nos`)
- **D-03** · 2026-07-14 · 100% GitHub
- **D-04** · 2026-07-14 · Tick em lote, 1 commit por tick
- **D-05** · 2026-07-14 · Combate por turnos autoritativo (v2)
- **D-06** · 2026-07-14 · TypeScript em engine e cliente
- **D-07** · 2026-07-14 · Pages via artifact deploy
- **D-08** · 2026-07-14 · Arte: pixel art 16×16, paleta Resurrect 64
- **D-09** · 2026-07-14 · NPCs v1/v2 sem LLM em runtime
- **D-10** · 2026-07-14 · Estrutura de agentes + docs de continuidade
- **D-11** · 2026-07-14 · Tick híbrido: relógio + evento
- **D-12** · 2026-07-14 · UI otimista no cliente
- **D-13** · 2026-07-14 · Login GitHub no site via OAuth device flow (v2)
- **D-14** · 2026-07-14 · Notificações via resposta de issue
- **D-15** · 2026-07-14 · NPCs com IA via GitHub Models (v2.5)
- **D-16** · 2026-07-14 · A Crônica auto-escrita
- **D-17** · 2026-07-14 · Federação de mundos (v3)
- **D-18** · 2026-07-14 · Quests que rendem achievements reais (v3)
- **D-19** · 2026-07-14 · Tick auto-corretivo + snapshots
- **D-20** · 2026-07-14 · Sem dinheiro. Ponto
- **D-21** · 2026-07-14 · Economia federada: ledger central + leitura mútua (v3)
- **D-22** · 2026-07-15 · "O Registro": o eu oficial é sólido, o eu local é fantasma
- **D-23** · 2026-07-15 · O Coração é A Fábrica — mundo pacífico, sem combate
- **D-24** · 2026-07-15 · Autonomia total do coder + política de agentes
- **D-25** · 2026-07-15 · Fechamentos de design da Fábrica e do cliente
- **D-26** · 2026-07-15 · Janela WebGL (PixiJS) como opt-in; Canvas2D padrão e fallback permanente
- **D-27** · 2026-07-15 · Portais fase 1: travessia sem sair do site, protocolo de registro, visitar é só-leitura (R6, atualiza o D-17)
- **D-28** · 2026-07-15 · Protocolo de sinalização P2P sobre GitHub (R7 "Fluidez A", fecha o D-25c)
- **D-29** · 2026-07-16 · A primeira pessoa é o cliente oficial
- **D-30** · 2026-07-16 · Dieta do repo: binários regeneráveis nunca na main
- **D-31** · 2026-07-16 · O carreiro, a clareira e o novo spawn (level design do cliente oficial)
- **D-32** · 2026-07-16 · A Clareira construída (v1, client-side) — telhados de cobre oxidado, veias de luz, nome oficial
- **D-33** · 2026-07-16 · Vocabulário ideador↔coder: IDs visíveis, setores e ?cam= (mecânica permanente)
- **D-34** · 2026-07-16 · Os Habitantes: cidadania por repositório (fase 1+2 no ar)
- **D-35** · 2026-07-16 · A bancada do coder: limitações viram ferramentas pelo diário de atrito
- **D-36** · 2026-07-16 · GI assada: o Actions é o render farm (passe A dos "gráficos realistas que pesam pouco")
- **D-37** · 2026-07-16 · A Clareira v2: um portal só (que mostra os mundos), bancas fora, alas com carpintaria
- **D-38** · 2026-07-16 · O chafariz: a água do Pulso aflora no coração do largo (forma final: v3)
- **D-39** · 2026-07-17 · O playtest do ideador: segundo hit no DDA, arco de entrada, ruínas ocultas, névoa removida, joysticks de celular
- **D-40** · 2026-07-17 · Som: áudio 100% sintetizado (Web Audio), zero arquivo no repo — ambiência v1 (vento + água do chafariz por proximidade)
- **D-41** · 2026-07-17 · Manutenibilidade do som + balões dos NPCs só de frente + painel dos Mundos em acordeão com "entrar"
- **D-42** · 2026-07-17 · Teste do pipeline de arte: o avatar da brasa, e os limites que ele revelou
- **D-43** · 2026-07-17 · Auditoria e arrumação do tooling: "deixar tudo redondo"
- **D-44** · 2026-07-17 · Os experimentos de resolução: onde o pixel vale e onde ele é jogado fora (medido, não teorizado)
- **D-45** · 2026-07-17 · Renderização em camadas: 640×360 ~2× mais rápido sem perder o visual (medido por fase)
- **D-47** · 2026-07-17 · O joystick cravado (2 buracos de pointer capture) + terceira rodada de otimização
- **D-48** · 2026-07-17 · O joystick que "buga depois de um tempo": o guarda do D-47 criou uma ratoeira — corrigido com defesa em 4 camadas
- **D-49** · 2026-07-17 · Billboards ORIENTADOS (arquitetura ancorada no mundo) + troca de preset sem teleporte + blindagem de gesto no celular
- **D-50** · 2026-07-17 · Profundidade nos arcos (extrusão em fatias) + A PRANCHETA (câmera de topo) + skill /estruturas
- **D-51** · 2026-07-17 · PLANTA v1 provada: o Santuário do Pulso nasceu de um blueprint
- **D-52** · 2026-07-17 · Lar unificado das ferramentas: `tools/` + índice
- **D-53** · 2026-07-17 · O motor de SETOR/PORTAL: a primeira casa de verdade, com interior
- **D-54** · 2026-07-17 · GPU no cliente: WebGL provado, beleza iterada com o ideador
- **D-55** · 2026-07-17 · Rumo v2/v3 + A OFICINA (ambiente padrão de criação)
- **D-56** · 2026-07-17 · O plano da potência: objetivo final + arsenal de ferramentas (docs/FERRAMENTAS.md)
- **D-57** · 2026-07-18 · O gargalo é o briefing, não a ferramenta + escopo do v3 travado (linguagem + prune)
- **D-58** · 2026-07-18 · O chão v3 nasce (ilha-chao) + o bug do hash2 — primeira vitória do senso crítico [cpu]
- **D-59** · 2026-07-18 · Port das ÁRVORES da V2 pro v3 (growTree extraído fiel) + 2 consertos de motor
- **D-60** · 2026-07-18 · Ferramentas de senso crítico [cpu] CONSTRUÍDAS e MEDIDAS por benchmark (o pedido: "descobrir quais ajudam, iterar, listar o que não ajuda")
- **D-61** · 2026-07-18 · Resolução de textura MEDIDA (32/64/128px) + o alicerce jogável do v3 (câmera livre, som, tiers de gráfico, menu de pausa/configurações)
- **D-62** · 2026-07-18 · Vila-ruína transplantada da tree3d-leaves pra v2 (main) + árvores 3D; NPCs/máquinas/monólitos fora; arco/portal SEM gambiarra de profundidade + peça `arco` v3 de geometria real
- **D-62b** · 2026-07-18 · Copa das árvores da v2 SEM CORTE (buffer 166→186 medido): carvalho/copado — as 2 espécies mais largas — deixam de sair fatiadas
- **D-63** · 2026-07-19 · Virada CARTOON das árvores do v3 (contorno de casca + cor de contorno por-lote + cel-shading no motor; elenco todo no estilo) + carimbo PLANTÁVEL (motor/arvore-cartoon.js)
- **D-64** · 2026-07-19 · Vegetação cartoon plantável (arbusto + flor + tufo) + ritmo de vento POR-LOTE no motor (windF: grama rápida, flor média, arbusto/árvore lentos)
- **D-65** · 2026-07-19 · A bancada ganha OLHO DE FORMA (render sem textura: normais/flat, --giro, alta-res) + a regra de comportamento: FORMA quem conclui é o ideador, eu só aponto defeito
- **D-66** · 2026-07-19 · Elenco fecha com seca/frondosa/raiz: copa FUNDIDA por SDF (frondosa), "pé vira tronco" (loft único raízes→tronco) e junção dos galhos disfarçada por PENUMBRA de textura
- **D-67** · 2026-07-20 · A 'raiz' muda de abordagem: volta ao tronco separado (galhos mais naturais), textura 100% uniforme, resolução de lados casada e o PÉ DE ELEFANTE integrado na geometria do tronco (zero emenda de verdade, não só disfarce de cor)
- **D-68** · 2026-07-20 · Merge da branch `colaborador4` (41 commits: câmera/colisão/HUD/gráficos/áudio/ferramentas) pra main; conflito real só em render.js, mesclado à mão (vento/toon/debug meu + aa/recon/projetar/depurar dele)
- **D-69** · 2026-07-20 · README com 3 botões claros (Jogar V1/V2/V3) + lacuna de deploy do V3 fechada (pages.yml passa a observar `prototipos/fps/**`, gap #1 documentado desde D-36 nunca corrigido)
- **D-70** · 2026-07-20 · MAPA gerado do repositório (`npm run mapa` + gate `mapa:check` no CI): árvore com resumo por arquivo, puxado do cabeçalho do próprio arquivo — índice que não apodrece por construção
- **D-71** · 2026-07-20 · O papel do ROBÔ (CI/Actions) fechado: conferidor+forno+relógio, nunca motor-ao-vivo nem conserto autônomo; ronda-da-oficina, sentinela-de-custo, diff no robô, aquecimento de shader como OPÇÃO no jogo; 3 sacadas novas (painel-de-botões, PR-com-olhos, ronda-profunda) — FERRAMENTAS.md §7
- **D-72** · 2026-07-20 · Reciclagem de commits do tick: NÃO agora (a 1 tick/h, 1 milhão de commits é ~1 século; o que degrada é tamanho/clone, gradual). Deletar commit avulso reescreve todo o histórico downstream (quebra forks — fatal na federação). Se um dia: horizonte-squash do histórico ANTIGO + doc-resumo, deliberado, nunca automático
- **D-73** · 2026-07-20 · Oficina, rodada de interface e funções (aprovada pelo ideador): layout comum (cena central + painel à direita que troca por espaço); funções extras de modelagem (valor exato, espelho, duplicar, medir, isolar; e seleção-por-critério pra IA); pintura com cor LIVRE (sem trava de paleta), densidade de texel, pincéis duro→macio→texturizado→esfumar + shift-reta/conta-gotas/balde/simetria/gradiente/estabilizador; som em DOIS níveis (blocos + presets); animação gatilho (modo uma-vez) e dirigido-por-estado (entrada)
- **D-74** · 2026-07-20 · Direção de áudio/cenas (`docs/AUDIO_E_CENAS.md`): música/voz/cutscenes como código/dados (não arquivo); teto de qualidade por síntese (FM, modelagem física, reverb por código, AudioWorklet), samples = exceção; wavetables/ciclo-único como POSSIBILIDADE; e — atendendo a preocupação de retrofit — reservar a arquitetura cedo (instrumento trocável + slot de efeitos) pra subir qualidade ser aditivo
- **D-75** · 2026-07-21 · Fundação gráfica travada: WebGL 2 como piso universal (todo mundo entra, PC ou celular) + renderizador trocável reservando o WebGPU pra depois — aditivo, não reescrita; a abstração NÃO é construída agora (só reservada no doc)
- **D-76** · 2026-07-21 · Migração WebGL 2 FEITA (passo 0 da Oficina): render.js em GLSL ES 300 es, saída BYTE-idêntica (20 renders por `cmp`), só o motor mudou; coder isolado + auditoria pelo orquestrador (diff + olho + byte-a-byte), revisor extra dispensado pela prova byte-idêntica; vértice cor/osso segue reservado (passo 14), WebGPU reservado (D-75)
- **D-77** · 2026-07-21 · Passo 1 da Oficina FEITO: núcleo `motor/oficina.js` (vértices únicos + faces + identidades posicionais por bloco, executor + adaptador v3 + colisaoDe), vocabulário inicial, bancada `executar` (replay provado) e peça `_oficina-toco`. Coder + revisor ADVERSARIAL que pegou 3 bugs que a revisão amistosa passou (D1 normal invertida da tampa/ALTA, D2 bowtie da mescla, D3 overflow de bloco) — todos consertados antes da main; formato salvo intacto
- **D-78** · 2026-07-21 · Passo 2 da Oficina FEITO: `oficina.html` (esqueleto D-73 + câmera do editor órbita/pan/zoom, cursor LIVRE) dirigindo o motor por `setCam` sem tocar render.js; centragem provada por `projetar` (0.00px). Revisor adversarial dispensado (núcleo de risco objetivamente provado, é interface). 2 nuances abertas: centrar na área visível (pede projeção deslocada, motor) e auto-enquadrar peça arbitrária
- **D-79** · 2026-07-21 · Lente deslocada no motor (fecha a nuance #1 do D-78): projeção assimétrica opt-in (`setLente`, NDC x,y; [0,0]=sem efeito, jogo/visor byte-idênticos) — a Oficina centra o objeto na área livre (fora do painel) sem mover o alvo da órbita. Alvo estável (0.00px) E no centro da área útil (376px medido); nuance #2 (auto-enquadrar) segue aberta
- **D-80** · 2026-07-21 · Passo 3 da Oficina FEITO: overlay 2D dos vértices (pontos) + faces (arestas) por cima da cena, projetado por `visor.projetar` e redesenhado no `antesDoQuadro` (casa com o quadro + lente D-79); `pointer-events:none`. Alinhamento provado por bbox de pixels (19/19 dentro), acompanha a órbita; tecla `i` mostra ids (1001 ausente = mesclado, prova a identidade). render.js/núcleo intactos; revisor adversarial dispensado (interface, alinhamento provado por pixel)
- **D-81** · 2026-07-21 · Passo 4 FEITO — o MILESTONE (arrastar um vértice → `moveV` → replay): hit-test câmera-vs-vértice no `pointerdown`, arrasto 2D→3D no plano da câmera na profundidade do vértice (segue o cursor 0.04-0.06px), grava `moveV`, lista editada re-executada bit-idêntica página==Node. Revisor adversarial pegou 1 bug (roda no meio do arrasto corrompia o `moveV` salvo) — consertado + teste (4g); núcleo/motor intactos. "O teste da ideia inteira" passou; a Oficina virou EDITOR
- **D-82** · 2026-07-21 · Passo 5 FEITO (desfazer/refazer sobre a lista de operações): Ctrl+Z/Y/Shift+Z; baseline trava as operações do arquivo (desfaz só a sessão, não a peça); edição nova limpa o redo. Desfazer traz o neutro bit-a-bit ao de antes, refazer ao de depois (bancada, teclas reais). Revisão do orquestrador pegou 1 bug (Ctrl+Z no meio do arrasto tangleava a lista, classe da brecha da roda) — guarda `if(arrasto)` + teste (5 guarda); núcleo/motor intactos
- **D-83** · 2026-07-21 · Passo 6 FEITO (gizmo de eixos + painel): setas X/Y/Z (tamanho `dist/8`, trava `compr<12px`) pra arrasto TRAVADO por eixo — `d=eixo·avanço`, vazamento 0 por construção; mesma máquina de arrasto (guardas 4/5 cobrem); painel mostra vértice+dimensões + valor exato editável. Passe adversarial: PRONTO PRA MAIN, só polimento; consertei o D3 (re-digitar o valor exibido gravava moveV fantasma sub-visual). D1 (seta cobre vértice ~21%) e D4 (sem clamp) ficam como nuances abertas de UX; núcleo/motor intactos
- **D-84** · 2026-07-22 · D1 do passo 6 RESOLVIDO (a seta do gizmo cobrindo outro vértice roubava o clique): o hit-test do `pointerdown` passou a dar PRECEDÊNCIA ao vértice clicado direto — ordem vértice-outro (≠ selecionado) → seta → vértice-selecionado (base, zona morta). Só `oficina.html`; teste de regressão (6 D1) que varre ângulos, acha a oclusão real e prova o clique selecionando o vértice — e que FALHA sem o conserto. `npm test` 383 verde, núcleo/motor intactos; sobra só a D4 (sem clamp, undoável)
- **D-85** · 2026-07-22 · D4 do passo 6 RESOLVIDO (campo de valor exato sem limite): agora recusa número fora de ±100 unidades — a peça é local (~1 unidade), então um `1e6` digitado não manda mais o vértice pra longe. Mesmo tratamento do não-finito (repinta, volta ao valor atual). Só `oficina.html`; teste (6 valor D4) prova a recusa e falha sem o limite. Com o D1 (D-84), fecha as nuances abertas do passo 6
- **D-86** · 2026-07-22 · Passo 7 FEITO (extrudar UMA face pela interface): seleciona uma FACE (a da FRENTE por profundidade do centroide, adiada pro pointerup pra não deselecionar ao orbitar) e arrasta um HANDLE na NORMAL → grava `['extruda',{face,dist}]`. Reusa a op do núcleo (INTOCADO) e a MESMA máquina de arrasto do passo 6 (as guardas de roda/Ctrl+Z já cobrem); a normal do handle é o Newell espelhado byte-a-byte do núcleo. Coder num wip + verificação por medição (bench 25 provas, replay página==Node, dist·compr batendo o cursor 0px, anel no bloco idx·1000) + prova adversarial em NODE da classe formato-salvo (edição composta determinística, órfão grita-não-corrompe com V/F idênticos, inset, empilhar 4×). Extrusão de REGIÃO (várias faces) fica pra depois (muda o núcleo). O revisor adversarial dedicado passou de 1h30 sem concluir/entregar; assumi a prova crítica eu mesmo em Node
- **D-87** · 2026-07-22 · Passo 8 FEITO (mesclar vértices + ímã): MULTI-seleção de vértice (Shift+clique, toggle adiado pro pointerup; o ATIVO é o último e dirige gizmo/painel — com 1 vértice idêntico aos passos 6/7, via a fonte única `definirSel`); tecla M/botão grava `['mescla',{de:[não-ativos ordenados],para:ATIVO}]` (a op provada do núcleo, INTOCADO); ÍMÃ com Ctrl no arrasto cola o vértice na posição EXATA do vizinho (`d=posAlvo−orig`), reusando a máquina de arrasto — a guarda `if(arrasto)` do desfazer já cobre o Ctrl-segurado (Ctrl+Z no meio do arrasto-com-ímã é ignorado). Coder num wip + verificação por medição (bench 35 provas, replay página==Node, ímã erro 5.6e-17) + prova adversarial em NODE da classe identidade/formato (mescla determinística e independe da ordem do `de`, cadeia A→B→C, bowtie grita-não-corrompe, órfão pós-mescla com V/F idênticos, auto-mescla no-op). Colar em face e lint de malha visual ficam pra depois; adversarial feito em Node de propósito (o revisor-subagente travou no passo 7)
- **D-88** · 2026-07-22 · Passo 9 FEITO (pintar faces — o pincel modo 'face'): MULTI-seleção de FACE (Shift+clique, espelha o passo 8; fonte única `definirSelFace`, XOR com vértice imposto nos DOIS sentidos) + um `<input type=color>`+presets no painel cujo `change`/clique gravam `['pincel',{modo:'face',faces:[ordenadas],cor}]` (a op provada do núcleo, INTOCADO), renderizando pelo SWATCH que já existe (`reexec` recarrega a textura). **Projeção em caixa / textura pintável ADIADA pro passo 11** — decisão do ideador: box projection faz topo/fundo de objeto fechado caírem no mesmo pedaço da textura (pintar um pinta o outro), pior que o swatch pra cor por face; quem precisa dela é o pincel macio. Verificado por medição (bench 26 provas, incl. probe de pixel do render madeira→azul; replay página==Node) + Node (última-pincelada-vence, cor sobrevive à extruda, pincel em face morta grita, modo 'livre' reservado grita). Guarda anti-arrasto + no-op de cor-igual (anti-D3); COR_PADRAO espelhado do núcleo. **Princípio novo (do ideador): feature com pré-requisito num passo posterior pode ser transferida pra lá** (registrado no `nos-fluxo`)
- **D-89** · 2026-07-22 · Passo 10 FEITO (exportar código + colisão automática): serializa o estado (PARAMS/TOPO/PASSOS/meta) num `.js` IDÊNTICO ao molde de peça — número via `String(double)` (reabre EXATO, sem arredondar), `colisao: colisaoDe(...)` como CHAMADA literal, cabeçalho gerado (passa `mapa:check` se commitada) — e a peça exportada REABRE bit-a-bit idêntica à editada. Servidor de dev NOVO (`tools/servir.mjs`): serve `prototipos/procedural/v3/` com `Cache-Control: no-store` + rota `POST /oficina/salvar` que grava em `pecas/<nome>.js`, com nome sanitizado (`[A-Za-z0-9_-]`, `.js` forçada) + confinamento por `resolve`/`startsWith(base+sep)` (barra traversal literal E codificado `%2e%2e`); download como fallback sem servidor. Interface de marcar `solido` (alimenta `colisaoDe`; aviso em destaque se nenhuma face sólida — senão a copa vira parede). Verificado: bench 26 (ida-e-volta bit-a-bit após editar, replay página==Node) + prova de segurança INDEPENDENTE em Node pelo orquestrador (traversal codificado não vaza, nomes maliciosos rejeitados). Núcleo/motor intactos
- **D-90** · 2026-07-22 · Passo 11a FEITO (fundação da textura pintável — meia-etapa do 11): o `adaptarV3` troca o SWATCH por um ATLAS por face — cada face uma ILHA própria (grade ~√N, ilha 32px, gutter 2px), UV por projeção em caixa DAQUELA face (privada, sem o furo do topo/fundo da caixa GLOBAL — que empilhavam no mesmo XZ, IoU 0.94). Em 11a a ilha é CHAPADA com a cor da face → render **BYTE-IDÊNTICO** ao swatch (0 pixels diff no cmp cross-versão; toco eyeball igual). **Primeira mudança no núcleo desde o passo 1, e SÓ no adaptador** (nucleo/neutroCanonico/ops/colisaoDe/`render.js` intactos; `adaptarV3` é importado só por peças da Oficina, não pelo jogo). Expõe `atlas.daFace(id)→{ilha,projeta}` (fonte única do UV) pro 11b converter superfície→texel. `npm test` 386, bancada 2-10 sem regressão. Escolhido com o ideador (opção "pincel completo"); o 11b (pincel macio) vem em cima desta base
- **D-91** · 2026-07-23 · Passo 11b (MOTOR) FEITO (pincel macio no núcleo — meia-etapa do 11): a op `pincel` aprendeu `modo:'livre'` — cada ponto `{f,a,b}` é um DAB numa face (`f.tinta`), com `{a,b}` FACE-LOCAL (o `s,t` da projeção do atlas em [0,1], não texel cru → a tinta ACOMPANHA a face num moveV) e `raio`/`dureza` por dab; o `adaptarV3` rasteriza um dab radial macio (núcleo duro + ombro smoothstep, alpha-over) na ilha, preso na célula. **Compat 'face' ESTRUTURAL:** `neutroCanonico` só anexa a tinta quando a face TEM dab, e a textura retorna a cor chapada quando não há dab → o toco (só 'face') canoniza E renderiza BYTE-idêntico (0 diff). Só `motor/oficina.js` (op `pincel` + `neutroCanonico` + `adaptarV3`); `render.js`/`oficina.html` intactos. Verificado por bancada+vitest (393) + prova adversarial em NODE do orquestrador (determinismo, round-trip, compat 'face', paint-follows-face, órfão, raio). 2ª mudança no núcleo desde o passo 1, e a que mexe no FORMATO SALVO. Falta o 11c (interface: pintar arrastando na superfície) pra fechar o passo 11
- **D-92** · 2026-07-23 · Passo 11 FECHADO (11c: a interface do pincel macio): modo pincel (chip "Pincel"/tecla B) — LIGADO, arrastar na superfície PINTA (raycast do cursor = inverso EXATO de `projetar`, COM a lente D-79 → round-trip 0.00px; acha a face da frente por `hitFace`, intersecta o plano dela → `{f,a,b}` face-local pela inversão do atlas), acumula dabs por distância (6px) com preview ao vivo e grava `['pincel',{modo:'livre',...}]`; raio/dureza/cor no painel; reusa a máquina `arrasto` (guardas de roda/Ctrl+Z cobrem). DESLIGADO, tudo segue como antes (sem regressão). Desfazível, replay página==Node, um arrasto atravessa faces num só passo. Só `oficina.html` (a op livre já vem provada do 11b-motor); `render.js`/motor intactos. **Com 11a (atlas) + 11b (op+raster) + 11c (interface), o passo 11 está COMPLETO** — a Oficina pinta degradê na superfície. Nuance: a borda sai pixelada (ilha 28×28 texels/face) — combina com o estilo, dá pra suavizar aumentando o tile
- **D-93** · 2026-07-23 · Passo 12a FEITO (materiais OPACOS — meia-etapa do 12; a 1ª vez que a Oficina toca a JÓIA `render.js`): a op `material` seta `f.material=nome` (declarado em `MATERIAIS` da peça, como PARAMS/TOPO; grita se `usa` não existe), o `adaptarV3` AGRUPA faces por material em LOTES (compartilhando o atlas; **peça sem material → 1 lote → mesh byte-idêntico ao 11a**), e o `render.js` ganha uniforms por-lote `uEmissivo/uAspereza/uSemLuz/uCorMul` no PADRÃO EXATO do `uRim` (default no-op). **A jóia fica BYTE-idêntica com material desligado** — todo efeito guardado `if(uX>0)` (código morto quando off), `tx.rgb *= (1,1,1)` (×1.0 EXATO em IEEE), `sh=shadow()` só renomeado; provado por leitura estrutural completa (cobre TODAS as cenas) + o cmp 7/7 do coder (jogo+peças, 0 pixel, com discriminação) + o jogo nem passa pelo `oficina.js` (só o `render.js` é compartilhado). UI mínima aplica material às faces (`['material',{faces,usa}]`); `neutroCanonico` inclui `f.material`, replay página==Node. `MATERIAIS` opcional (default {} = compat total). Transparência fica pro 12b. `npm test` 397, bancada 2-11 sem regressão
- **D-94** · 2026-07-23 · Passo 12 FECHADO (12b: a MISTURA transparente — completa os materiais): um material com `mistura:'transparente'` + `opacidade` (0..1, default 1) marca o lote (`adaptarV3`: `transparente`/`opacidade`), e o `render.js` desenha os transparentes numa PASSADA EXTRA depois dos opacos — de TRÁS PRA FRENTE (distância² do centroide LOCAL do lote transformado à câmera), com blend alpha OVER (precedente das partículas), depth-TEST ligado (opaco ainda oclui) e depth-WRITE off (`depthMask(false)` → um transparente não esconde o de trás), restaurando `depthMask(true)`+`disable(BLEND)` pras partículas verem o MESMO estado. **A jóia fica BYTE-idêntica sem transparente:** sem lote marcado roda o `draw(scene,AL,true)` de sempre (o ramo `else`), o `discard` vira `tx.a<0.5 && uOpacidade>=1.0` = o de antes (uOpacidade=1 EXATO), e o alpha é `1.0` literal; provado pelo cmp de relógio congelado do orquestrador (15 frames do JOGO — arvore3d/ilha-chao/casa-toras/vegetacao/arvore-cartoon × 3 ângulos — bit-a-bit idênticos trocando SÓ o `render.js`) + o cmp 20/20 do coder (jogo-padrão/recon inclusos) + prova de 2 LADOS (vidro transparente deixa o núcleo aceso aparecer ATRAVÉS da casca; forçado opaco esconde). UI: checkbox transparente + slider opacidade no bloco de material; peça-exemplo `_oficina-transp` (relicário de vidro). Limites ANOTADOS: ordenação por LOTE (não por triângulo) → dois transparentes muito interpenetrados podem compor fora de ordem; sombra dura via shadow-map (passe de profundidade intocado, menos risco na jóia). `npm test` 402, bancada 2-12 sem regressão. **Passo 12 (materiais) COMPLETO** — a Oficina tem materiais opacos (12a) e transparentes (12b)
- **D-95** · 2026-07-23 · Passo 13a FEITO (animação rígida por parte — o MOTOR, meia-etapa do 13; NÃO toca a jóia): a op `['parte',{nome,faces,pivo?}]` dá NOME a um conjunto de faces (`f.parte`), o `adaptarV3` passa a agrupar pela DUPLA (parte,material) — cada parte vira lote(s) com MATRIZ própria — e uma seção `ANIMACOES` da peça (`{duracao,repete,trilhas:[{parte,canal,chaves:[[t,v]]}]}`) vira `peca.animar` via `montarAnimar`. **A jóia `render.js` fica com DIFF VAZIO:** o animador entra pelo hook `if(animar) animar(T,lotes)` que JÁ existe (cada lote já tem `.matriz`=uModel), casando parte↔lote POR ÍNDICE (`infoPorLote`, paralelo aos lotes que o render mapeia 1:1) — nunca lê um campo novo do lote do render. Canais `rotX/Y/Z`+`posX/Y/Z` (somam, 0 default) + `escala` (multiplica, 1 default), interpolação SUAVE (smoothstep por segmento), pivô = centroide da parte (default) ou explícito (`pivo`); matriz `T(pos)·T(piv)·R·S·T(−piv)` coluna-major, determinística (sem Date/random; helpers de matriz LOCAIS ao oficina.js). **Compat byte-a-byte:** face sem parte → canon idêntica (guarda anexada como o `tinta`, string vs array disjuntos) e mesh idêntico (chave de grupo `\u0000` colapsa pra 1 lote na ordem de id); provado por cmp de relógio congelado do orquestrador (o `_oficina-toco` renderiza `c11454f27968ea0a` idêntico origin/main↔HEAD) + o `render.js` diff VAZIO (jogo intocado por construção, não passa pelo oficina.js). Anima de verdade: a peça-exemplo `_oficina-anim` (engrenagem no centroide + braço no pivô-base) gira visível em 3 fases, mesma fase 2x = idêntica (24126px entre fases, 0px na mesma). `neutroCanonico` inclui `f.parte`; replay página==Node. Canal desconhecido grita; órfão grita, malha intacta. `npm test` 409, bancada +11 oks. Fora de escopo (a spec manda): gatilho `uma-vez`/`entrada:'velocidade'` (D-73, quando a movimentação chegar) e o esqueleto que dobra malha (passo 14). Falta o 13b: a interface (linha do tempo, nomear parte, gravar/serializar ANIMACOES)
- **D-96** · 2026-07-23 · Passo 13 FECHADO (13b: a INTERFACE do espaço Animação — SÓ `oficina.html`, motor e JÓIA intocados): um SELETOR DE ESPAÇO no topo (chips Modelar/Animação, estilo do chip Pincel) entra na autoria mantendo câmera/seleção/objeto (é ESPAÇO, não aba, D-73); no espaço Animação aparecem o painel de partes/animações/trilhas (à direita) + a LINHA DO TEMPO embaixo (régua + playhead + uma faixa por trilha com as chaves como pontos), e os hit-tests de modelagem desligam — só a seleção de FACE segue viva, pra nomear parte. Nomear parte grava `['parte',{nome,faces}]` (a op do 13a; desfazível pela pilha do passo 5); as animações vivem em `animacoesAtual` (a seção ANIMACOES MUTÁVEL, como MATERIAIS): criar animação (duracao/repete), trilha (parte+canal), chaves `[t,v]`. **PREVIEW ao vivo JÓIA-SAFE:** o `reexec` fia ANIMACOES no `executar` (o 13a monta o `animar` via `montarAnimar`) e EMBRULHA o `animar` num wrapper que lê `previewT` — scrub congela a pose (previewT=segundos), play volta ao relógio (previewT=null→laço) — **sem tocar no `render.js`** (segue chamando `animar(T,lotes)` por quadro). `serializarPeca` ganha ANIMACOES (bloco `export const ANIMACOES` + arg no `construir`, como MATERIAIS; ANIMACOES DEPOIS de MATERIAIS → peça sem anim byte-idêntica). **Provado:** `git diff origin/main -- motor/oficina.js render.js` = VAZIO (confirmado); round-trip ida-e-volta (PASSOS com a op `parte` + ANIMACOES bit-a-bit + a POSE em T=1 página==Node) na bancada E no cmp do orquestrador (serializar REAL da UI → reimport, PASSOS/ANIMACOES/MATERIAIS idênticos no `_oficina-anim`); preview discrimina por pixel (mesma T 2x = 0px, T=0 vs T=2 = 26875px, scrub real na régua); nomear/undo/redo, guardas (canal/parte inválidos no-op). `npm test` 409, bancada 275 oks. Cortes ANOTADOS: pivô só centroide na UI (o op suporta explícito), arrastar chave no tempo via campo (não drag), várias animações presentes mas provado com uma. **Passo 13 (animação) COMPLETO** — a Oficina autora e exporta animação rígida por parte em laço; falta só o passo 14 (esqueleto que dobra a malha)
- **D-97** · 2026-07-23 · Passo 14a FEITO (esqueleto com deformação suave — o MOTOR, meia-etapa do 14; a 1ª vez que a animação toca a JÓIA `render.js` DE VERDADE): a malha DOBRA suave nas juntas (linear blend skinning), não em pedaços rígidos (isso é o 13). Modelo de dados declarável em código (a UI é o 14b): `ESQUELETO = {ossos:[{nome,pai?,pivo?}]}` (hierarquia + pivô; valida pai/ciclo/teto=32, senão GRITA) + a op `['pesar',{osso,vs?/faces?,peso}]` (acumula peso por (vértice,osso); o `adaptarV3` normaliza top-4 influências; osso/vértice inexistente = órfão). **A JÓIA fica byte-idêntica sem esqueleto:** o skinning é ADITIVO (precedente da passada transparente do 12b) — lote SEM osso segue em 8 floats/vértice no shader/`draw`/`glMesh` de HOJE (INTOCADOS); lote COM osso usa 16 floats + um PROGRAMA skinado NOVO (VS soma `Σ peso·skin(osso)·pos`, FS cópia verbatim do de cena) num passe SEPARADO, gated em `lotesSkin.length` — sem lote skinado (todo jogo + peças 1..13) NADA novo roda. LBS determinístico: bind=identidade no pivô → skin=global·T(−pivo) (inversa EXATA, sem 4x4 geral); sem animação → identidade → deforma 0; trilha de ANIMACOES pode mirar um OSSO (o animador escreve `L.ossos` por quadro, o render sobe em `uOssos[]`) — alvo em `ossoSet` dirige skinning, fora dele é parte rígida (13a intocado). `neutroCanonico` inclui o peso do vértice (vértice sem peso → canon byte-idêntica). **Provado:** cmp de relógio congelado do orquestrador — peça do jogo (arvore3d/ilha-chao/casa-toras/vegetacao/arvore-cartoon × 3 ângulos) renderiza **15/15 byte-idêntico** trocando SÓ o `render.js` HEAD↔origin/main — + o frz-jewel 9/9 do coder; toco sem esqueleto canon+mesh byte-idêntico; a peça-exemplo `_oficina-esqueleto` (tentáculo de 3 ossos) DOBRA suave (junta mista = combinação convexa EXATA dos dois ossos, não rígida; T=0 reto vs T=1.5 curvado 15818px, mesma fase 0px); matrizes de osso página==Node em 5 tempos; órfão/ciclo/pai/teto GRITAM. `npm test` 417, bancada 282 oks. Cortes ANOTADOS: mesh skinado NÃO projeta sombra nem contorno (só o passe de cena, como o 12b deixou a sombra da transparência dura); 4 influências/vértice, teto 32 ossos; interface de rigging (pintar peso, editar ossos) = o 14b. Falta o 14b pra fechar o passo 14 (e o roteiro)
- **D-98** · 2026-07-23 · Passo 14 FECHADO — e com ele o ROTEIRO INTEIRO (passos 0–14) da Oficina (14b: a INTERFACE do rigging — SÓ `oficina.html`, motor e JÓIA intocados): no espaço Animação (o do 13b), um painel ESQUELETO cria/nomeia ossos com PAI (hierarquia) e PIVÔ (campos x/y/z ou "= centroide das faces selecionadas"), lista a hierarquia (chips `b0`/`b1←b0`/…), e valida PELO MOTOR (`nucleo` com o ESQUELETO): ciclo/pai/teto GRITAM, o erro aparece na UI e o reexec cai pra esqueleto null via try/catch — não quebra. PESAR: face(s) + osso ativo + slider de peso → grava `['pesar',{osso,faces:[ordenadas],peso}]` no fim de PASSOS (desfazível); pesares acumulam (o motor normaliza top-4) → junta suave = as mesmas faces 50% num osso + 50% no outro; um HEATMAP no overlay tinge as faces do osso ATIVO. POSAR: o dropdown de alvo da trilha (13b) passou a listar OSSOS além de partes → uma trilha que gira um osso DEFORMA a malha no preview (`montarAnimar` do 14a resolve osso×parte, escreve `L.ossos`). **PREVIEW JÓIA-SAFE:** o `reexec` fia `esqueletoAtual` no `executar` (7º arg) e no `nucleo` do overlay; peça SEM osso passa **null** (não `{}`, que marcaria skin com 0 ossos) → byte-idêntico. `serializarPeca` ganha ESQUELETO (`export const ESQUELETO` + 7º arg posicional). **Provado:** `git diff origin/main -- motor/oficina.js render.js` = VAZIO (confirmado); round-trip ida-e-volta (PASSOS com `pesar` + ESQUELETO bit-a-bit + a pose skinada em T=1 página==Node, vértice deslocou 0.363 do repouso) na bancada E no cmp do orquestrador (serializar REAL da UI → reimport no `_oficina-esqueleto`: PASSOS/ESQUELETO/ANIMACOES idênticos, 6 `pesar`, ossos b0/b1/b2); junta 50/50 = combinação convexa (0.500/0.500); preview deforma (26630px); ciclo/pai GRITAM na UI sem quebrar; undo/redo. `npm test` 417, bancada 304 oks. Cortes ANOTADOS: ossos são seção MUTÁVEL (criar/remover osso não é desfazível, só o `pesar`; `removerOsso` re-enraíza os filhos); sem pincel-de-peso por raycast nem seleção de vértice fina (o heatmap + peso por face bastam pro loop). **A OFICINA ESTÁ COMPLETA (passos 0–14):** modelar (vértice/face/extrudar/mesclar+ímã), pintar (cor por face + pincel macio no atlas), exportar+colisão, materiais (opacos + transparentes) e animar (rígida por parte + esqueleto com deformação suave) — tudo dirigindo o MESMO motor do jogo, com a jóia byte-idêntica quando cada recurso está desligado
- **D-99** · 2026-07-23 · Aba Som, passo 1 FEITO (a FUNDAÇÃO — o núcleo do evento de som + o adaptador Web Audio + a bancada de replay). Começa o roteiro NOVO da Aba Som (paralelo à Oficina, mas pro áudio): o EVENTO de som (um passo, uma bolha) é uma peça — PARAMS + PASSOS (grafo de sinal) + `semente` + `construir`, determinístico. `motor/somnucleo.js` (NOVO) resolve os PASSOS num GRAFO em dados, headless e determinístico (o análogo do `nucleo`): vocabulário `oscilador`/`ruido`/`filtro`/`envelope`/`ganho`/`soma` + **`lfo`** (modula um param de outro nó) + **`alturaEnv`** (sweep de frequência); identidade por `id` (não posicional — os passos se referenciam por nome), órfão/id-duplicado/ciclo GRITAM (aresta de volta derrubada, o resto renderiza), saída = o único nó de áudio livre (com guarda de "toco mudo" pra um órfão não sequestrar o render), `somCanonico` pro replay, `rng(semente)` mulberry32 (Math.random cru PROIBIDO). `motor/somweb.js` (NOVO) é o adaptador grafo→Web Audio (`construirGrafo` + `renderarOffline` via OfflineAudioContext + `tocarEvento`) — o par do `adaptarV3`. **O `som.js` do JOGO fica INTOCADO** (diff vazio; os arquivos novos só são importados pelo bench+peça-exemplo, não pelo jogo — amarrar é o passo posterior). **Provado:** o cmp de som do orquestrador (o `_bolha` renderiza **byte-a-byte idêntico 2×** — 14553 amostras, mesmo hash — e o hash MUDA quando um param muda) + a bancada `sintetizar` (replay delta MÁX 0, `lfo`/`soma`/`semente` discriminam por energia espectral, validação grita) + 15 testes vitest (núcleo sem browser). Evento-exemplo `_bolha` (a bolha do jogo como grafo: oscilador+alturaEnv+envelope) no diretório novo `pecas-som/`. `npm test` 432. Falta: a casca da aba (S2), os blocos ao vivo (S3), os presets do catálogo (S4), exportar + amarrar no `som.js` do jogo (S5)
- **D-100** · 2026-07-23 · Aba Som, passo S2 FEITO (a CASCA — VER a onda e OUVIR). Página NOVA `prototipos/procedural/v3/som.html` (aba = contexto SEPARADO, D-73: sem câmera/malha) com o esqueleto da Oficina — forma de onda no CENTRO (canvas), transporte (Play + loop + duração) EMBAIXO, o grafo do sinal + placeholder dos blocos À DIREITA (foreshadow do S3). Carrega um evento de `pecas-som/` (via `?som=`, default `_bolha`, + seletor), desenha a onda de `renderarOffline` (min/max por coluna; canvas ESTÁTICO pra o hash bater, playhead é um div à parte), e o Play (+ tecla espaço) cria o AudioContext DENTRO do gesto e liga o evento (`tocarEvento`) na saída — som de verdade. Reusa o motor do S1 (`somnucleo`/`somweb`); **`som.js` do jogo INTOCADO** (diff vazio). O chip "Som" do `oficina.html` virou link pra `som.html` (Objeto↔Som navega; a aba Objeto segue carregando limpa — verificado, __ready + 0 erro). **Provado:** a bancada `somtela` (18 oks) + o eyeball do orquestrador — a onda da bolha (pico alto à esquerda decaindo ao silêncio) BATE com as amostras (pico do desenho na coluna do pico real, faixa esquerda = ataque cedo 16.4 ms), o desenho é DETERMINÍSTICO (mesmo evento → mesmo hash de pixels, diferente → muda), o Play liga o grafo vivo (sem gesto = sem contexto; clique/espaço → AudioContext 'running' + saída no destination), e a nav Objeto↔Som funciona sem erro. `npm test` 432 (sem regressão). Falta: S3 blocos ao vivo, S4 presets, S5 exportar + amarrar
- **D-101** · 2026-07-23 · Aba Som, passo S3 FEITO (os BLOCOS AO VIVO — o painel direito virou o EDITOR do grafo). SÓ `som.html` (jóia `som.js` + `oficina.html` diff VAZIO). Cada nó vira um CARD com os params como CONTROLES — slider+campo `type=number` pro número (o campo alcança fora da faixa e valores pequenos), dropdown pro enum (tipo/cor/filtro); a saída destacada, o "mod" no modulador. ADICIONAR bloco (os 8 tipos, id automático `osc1`/`flt1`/…), LIGAR o `de` por dropdown dos ids elegíveis (`soma` = chips de vários, `lfo`/`alturaEnv` = alvo+param válido por `PARAM_MOD`), REMOVER. Editar um controle RE-RENDERIZA a onda na hora e o Play toca a versão editada (o grafo editado É a lista de PASSOS mutável; slider re-renderiza sem re-montar o painel pra não perder o arrasto). Validação surfada como o rig do 14b: cada reexec roda `somNucleo` em try/catch, `grafo.orfaos` viram card âmbar + aviso, o grafo bom segue renderizando (órfão/ciclo/sem-saída não quebram). PARAMS nomeados → resolvidos pra número inline ao carregar (round-trip determinístico; PARAMS de verdade = bônus futuro). **Provado:** bancada `somtela` (32 oks) — editar o slider muda a onda (hash de pixels) e é determinístico; ADD filtro nasce solto (grita, onda intacta) e LIGAR abafa (RMS 0.1744→0.0729); LFO mira `saida.ganho` (tremolo muda a onda); remover encolhe; validação grita sem travar; `somCanonico` da UI round-trip estável; o Play monta o PASSOS editado (corpo.freq=620). + o eyeball do editor + o "ouvido" do orquestrador (espectrograma: a bolha varre ~400→1000 Hz em ~0.1s e apaga — bate com freqTopo=1000/tempoSweep=0.1). `npm test` 432. Falta: S4 presets, S5 exportar + amarrar. **Anotado — instrumentação:** o espectrograma + descritores (pitch/brilho/envelope) + A/B contra os sons do `som.js` viram o "ouvido" da verificação de som (hoje harness do orquestrador; proposto como painel na aba + bancada)
- **D-102** · 2026-07-23 · Aba Som, passo S3.5 (ANÁLISE) FEITO — o "OUVIDO": como a IA (e o usuário não-especialista) não ESCUTA, som se prova por MEDIDA + IMAGEM. `motor/somanalise.js` (NOVO, puro/determinístico): `analisar(amostras,sr)` → ESPECTROGRAMA (STFT Hann 1024/hop 256, FFT radix-2 própria) + DESCRITORES (pitch no tempo por bin-de-pico refinado sub-bin, brilho = centroide espectral, envelope RMS→ataque, duração). Na aba `som.html`: o espectrograma (colormap magma, eixo de freq rotulado, trilha de pitch por cima) + a tira de descritores em LINGUAGEM DE GENTE ("TOM 426→1001 Hz · BRILHO ~510 Hz · ATAQUE 17 ms · DURAÇÃO 0.33 s · brilho = quão agudo soa"), atualizando junto com a onda ao editar. `tools/bancadas/analisar.mjs` (NOVO) é o ouvido FIXO da verificação. **`som.js` do jogo + `oficina.html` INTOCADOS** (diff vazio). **Provado:** a bancada `analisar` (16 oks) + o cross-check com o harness do orquestrador (DUAS implementações do ouvido CONCORDAM): a bolha varre 426→1001 Hz (encosta em freqTopo=1000), brilho ~510 Hz, ataque 17 ms; um SENO 440 vira linha reta @ 440.6 Hz (centroide 439) e 880 sobe (eixo de freq certo); um passa-baixa 300 DERRUBA o centroide (510→476) e some o topo do sweep (energia 5× menor) — corta agudo ≠ abaixa volume; determinístico (mesmo evento → mesmo hash de espectrograma + descritores). A aba desenha e bate bit-a-bit com `analisar(renderarOffline)`. `npm test` 441 (+9), `somtela` 36 oks. **Anotado:** o A/B contra os sons reais do `som.js` (renderizar o jogo offline) fica pro S5 (amarrar); por ora comparar dois EVENTOS serve
- **D-103** · 2026-07-23 · Aba Som, passo S4 (PRESETS) FEITO — o CATÁLOGO de sons prontos, cada um semeado dos números já tunados do `som.js`. Nível FÁCIL do vocabulário (D-73): preset é som PRONTO pra variar, FEITO com os blocos do S3 (PARAMS+PASSOS+semente) — ao abrir já é EDITÁVEL (editor S3) e ANALISÁVEL (ouvido S3.5), peça inicial e não engrenagem à parte. NOVOS em `pecas-som/`: `_passo` (grão de impacto passa-banda 1800 Hz + corpo grave passa-baixa, do `PISOS.grama`), `_vento` (ruído filtrado: base larga passa-baixa 1200 + ressonância passa-banda 600 derivando num LFO 0.07 Hz + tremor da turbulência num LFO 2 Hz ±0.35 + envelope da rajada 4.5 s), `_agua` (lambida: ruído grave abafado por passa-baixa 420 Hz); `_bolha` mantido. `som.html`: o seletor lista o CATÁLOGO curado (nome amigável→arquivo: Passo/Vento/Bolha/Água). **Jóia `som.js` + `oficina.html` diff VAZIO.** **Provado (o OUVIDO do S3.5 pagando) — cada preset pela MEDIDA, DUAS implementações do ouvido concordam ~1% no brilho:** passo brilho 2969/2994 Hz (agudo, ruído não-tonal, ataque 0 ms → estalo curto e largo), vento 4.53 s sustentado brilho 1108/1109 Hz com ondulação 7.3×@2 Hz (rajada), bolha varre 445→660 Hz (tonal), água centroide 308/352 Hz (grave/abafado); a discriminação segura (centroide passo>vento>água, passo largo vs bolha tonal, vento longo vs resto curto). `somtela` (9 seções): cada preset carrega EDITÁVEL (7/11/3 nós, 0 órfão, espectrograma desenhado), 0 erro de console, aba Objeto intacta. `npm test` 447. **Escopo/anotado:** os presets CONDENSAM (passo: 1 grão no lugar do granular de 16; vento: 1 seno de turbulência a 2 Hz no lugar da soma 0.25–7 Hz, dropou bp2/assobio) — o A/B FIEL byte-a-byte contra o `som.js` é o S5; e o passo sai ~17 dB mais baixo (o passa-banda come a energia de um grão só), nível seguido do jogo, subível no `graoPico`.
- **D-104** · 2026-07-23 · Aba Som, passo S5a (EXPORTAR) FEITO — o análogo sonoro do passo 10 (D-89): a aba SALVA o evento num `.js` que REABRE bit-a-bit. `motor/somexport.js` (NOVO, puro/headless): `serializarEvento({meta,PARAMS,PASSOS,semente})` → string `.js` com a anatomia IDÊNTICA a `pecas-som/` (cabeçalho gerado, os dois imports do núcleo/adaptador, `PARAMS`/`semente`/`PASSOS`, `meta.duracao` como CHAMADA `duracaoDoGrafo(somNucleo(...))` — recalculada no load, não o valor —, e `construir`); número via **`String(double)`, NUNCA `toFixed`** (arredondar quebra o bit-a-bit — a lição do passo 10, provada: `toFixed(3)` faz `440.123456789`→`440.123` e o round-trip DIVERGE; `String` reabre exato). Botão "Exportar" em `som.html` POSTa pra `POST /som/salvar` (rota-IRMÃ nova no `tools/servir.mjs`: grava em `pecas-som/` reusando o MESMO `salvar()`+`nomeSeguro`, `criarServidor({raiz,pecas,pecasSom})` — o `/oficina/salvar` do passo 10 fica INTOCADO), com fallback pro download (blob+`<a download>`) quando não há servidor. **Jóia `som.js` + `oficina.html` diff VAZIO.** **Provado:** a bancada `somexportar` (Playwright, página==Node, 8 seções) — o grafo editado reabre bit-a-bit (919 chars), os 4 presets do catálogo round-trip idêntico (2309/2592/1973/1977 chars), o botão grava e o arquivo === a string, 6/6 nomes maliciosos rejeitados sem escapar, `no-store`, o fallback baixa, 0 regressão (o `/oficina/salvar` segue 200, aba Objeto intacta) + o cross-check do orquestrador em Node (os 4 presets reabrem bit-a-bit por `somCanonico` comparado por VALOR — o objeto {semente,saida,nos} —, e a discriminação String-vs-toFixed confirmada com `440.123456789`). `npm test` 457 (+10), `oficina` (passo 10) sem regressão. **Falta só o S5b:** amarrar — o `som.js` ganha, ADITIVO (jogo byte-idêntico off), a ponte pra tocar um evento exportado + a renderização offline dos próprios sons — e o A/B contra os sons REAIS do jogo, o fecho do ouvido.
- **D-105** · 2026-07-24 · Aba Som, passo S5b (AMARRAR + A/B) FEITO — FECHA a aba Som (roteiro S1→S5) e, com ela, o roteiro INTEIRO da Oficina (Objeto 0–14 + Som). Opção A do ideador (ADITIVA): o jogo GANHA a capacidade de tocar um evento da Oficina, SEM trocar os sons que já tem. **A ponte — a ÚNICA mudança no `som.js` (a jóia) e PURAMENTE ADITIVA (24 linhas / 0 removidas):** `criarSom()` ganha `tocarEvento(evento,{quando})` — reusa o `ensure`, monta o grafo pelo MESMO núcleo+adaptador das peças-som (`construirGrafo(somNucleo(evento.PASSOS,PARAMS,semente))`) e liga a saída no barramento `eventosG` (entra no mix, sob o volume de AMBIENTE), NUNCA em `AC.destination` (furaria o mix). `build`/`passo`/`makeNoise`/`proximidadeAgua`/vento/água e os métodos que já existiam ficam byte-a-byte → sem chamar `tocarEvento`, o áudio do jogo é idêntico POR CONSTRUÇÃO (nenhuma linha da síntese mudou). **O A/B — bancada `somab`, o `som.js` NÃO muda por isso:** renderiza o passo REAL do jogo offline (stub de `window.AudioContext`→`OfflineAudioContext`, `setVolumes({ambiente:0})` isola a pisada do leito de vento/água), N=20 renders (o passo varia por `Math.random`), caracteriza a faixa pelo ouvido (`somanalise/analisar`) e compara o `_passo`: **dentro em 4/5 eixos** (brilho 2994 vs 2474±489 Hz z+1.1, ataque 11.6 vs 13.4±2.7 ms z-0.7, pico 0.035 z+0.5, achatamento 0.837 z+0.5); a **duração fica FORA** (41 vs 151±15 ms, z-7.4) — a distância MEDIDA da simplificação (o `_passo` condensa os 16 grãos do granular num só grão+corpo). Água/vento/bolha ficam fora do A/B offline (internos/agendados por `setTimeout`, não isoláveis num `startRendering` síncrono) — entregue só o passo, o caso limpo. **Provado (o orquestrador reproduziu):** o diff do `som.js` é SÓ adição (24/0, verificado linha a linha — nenhuma da síntese mudou); a ponte liga no `eventosG` (0.800× = volume de ambiente, não 1.0× do destination; `ambiente=0` silencia → passa pelo bus); não-regressão (`criarSom()` real: ensure/passo/proximidadeAgua/setVolumes/tocarEvento rodam, agendadores disparam, 0 erro de console); `render.js`+`oficina.html` diff VAZIO. `npm test` 457, typecheck/mapa/toc verdes. **A aba Som está COMPLETA; a Oficina (modelar+pintar+exportar+colidir+materiais+animar+esqueleto + criar/exportar SOM) fechou por inteiro. O A/B fiel byte-a-byte (substituir o som do jogo por um evento) fica como decisão por-som do ideador, ouvindo — NÃO um atacado: os presets são simplificações (a duração do passo prova).**
- **D-106** · 2026-07-24 · Agents reduzidos de 6 pra 2 (o time do v3). Os 6 agents (engine-dev/pixel-artist/lore-writer/code-reviewer/art-reviewer/qa-tester) descreviam o modelo 2D/tick (pixel art 16×16, "1 tick = 1 commit", revisão em paralelo) — NÃO o jeito que o v3 de fato se constrói (o orquestrador briefa 1 coder, verifica por MEDIÇÃO, e chama revisor adversarial POR RISCO). E na prática eu não despachava os 6 — briefo um coder genérico. Substituídos por 2, orientados ao v3 e à realidade do fluxo (`nos-fluxo`): **`game-builder`** (constrói o v3 — as jóias `render.js`+`som.js` aditivas/no-op quando off, 3 camadas núcleo→adaptador→interface, prova por medição, branch `wip/` sem push; model sonnet) e **`revisor-adversarial`** (tenta QUEBRAR por risco: fundação / formato salvo / jóia / conta de julgamento; model opus). Mantém o D-24 (coder sonnet, revisor opus). **Decisão de forma (a recomendação que o ideador aprovou):** o DOMÍNIO (som, animação, geometria, pintura) mora nas SKILLS que o builder carrega, NÃO num agent por assunto — porque uma peça é modelada+pintada+animada+com som ao mesmo tempo, e um agent-por-domínio fragmentaria o contexto. Os 6 antigos servem a frente 2D (o mundo / `site` / `engine` / o tick, hoje sem desenvolvimento ativo) — ficam no HISTÓRICO do git, ressuscitáveis se ela voltar (ou migram pro repo de demos, se a V1/V2 saírem). **Pendente (próxima etapa da reorganização):** o `CLAUDE.md` ganhou só uma nota de ROTEAMENTO (duas frentes: v3→skills+2 agents, 2D→regras de tick), mas o enquadramento 2D e o `docs/RECURSOS.md` (que cita os 6) ainda pedem o reroute completo; e os NOMES das versões (sair de V1/V2/V3 — Clareira / Miragem / o carro-chefe a nomear) ficam pra fechar.
- **D-107** · 2026-07-24 · Nomes das versões: sai o V1/V2/V3, entram **Clareira / Miragem / Atelier**. Chamar de V1/V2/V3 dá a entender que a V3 é "melhor", quando são ARTES/JOGOS diferentes (decisão do ideador). Nomeados por NATUREZA, sem número: **Clareira** = o mundo 2D visto de cima (`site/`, publicado em `/nos/`); **Miragem** = o FPS raycaster (`prototipos/fps/nos-fps.html`, `/nos/fps/`); **Atelier** = o FPS GPU + a Oficina (criar objeto e som — `prototipos/procedural/v3/`, `/nos/fps/v3/`), a frente de DESENVOLVIMENTO ATIVO. No README os 3 links foram renomeados (URLs iguais, só os rótulos), e removidas a tagline "O contrário de Jogador Nº 1." e a seção "Como jogar" (descrevia comandos do tick — `/entrar`/`/mover`… — e os Nativos/Habitantes que não existem mais). **Pendente (próxima etapa da reorganização):** o enquadramento 2D no resto do README e do `CLAUDE.md` ("metaverso 2D", "mesmo mundo", a federação) + o `docs/RECURSOS.md` ainda pedem o reroute completo — só o topo (links + nomes) e a nota de roteamento foram feitos por ora.
- **D-108** · 2026-07-24 · Três consertos de acesso/UX no Atelier (o cliente v3), reportados pelo ideador — jóias `render.js`/`som.js` INTOCADAS (os consertos são em `jogo.html`, `motor/input.js` e README). **(1) Acesso à Oficina:** ela estava PUBLICADA (o build copia a `v3/` inteira) mas sem PORTA — o README só linkava o jogo e o `jogo.html` não tinha botão. Agora o menu (ESC) tem um botão "Oficina ✎" que navega pra `oficina.html`, e o README linka a Oficina direto no item do Atelier. **(2) Pointer lock:** o `input.js` pedia `requestPointerLock` no clique SEM tratar a recusa; clicar dentro de ~1,25 s depois de um Esc (cooldown do browser) vazava "Uncaught SecurityError" embaixo da tela. Envolvido em `Promise.resolve(...).catch(()=>{})` — o MESMO padrão que o `jogo.html` já usava na chamada dele —, a trava segue funcionando, só a recusa para de vazar. **(3) Minimapa:** o `CFG_PADRAO.mapaCanto` nascia em 3 (baixo-esquerda); agora nasce em 2 (cima-direita), a pedido — só afeta quem ainda não escolheu um canto (config salva no `localStorage` mantém). **Provado:** smoke Playwright (6 checks) — o jogo carrega com 0 erro de console, o botão existe ("Oficina ✎"), o menu(ESC)→Oficina NAVEGA e a Oficina ABRE (tem canvas), o minimapa nasce `mini c2`, e o clique no canvas não vaza erro de pointer lock (o `.catch` engole a recusa). `npm test` sem regressão.
- **D-109** · 2026-07-24 · Reorganização das pastas de governança — o reroute COMPLETO (fecha o que D-106/107 deixaram pendente) + o plano do `nos-mentes`. **Feito neste repo:** o `CLAUDE.md` virou DUAS FRENTES explícitas — **Atelier (v3)** = ativo (skills `nos-fluxo`+`oficina` + os 2 agents, as 2 jóias, prova por medição, direto pra `main`) e **O Coração (2D)** = manutenção (tick=commit, schema, pixel art, PR) — com as regras separadas por frente; o `docs/RECURSOS.md` troca a tabela dos 6 agents pela dos 2 (`game-builder`/`revisor-adversarial`); o README larga o "metaverso 2D em pixel art" e o "mesmo mundo, mesmo Registro"; a skill `nos-fluxo` passa a nomear as DUAS jóias (`render.js`+`som.js`) e a verificação de SOM (o ouvido: `cmp` de amostra + espectrograma + descritores + A/B, D-102/105); a skill `estruturas` foi marcada **LEGADO** (frente Miragem/v2 raycaster); o `docs/HABITANTES.md` foi marcado **CONCEITO PRESERVADO** (os Nativos/Habitantes não rodam mais — a ideia de mentes-IA-como-repositório fica guardada). **Plano do `nos-mentes` (NÃO executado — é cross-repo E drástico: precisa de go explícito e do repo no escopo):** (1) sintetizar o conteúdo do `nos-mentes` num doc limpo; (2) mover as demos **Clareira** (`site/`+`engine/`+`world/`+o tick) e **Miragem** (`prototipos/fps/nos-fps.html`+`gpu/`+`estudio/`) pra lá, mantendo o README de lá com os botões de jogar; (3) DESLIGAR o tick — tira o mundo 2D do ar, é decisão de PRODUTO; (4) aqui o repo fica só com o **Atelier** e referencia o `nos-mentes` como fonte de ideias. Riscos medidos: a Clareira depende do `engine/`+tick (arrasta junto); o Atelier é AUTOCONTIDO (não importa `engine/`/`site/`, não quebra); o `pages.yml` hoje publica os três juntos, então separar exige mexer no build/deploy.
- **D-110** · 2026-07-24 · O Coração CONGELADO e o `nos` enxugado pra Atelier-only (fase 2a da migração do D-109; a 2b — a remoção física de `site/`/`engine/`/`world/` — vem depois, tem cascata). As demos foram HOSPEDADAS no `brigsd/nos-mentes` (GitHub Pages a partir de `docs/`: Clareira 2D + Miragem raycaster + uma landing, com o retrato do mundo congelado ~#250; PROVADO no ar — 200 nas 3 URLs, páginas renderam). Aqui no `nos`: **(1)** o **tick foi DESLIGADO** — `tick.yml` perdeu `schedule`+`issues`, sobra só `workflow_dispatch`; O Coração não pulsa mais. **(2)** o **deploy virou Atelier-only** — `pages.yml` não constrói mais o `site/`; publica só `prototipos/procedural/v3/` em `/fps/v3/` (URL preservada) + uma landing na raiz que redireciona pro jogo e linka as demos. **(3)** o **README re-aponta** — o Atelier em destaque (a frente viva), Clareira/Miragem viram links pro `nos-mentes`, e o "O Coração pulsa mesmo agora" virou "congelado". O Atelier é AUTOCONTIDO (nada referenciado fora de `prototipos/procedural/v3/` — verificado por grep), então o deploy só-v3 funciona. **Pendente (fase 2b, com CASCATA):** a remoção FÍSICA de `site/` (cliente 2D), `engine/`, `world/`, `worlds/`, `assets/`, `nos-fps.html`, `gpu/`, `estudio/` + scripts/docs 2D — exige ANTES realocar o Playwright (as ~10 bancadas do Atelier importam de `site/node_modules/playwright`) pra raiz e enxugar o `ci.yml` (`lint:sprites`/`validate-world`/build do site). Por ora esses arquivos ficam DORMENTES (não deployados, não tickando).
- **D-111** · 2026-07-24 · Fase 2b CONCLUÍDA — o 2D removido FISICAMENTE; o `nos` agora é só o Atelier (fecha o pendente do D-110). **Removido** (`git rm` — 198 arquivos, −39.829 linhas somando as duas etapas): o cliente 2D `site/`, o `engine/`, `world/`, `worlds/`, `assets/`, `scripts/` (2D), o FPS raycaster antigo INTEIRO (`prototipos/fps/nos-fps.html`+`gpu/`+`bake/`+os órfãos de topo `data.js`/`tree-core.js`/`tree3d-core.js`/`build-data.mjs`/`README.md`/`.gitignore`), `prototipos/estudio/`, `tools/art-mcp/`, as bancadas 2D (`olhar`/`ouvir`/`prancheta`/`res-bench`), o `.mcp.json` e o `tick.yml` (o tick já estava desligado no D-110; agora saiu de vez). **A CASCATA (o que destravou a remoção):** **(1)** o **Playwright foi realocado** de `site/node_modules` pra RAIZ (`playwright@1.56.0` nas devDeps; as ~10 bancadas do Atelier reapontadas) — sem isso, tirar o `site/` quebraria as bancadas; **(2)** o **`package.json`** perdeu os 12 scripts 2D; **(3)** o **`tsconfig`** repontou o `include` de `engine`/`scripts` pra `tools/som`+`tools/oficina` — e como a TS do Atelier NUNCA tinha passado por `tsc` (o gate só olhava o 2D), o `noUncheckedIndexedAccess` foi DESLIGADO (só ele; `strict` segue ligado) pra não afogar em ruído de índice terso sobre o motor `.js` sem tipos — o gate agora COBRE o Atelier de verdade, o que a main não fazia; **(4)** o **`vitest`** tirou o glob morto `engine/**`; **(5)** o **`ci.yml`** perdeu `lint:sprites`/`validate-world`/build-do-site. **Provado (todos os portões, rodados à mão na verificação):** o diff `origin/main -- prototipos/procedural/v3/` VAZIO (o Atelier byte-a-byte igual — as jóias `render.js`/`som.js` INTOCADAS), typecheck strict 0, 89 testes, `mapa:check` 99 arquivos, `docs:toc:check` 37 seções, e as bancadas Playwright `oficina` (BANCADA OK) + `somtela` (que provam a realocação do Playwright, subindo Chromium contra as páginas do Atelier). A migração do D-109 está COMPLETA: o `nos` hospeda só o Atelier; as demos vivem no `brigsd/nos-mentes`.
- **D-112** · 2026-07-24 · Recursos CENTRALIZADOS pra IA (homologação, parte 1 — pedido do ideador: tudo organizado e acessível numa sessão de contexto ZERADO). **(1) Limpeza:** as 9 bancadas que mandavam rodar `cd site && npm ci` (morto) agora dizem `npm ci` na raiz; `run-dev.bat` (chamava o `site/` apagado) removido; o `v3/README.md` parou de citar o build antigo e a skill aposentada. **(2) docs/ reorganizado:** os 10 docs d'O Coração (ARCHITECTURE, GDD, CIDADE, COMUNICACAO, PORTALS_PROTOCOL, HABITANTES, CODER, CONTINUITY, IMPLEMENTATION_PLAN, walkthrough_colab2) moveram pra **`docs/legado/`** com um README-índice — a raiz de `docs/` agora é só o presente (Atelier) + logs projeto-inteiro (DECISIONS/LORE/VISION/FERRAMENTAS/AUDIO_E_CENAS). **(3) `docs/RECURSOS.md` REESCRITO** como o índice único real: os 20 scripts npm atuais (gates + bancadas de objeto/render + bancadas de som + dev), as 4 skills, os 2 agents, os docs — o ponto de partida de sessão nova, junto do CLAUDE.md. `tools/README.md` idem. **(4) Skill NOVA `criar-peca`** — o manual de AUTORIA da IA (a skill `oficina` é pra mexer NA ferramenta; esta é pra USÁ-la): o formato da peça (PARAMS/TOPO/PASSOS/meta), a identidade por bloco com a numeração REAL do cilindro (conferida no núcleo: sem vértice de centro), o vocabulário IMPLEMENTADO (12 ops — deixando explícito que esfera/cone/lathe/loft são roteiro, não realidade), animação/materiais/esqueleto com a assinatura verdadeira do `executar`, o laço de VER (peca/giro/geo), o ouvido do som (analisar/sintetizar/somab, nós do somnucleo), o caminho JS-puro como fallback declarado, e a regra de nunca concluir 'ficou bom' sozinho. O `game-builder` aponta pra ela quando a tarefa é criar conteúdo; o CLAUDE.md também. **(5) Skill `estruturas` APOSENTADA** (era do v2 raycaster, que saiu do repo) e as skills `nos-fluxo`/`oficina` corrigidas: o git sem o tick (desligado), o ouvido apontando pras bancadas do repo (o scratchpad de sessão é efêmero — nada durável pode morar lá). Gates todos verdes; no v3 só o README.md (doc) mudou.
- **D-113** · 2026-07-24 · O ÉPICO DO PLAYGROUND aprovado pelo ideador — roteiro em **`docs/playground.md`** (P0–P9). A tese (do debate): o playground perfeito pra IA não é interface — é (a) VOCABULÁRIO completo (as ops que faltam: primitivas/lathe/loft/inflate/espelha/edição), (b) FORMA COMO NÚMERO (contorno como dado + gabarito IoU multi-ângulo), (c) a CAMADA IA de laço único (uma bancada devolve estado-como-dado + renders + gates + veredito num comando). Decisões fixadas: **(1)** o formato de PASSOS é sagrado — nada de malha assada (Blender descartado como formato; vocabulário se compra emitindo PASSOS); o `construir(ctx)` JS-puro vira *fallback que encolhe*. **(2)** Núcleo primeiro, interface depois (a peça com op nova já reabre; o botão vem na onda P9). **(3)** Contra o falso-positivo/complacência (a lição dos '83%' do nos-Craft, e do D-65 daqui): toda ferramenta de medir nasce com número + VEREDITO calibrado (exit≠0, limiar provado por exemplos bons × defeitos plantados, método do bench/D-60) + evidência forçada; sentidos independentes têm que concordar; 'está bom' segue sendo do ideador. **(4)** Numeração de op nova é FORMATO SALVO — documentada e travada por teste antes do merge, revisor adversarial sempre. Régua de pronto: a moto do Tron estilizada de ponta a ponta sem sair do formato (P1–P4 já a destravam; o emissivo do 12a já existe); o dragão é a régua do capítulo seguinte (esqueleto/skinning/keyframes prontos — falta a carne: loft+inflate). O batimento do Atelier (mundo vivo) segue BLOQUEADO aguardando o go explícito do ideador — este épico é tooling, parte da homologação.
- **D-114** · 2026-07-24 · Playground P1 FEITO — as 3 primitivas que faltavam (`esfera`/`cone`/`plano`) no NÚCLEO da Oficina (`motor/oficina.js`), SEM interface (a peça com a op nova já REABRE; o botão vem no P9). Aditivas puras (89 inserções, 0 remoções; jóias `render.js`/`som.js` diff-VAZIO). Numeração (FORMATO SALVO) travada por teste e documentada no comentário de cada op: esfera V=2+(A−1)·L / F=A·L (polo sul b+0; anel k, vértice j = b+1+(k−1)·L+j; polo norte no topo; faces contíguas por faixa sul→norte), cone V=F=L+1 (anel-base b+0..b+L−1, ápice b+L; tampa −y no winding do cilindro), plano V=(S+1)² / F=S² (grade XZ centrada, linha a linha, quads +y — o chão). Guarda de overflow por-passo em cada uma, cobrindo o eixo que estoura primeiro (face na esfera, pois nF>nV sempre; vértice no plano; L+1 no cone). Peça-exemplo `pecas/_primitivas.js`. **Provado (por mim, reproduzido):** 100 testes (11 novos), typecheck/replay/mapa/toc verdes, `porteiro`/`auditar` limpos, PNGs (normal + geo=normais) com forma certa e winding pra fora sem buraco de polo. **Revisor adversarial (opus) — nada BLOQUEIA, nada DEVERIA:** a COMPOSIÇÃO depois das ops novas aguenta (extruda/pincel/mescla/moveV/solido+colisaoDe em cima de cada primitiva — 0 órfão, normal da feature pra fora); a malha é ESTANQUE e orientada pra fora no pior caso (teste de manifold — esfera 30×30 e cone L=500, toda aresta dirigida pareada 1×, 0 winding invertido — prova mais forte que a amistosa); a fronteira de overflow é exata (esfera lados 500/501, aneis 125/126; cone 999/1000; plano seg 30/31); e a numeração doc==código foi confirmada id-a-id por derivação INDEPENDENTE do comentário (imune à trig — ids são aritmética inteira; só a POSIÇÃO varia e é recalculada de PARAMS, não é formato salvo). **Limitação registrada (NIT-1, PRÉ-EXISTENTE, não do P1):** o serializador do passo 10 (`serializarPeca`) nunca preservou flags de render de peça (ex. `palco:false`) — re-exportar `_primitivas` pela Oficina descartaria o `palco:false` (o plano voltaria a brigar em z com a grama); vale pra `ilha-chao` também. Reabrir pra EDIÇÃO segue ok (lê PASSOS/PARAMS/TOPO). Fica pra decidir se o exportador passa a carregar flags de render. Próximo: P2 (`lathe`).
- **D-115** · 2026-07-24 · Playground P2 FEITO — a op `lathe` (perfil 2D girado em torno de Y) no NÚCLEO da Oficina, SEM interface. Aditiva pura (jóias `render.js`/`som.js` diff-VAZIO). **A esfera do P1 É um `lathe`** de meia-circunferência — a op GENERALIZA o esquema já provado (polo↔anel↔polo), reusando byte-a-byte o winding da esfera (faixa de quad + leques sul/norte). **Decisões de FORMATO SALVO (irreversíveis, fixadas por mim antes de delegar):** **(1)** ponto de perfil = `[raio, y]` (2 elementos), `raio`/`y` podem citar PARAM. **(2)** `raio` resolvido `=== 0` vira POLO (1 vértice no eixo); `>0` vira ANEL de `lados` vértices; `<0` grita e aborta. **(3)** SEM tampas automáticas — superfície de revolução pura; fechar uma ponta é terminar num polo (coluna com tampas = `[[0,0],[R,0],[R,h],[0,h]]`, os leques de polo viram as tampas). **(4)** perfil só ABERTO (loop/pneu fora do P2). **(5)** numeração por cursor (polo=1 id, anel=`lados` ids; faces por segmento), depende só da estrutura + `lados` = TOPO, travada por teste. Guarda de overflow por-passo (vértice E face). Peça-exemplo `pecas/_torno.js` (peão torneado, fechado nas duas pontas). **Revisor adversarial (opus) — 1 BLOQUEIA, CONSERTADO antes da main:** a reserva da alça de curva era FAIL-OPEN — um ponto com 3º elemento GRITAVA mas construía RETO e shipava verde (o revisor provou: porteiro passou na peça-armadilha), então a MESMA peça mudaria de figura quando a curva chegasse (o exato medo do formato salvo). **Conserto:** um ponto ≠ 2 elementos agora GRITA e ABORTA o passo (0 V/0 F, fail-closed, como o `raio<0`) — impossível shipar peça 'reservada'; +1 teste de regressão trava. O mesmo conserto cobriu o NIT-3 (ponto malformado `[1]` agora grita+aborta em vez de throw cru). **NITs deixados (documentados, não bloqueiam):** polo = `raio === 0` EXATO (um raio minúsculo não-zero vira anel-fio silencioso — autoria usa 0 literal); `colisaoDe` num peão é cilindro delimitador conservador (gordo no pescoço). **O que aguentou (por medição):** composição (extruda/pincel/mescla/moveV/solido+colisaoDe em cima do lathe), polos no meio (pinça), manifold no pior caso (lados=64, watertight), numeração doc==código id-a-id por derivação independente, determinismo/round-trip/overflow exato. À parte: corrigido o `CLAUDE.md` que dizia 'commits em inglês' (o repo é PT-BR em tudo — pego pelo coder). Próximo: P3 (`espelha` + `rotaciona`).
- **D-116** · 2026-07-24 · Playground P3 FEITO — as ops `espelha` + `rotaciona` no NÚCLEO da Oficina, SEM interface. Aditivas puras (jóias `render.js`/`som.js` diff-VAZIO). Estreiam o padrão "transformar uma seleção" (não existia — e de passagem consertei um erro meu: a tabela listava um op `escala` de malha que NÃO existe, é só canal de animação; removido). **`rotaciona`** (`eixo`/`graus`/`sel?`/`pivo?`): gira posições in-place em torno do pivô (default = centroide da seleção), NUNCA cria id — sem risco de formato salvo. **`espelha`** (`eixo`/`pos?`/`sel?`): DUPLICA faces refletidas (`coord'=2·pos−coord`), ids novos do bloco do passo = FORMATO SALVO. Decisões fixadas por mim antes de delegar: **(1) WELD** — vértice com a coord do eixo EXATAMENTE `pos` é COMPARTILHADO (solda a costura de quem modela a metade com a borda no plano; igualdade exata, como o polo `raio===0` do lathe); fora do plano ganha cópia. **(2)** cantos da face nova REVERTIDOS (desfaz a troca de mão do espelho → normal pra fora). **(3)** atributos (cor/liso/material/parte/solido) herdados. Numeração determinística (mapa orig→espelho por id ordenado), guarda de overflow independente pra vértice-novo/face-nova. Peça-exemplo `pecas/_espelhado.js` (cabeça + par de chifres, um modelado e o outro por espelha, watertight). **Revisor adversarial (opus) — 1 BLOQUEIA, CONSERTADO antes da main:** uma face INTEIRAMENTE no plano (todos os cantos soldados) virava uma face COINCIDENTE de normal oposta, SILENCIOSA — e o pior, o teste de manifold é CEGO a ela (as arestas da face e da coincidente-invertida se pareiam), então o "watertight provado" não a pegava; além disso o teste amistoso travava o degenerado como correto. **Conserto:** a face toda-no-plano agora GRITA e é PULADA (0 face, cursor não avança), idêntico ao `polo↔polo` do lathe; +1 teste de regressão, e o teste de overflow de face reescrito (o caso "zero vértice novo" virou impossível — é a degenerada pulada). **1 DEVERIA consertado:** o comentário do `_espelhado` documentava a numeração ERRADA (passo 14 / ids 10000); o real é passo 15 / ids 15000 (MEDIDO com um harness, não recontado no olho — o recontar no olho foi o próprio erro), comentário corrigido + os ids travados por teste. **2 NITs consertados:** aviso quando passam `sel.v` ao espelha (que só usa `sel.f` — não vira no-op silencioso), e nota da reflexão que cai sobre vértice existente. **O que aguentou (por medição):** weld (−0/+0, colisão de addV, PARAM), numeração id-a-id por derivação independente, winding (volume assinado >0 em todas), composição (espelha∘espelha, extruda/pincel/mescla/solido em cima de face espelhada, rotaciona quebrando a solda como esperado), manifold do _espelhado, determinismo/round-trip/overflow exato, e o `rotaciona` inteiro. **Nota de processo:** o coder do P3 terminou com o sign-off embaralhado (confusão de papel) e deixou um auto-trigger agendado nesta sessão — o trigger disparou uma vez (run_once) e foi tratado; o trabalho em si ficou íntegro. Próximo: P4 (`loft`).
- **D-117** · 2026-07-25 · Playground P4 FEITO — a op `loft` (seções circulares encadeadas ao longo de um CAMINHO 3D arbitrário) no NÚCLEO da Oficina, SEM interface. Aditiva pura (jóias `render.js`/`som.js` diff-VAZIO). **O `lathe` é o TEMPLATE** (mesmo cursor/polo/anel/leque/guarda de overflow); a peça nova é o **FRAME por TRANSPORTE PARALELO** — sem um eixo fixo (o Y do lathe), cada anel é orientado a partir do anterior projetando a base fora da tangente nova e renormalizando. Reimplementado local ao núcleo (`quadroLoft`/`transportaLoft`), byte-equivalente ao `quadro`/`transporta` do `galhoSeca` em `motor/arvore-cartoon.js` — a convenção JÁ provada no jogo. É isso que impede o tubo de TORCER numa curva (medido: pior produto-escalar entre os 2 triângulos de um mesmo quad = 0,527 numa hélice de 12 seções, 1,000 num caminho reto — nenhuma borboleta). Args `secoes:[{pos,raio}]` (≥2) + `lados` (TOPO); a chave `secao` (contorno 2D) é RESERVADA e fail-closed (nasce no P5). Peça-exemplo `pecas/_galho.js` (galho curvo em 2 eixos, afinando, fechado nas duas pontas por polo — watertight + volume assinado > 0). **Revisor adversarial: o agente MORREU na cota da sessão** com a revisão incompleta, então continuei os eixos restantes eu mesmo — e achei **2 BLOQUEIA, os dois CONSERTADOS antes da main**. **BLOQUEIA 1 — CUSP:** num caminho que dobra ~180°, a tangente interior (média dos dois segmentos vizinhos) zera; `norm3` devolve `[0,0,0]` pelo guarda `|| 1`, então `w = cross(u,0) = 0` e o anel do meio **COLAPSA numa linha**. Degenerado SILENCIOSO, não-NaN (medido: distâncias ao centro do anel 0.3/0.15/0.15/0.3/0.15/0.15 em vez de um círculo) e **invisível pro teste de manifold** — a mesma classe do achado do P3. Conserto: grita + ABORTA o passo (0 V/0 F), como o comprimento-zero. O `galhoSeca` do jogo nunca cai nisso (o `desviar` dele só faz desvios pequenos, nunca antiparalelos); o loft aceita caminho arbitrário e precisa da guarda explícita. **BLOQUEIA 2 — número não-finito e aridade de ponto, no NÚCLEO INTEIRO:** `st.num` aceitava `NaN`/`Infinity` e `st.vec` aceitava array de qualquer tamanho, então 7 de 9 entradas malformadas do loft construíam **12 vértices com coordenada NaN e ZERO órfãos**; e o furo era de TODA op (cubo/cilindro/esfera/cone/plano/lathe/moveV — todos medidos). O caso pior é num **TOPO**, onde **nenhum gate pega**: `lados: NaN` -> `NaN|0` = 0 -> `Math.max(3,0)` = 3, então um cilindro de V=16/F=10 vira V=6/F=5 com malha LIMPA — a peça salva muda de CONTAGEM e todo id de face dos passos seguintes passa a apontar pra outra face, calado. (Medido também o que o gate PEGAVA: no caso dimensional o `lint-de-malha` do `auditar` reprovava a peça-armadilha com exit 1 — "180 coordenadas NaN/Inf" — mas sem dizer QUAL passo; o `porteiro` passava verde.) Conserto em 3 pontos: `st.num` recusa não-finito (throw, a lei que já aplicava pra nome de PARAM inexistente), `st.vec` exige `[x,y,z]` (throw — antes `pos:[0,1]` deixava o z virar `undefined`, e não-array estourava `a.map is not a function` cru), e o `loft` valida a aridade LOCALMENTE pra GRITAR nomeando a seção (a lei do lathe: rede central garante que nada vaza, checagem local dá o diagnóstico). **Prova de não-regressão do conserto central** (mexe em toda op): 161 testes verdes (+17), typecheck 0, `executar` replay provado, veredictos do `auditar` **byte-idênticos** peça-por-peça antes/depois (as 3 falhas — arco/arvore-cartoon/vegetacao-cartoon — são achados de arte pré-existentes, provado por stash), `porteiro` 7/7, `mapa` ok, e o jogo real rodando (`jogar`: ready=true, 35 fps, 0 erros, screenshot conferido). **O que aguentou (por medição, eixos que eu mesmo ataquei):** numeração doc==código id-a-id por derivação INDEPENDENTE do comentário em 4 cenários (inclusive passo ≠ 0 e polo-polo no meio); manifold 0 falhas + volume assinado > 0 + raio de anel EXATO em 7 caminhos extremos (dobra 90°, dobra 179°, hélice, raio >> segmento, pinça de polo interior, lados=3); composição em 10 casos (extruda em quad E em triângulo de leque, rotaciona total e por seleção, espelha com e sem weld, mescla, pincel/liso/solido/parte, moveV, loft depois de outra op) — manifold 0 em todos; fronteira de overflow exata (V=1000 passa, 1010 estoura); determinismo bit-a-bit. **Lição de método:** foi a 3ª op seguida em que a revisão adversarial achou um BLOQUEIA real de formato salvo (P2 fail-open, P3 cego ao manifold, P4 cusp + não-finito) — a regra 4 do `docs/playground.md` (revisor adversarial em toda op) está se pagando, e quando o agente morre a revisão NÃO pode ser dispensada. Próximo: P5 (contorno como dado + gabarito IoU).
- **D-118** · 2026-07-25 · Playground P5 FEITO — CONTORNO COMO DADO + gabarito IoU: FORMA COMO NÚMERO. Duas frentes, uma sozinha (sem subagentes — a peça no `/model` que trocou pra Sonnet 5 nesta sessão não muda o método). Aditivo puro (jóias `render.js`/`som.js` diff-VAZIO). **(A) `contorno` no `loft` (núcleo, `motor/oficina.js`):** a chave `secao` reservada desde o P4 virou de verdade `contorno: [[u,w],...]` — EXATAMENTE `lados` pontos no plano LOCAL do anel (os eixos do transporte paralelo), substituindo `raio`+ângulo pelas coordenadas exatas — destrava seção NÃO-circular (estrela, hexágono, retângulo) sem tocar em UMA LINHA da numeração/faces/overflow, que só enxergam "polo ou anel de `lados` vértices". Decisões de formato salvo fixadas antes de codar: `raio`/`contorno` mutuamente exclusivos (os dois juntos, ou nenhum, GRITA e ABORTA); ponto `[u,w]` com a MESMA lei de aridade do perfil do lathe (3º elemento = alça de curva RESERVADA, GRITA); contagem ≠ `lados` GRITA; **winding CCW obrigatório, validado por ÁREA COM SINAL (shoelace)** — CW ou degenerado GRITA e ABORTA, porque produziria normal invertida ou nula SILENCIOSA, a mesma classe do achado do P3 (cega ao teste de manifold). Peça-exemplo `pecas/_viga.js` (viga curva de seção retangular, fechada nas duas pontas — watertight, sem `liso` de propósito: seção angular não deve suavizar). **Revisão adversarial (eu mesmo, todos os eixos): geometria conferida por medição** (harness independente, não recontada no olho — a lição do D-116): numeração id-a-id bate a doc em cenário reto E curvo; manifold 0 falhas + volume>0 em hexágono/estrela CÔNCAVA/retângulo/contorno→contorno com formas DIFERENTES/contorno misturado com raio/caminho curvo; composição limpa (extruda em quad e em leque triangular, rotaciona total/seleção, espelha COM weld — o padrão real, metade aberta + espelho —, mescla, pincel/liso/solido/parte, loft depois de outra op); fronteira de overflow exata (V=1000 passa contorno de 250 lados, 1010 estoura); todos os 8 caminhos de erro (ambíguo, nem-um, contagem errada, aridade do ponto ×2, CW, degenerado ×2) abortam 0V/0F. **1 achado NÃO-bloqueante registrado:** espelhar uma peça JÁ FECHADA inteira (fora do uso documentado — modelar a METADE) produz aresta duplicada não-pareada; reproduzido IDÊNTICO com um `cubo` puro (sem `loft`/`contorno` nenhum) — é comportamento PRÉ-EXISTENTE do `espelha` (P3), não regressão desta rodada; fica pra registro, não bloqueia. **(B) A bancada `gabarito.mjs` (`tools/bancadas/`, tooling — fora do núcleo):** `npm run gabarito -- <peça>` mede a silhueta RENDERIZADA × um `CONTORNOS` de referência (`prototipos/procedural/v3/gabaritos/<peça>.js`, o MESMO formato de ponto do `contorno` do loft — 2 consumidores, 1 formato) e devolve IoU + VEREDITO calibrado, com evidência em PNG (nunca número cru). **A decisão difícil: como separar objeto de fundo?** Nem cor-chave (céu tem gradiente+estrelas, chão tem grama com dither — nenhuma cor fixa separa os dois) nem os modos de debug existentes (medido: `--geo=normais` NÃO achata o fundo). Solução: **diferença contra o fundo VAZIO** — a peça-fixture `_vazio.js` (0 passos, só o palco padrão) renderizada no MESMO ângulo/câmera — e um filtro de COMPONENTE CONEXO pequeno contra o resíduo (partículas de pólen à deriva, grama balançando no vento: medido A/B do `_vazio` consigo mesmo, 0,31% dos pixels diferem, maior componente de ruído 19px @ limiar=40 — `LIMIAR_DIFF`/`MIN_COMPONENTE` têm margem de ~3× sobre esse piso MEDIDO, não chutado). **2º achado no caminho: o HUD (`#hud`, DOM fixo top-left em `visor.html`) vaza na diferença** — o nome/descrição da peça difere do `_vazio` como qualquer pixel, e um recorte só por tamanho de componente NÃO bastava (texto forma blocos >60px) — visto direto no PNG da máscara extraída (letras fantasma). Consertado com `IGNORAR_TOPO=90px` (corte fixo, medido contra a descrição MAIS LONGA do repo — `_viga`, 145 caracteres, ~60px de HUD — com margem). **`LIMIAR_IOU=0.55` calibrado pelo método do bench/D-60** (exemplos bons × defeitos plantados): contra a silhueta REAL da `_viga`, 3 contornos "bons" (traçados à mão olhando o PNG — o jeito real que uma IA autora, sem canvas) deram 0,65/0,74/0,88; 5 "ruins" (forma trocada, deslocado, escala errada, girado 90°) deram 0,00/0,15/0,20/0,25/0,44 — vale de 0,21 entre o pior bom e o melhor ruim, limiar no meio puxado pro lado permissivo. Sem gabarito pra uma peça, a bancada FALHA ALTO (nada foi medido não é "passou") — a mesma lei fail-closed do núcleo, só que em tooling. **Prova:** núcleo puro (`gabarito-nucleo.mjs`: máscara por diferença, componentes conexos, rasterização por scanline par-ímpar, IoU) com 20 testes vitest determinísticos (sem browser); end-to-end provado nos 3 caminhos (aprova IoU=0,879, reprova IoU=0,000 com referência trocada, falha-alto sem gabarito) com a EVIDÊNCIA (sobreposição branco/verde/magenta) conferida a olho. **Não-regressão:** 190 testes (+29 desde o P4), typecheck 0, `executar`/`auditar`/`porteiro`/`mapa`/`docs:toc` sem mudança de veredito em nenhuma peça, `jogar` ready=true 35fps 0 erros. `pngstats.mjs` ganhou `decodePng` (antes só `pngStats`, que descartava os pixels — reuso, não duplicação) e `pngwrite.mjs` é novo (codificador PNG mínimo, a evidência visual da bancada; round-trip byte-a-byte provado contra o decoder). **Limitação registrada:** um `CONTORNOS` é um polígono SIMPLES — sem buraco (uma roda com vão mede a silhueta cheia); refinamento fica pra quando aparecer o caso real. Próximo: P6 (`inflate`, contorno de lado+topo → volume — o formato já existe, falta a op).
- **D-119** · 2026-07-25 · Playground P6 FEITO — a op `inflate` (dois contornos 2D → volume, por interseção de prismas) no NÚCLEO da Oficina, SEM interface. Aditiva pura (jóias `render.js`/`som.js` diff-VAZIO). Sozinho, sem subagentes (segue a lição do P4/P5: o revisor sou eu). **A decisão de método, fixada antes de codar:** `inflate` NÃO tem fórmula fechada como lathe/loft (o volume é a interseção de duas silhuetas arbitrárias — geometria sem forma analítica geral) — a alternativa óbvia (CSG geral, interseção exata de malhas booleanas) é notoriamente frágil (casos degenerados, coplanaridade, T-junctions — a classe de bug que este projeto inteiro tem caçado). Escolhido em vez disso: **GRADE DE VOXEL** — um voxel está DENTRO se a projeção (z,y) do centro cai no `contornoLado` (plano z×y) E a projeção (z,x) cai no `contornoTopo` (plano z×x, a convenção da Aba Desenho); uma face só é emitida entre um voxel DENTRO e um vizinho FORA — **watertight por CONSTRUÇÃO topológica, não por sorte** (parede interna nunca aparece, então toda face emitida tem exatamente 1 voxel-dentro de cada lado). Custo assumido e documentado: o resultado é BLOCKY (facetado pelos voxels), não suave — a mesma classe de limitação honesta do "lathe só reto por enquanto" (D-115); suavizar (marching cubes ou parecido) fica pra quando o caso real pedir. **Formato:** `contornoLado`/`contornoTopo` seguem a MESMA lei de ponto do `contorno` do loft (D-118) — aridade ≠ 2 é a alça de curva RESERVADA, GRITA e ABORTA; <3 pontos GRITA; Z tem que CASAR entre os dois contornos (mesmo eixo físico) — a caixa combinada usa a união dos dois intervalos; se os contornos não se cruzam em NENHUM voxel (incluindo contorno degenerado num único ponto), GRITA e ABORTA o passo inteiro — volume vazio nunca é o que o autor queria. `divisoes` (TOPO, mín 2, silenciosamente clampado como `lados`/`aneis`) subdivide o eixo mais LONGO da caixa combinada; os outros dois eixos ganham a MESMA aresta de voxel (cúbico, não distorcido). **Numeração SEM fórmula fechada** (diferente de tudo shipado até aqui): emerge de um SCAN determinístico — voxels em ordem `ix,iy,iz`, por voxel as 6 direções de face em ordem fixa `[-x,+x,-y,+y,-z,+z]`, por face os 4 cantos da grade em ordem CCW vista de fora (tabela fixa); cada canto ganha id na PRIMEIRA vez que uma face o referencia (Map local, o mesmo espírito não-fechado do `espelha`, que numera por mapa em vez de fórmula). Guarda de overflow (D3) adaptada: como não há fórmula pra prever o total, a malha é montada numa estrutura LOCAL — nunca toca `st.V`/`st.F` — até o scan terminar; só então os totais REAIS (contados, não estimados) são comparados ao bloco de 1000 ids, e só aí tudo é commitado de uma vez. Guarda de SANIDADE separada (throw, antes até do scan rodar): grade com mais de 200.000 voxels — protege contra `divisoes` absurdo travar a sessão, independente do bloco de ids. **Prova por medição (eu mesmo, todos os eixos, antes de escrever um teste permanente):** protótipo isolado primeiro (fora do núcleo) — caixa dá volume EXATO (2.8000 == 2·1·1.4 analítico), bicilindro (dois cilindros perpendiculares) converge pro sólido de Steinmetz exato (16/3≈5.333) dentro de ruído de discretização esperado em 5 resoluções, manifold sempre 0 falhas; numeração de um caso mínimo (1 voxel isolado, 8 cantos, 6 faces) DERIVADA À MÃO e batida contra o código antes de confiar (a lição do D-116: nunca recontar no olho) — bateu exato. Portado pro núcleo real, os MESMOS números bateram de novo, MAIS um teste de winding por Newell INDIVIDUAL (não só o volume agregado): todas as 520 faces de um blob apontando pra fora. Formas côncavas (estrela) e assimétricas (retângulo alongado × círculo) — manifold 0, volume>0. Fronteira de overflow exata (divisoes=13 passa, 14 seguinte já não — a curva de F cresce rápido com divisoes³). Guarda de sanidade de 200.000 voxels dispara corretamente pra divisoes=100. 8 caminhos de erro (contorno ausente/curto/não-array/aridade errada ×2, sem overlap, degenerado num ponto) abortam 0V/0F. Composição limpa (extruda/rotaciona/espelha/mescla/pincel/liso/solido/parte em cima do inflate, e inflate depois de outro passo — ids todos no bloco certo). PARAM/TOPO citados por nome funcionam (`divisoes` e coordenadas de ponto). Determinismo bit-a-bit. **141 testes (+11), typecheck 0, executar/auditar/porteiro sem mudança de veredito em nenhuma peça, jogar ready=true.** Peça-exemplo `pecas/_corpo.js` (corpo oval achatado — largura≠altura, o que nem lathe nem loft-com-`raio` conseguem, os dois só fazem seção circular — watertight, contornos GERADOS por uma função "ovo" documentada em vez de pontos hand-tuned, variante honesta do estilo `_torno.js`/`_galho.js`). Com P6 fechado, o VOCABULÁRIO do épico está completo (P1-P6): faltam só a camada IA de laço único (P7), a edição restante (P8) e a onda de interface (P9) — nenhuma delas bloqueia a IA modelar hoje. Próximo: P7 (a bancada `criar`, laço único — estado como dado + renders + gates + veredito agregado num comando).
- **D-120** · 2026-07-25 · Playground P7 FEITO — a bancada `criar.mjs` (`npm run criar -- <peça>`), o LAÇO ÚNICO: fecha o épico do vocabulário+medição (P1-P7). Sozinho, sem subagentes. **O que ela faz, tudo num comando:** (1) estado do núcleo — vértices/faces/caixa/colisão, direto de `nucleo`/`neutroCanonico`/`colisaoDe`, SEM browser, pra peças-Oficina (as JS-puras reportam isso claro em vez de fingir); (2) o MANIFESTO DE CAPACIDADES — `OPS` (agora **exportado** do núcleo, era `const` interna) cruzado contra a tabela da skill `criar-peca`: aponta op no núcleo sem doc E doc citando op que não existe mais, nas DUAS direções — provado plantando os dois tipos de deriva de propósito e medindo que a bancada realmente acusa (não só "bate hoje por acidente"); (3) `auditar` (os críticos [cpu], reusando `construirPeca`+`bench/tools/*`); (4) `porteiro` (ready/frame degenerado/erros de página) — reaproveitando a MESMA navegação que já tinha rodado pro 1º render, não abre página à toa; (5) os RENDERS — 3 ângulos texturizados + 3 `geo=normais`, a evidência forçada (salvos, não só citados); (6) `gabarito` (IoU) SE existir `gabaritos/<peça>.js` — reusa as funções puras de `gabarito-nucleo.mjs` (nenhuma lógica de silhueta duplicada, só a navegação Playwright é local, seguindo o mesmo padrão de cada bancada ser autocontida que `porteiro.mjs`/`olhar-peca.mjs`/`gabarito.mjs` já tinham). **Um browser só** pra todo o passo 3-6 (não relança Chromium por gate). **Veredito agregado:** REPROVADO (exit 1) se núcleo/auditar/porteiro falharem, OU se existir gabarito e o IoU não bater o limiar; sem gabarito, o veredito nota explicitamente que a forma não foi verificada numericamente (nunca deixa "aprovado" parecer mais forte do que é). **Achado ao escrever (não bloqueante, consertado no próprio processo):** `neutroCanonico` devolve linha de vértice `[id,x,y,z]` ACHATADA, não `[id,[x,y,z]]` — a 1ª versão da extração de caixa delimitadora indexava errado (`v[1][0]` num número, não um array) e teria dado `NaN` silencioso na caixa; pego ANTES de rodar (lendo a implementação real de `neutroCanonico`, não assumindo a forma) — a mesma disciplina que pegou o bug do P4/P6 antes de eles chegarem a rodar errado. **Resiliência:** a seção de estado do núcleo e a seção inteira do browser (porteiro+renders+gabarito) ganharam try/catch — antes um throw no meio (peça malformada, navegação falhando) deixaria o browser/servidor pendurados e a saída virar stack trace crua em vez de um REPROVADO com motivo; corrigido e reverificado. **Prova (execução real, não só teste sintético — CLI de ponta a ponta):** `_viga` (com gabarito) dá APROVADO com IoU=0,879, idêntico ao `gabarito.mjs` isolado; `_galho` (sem gabarito) dá APROVADO com a nota explícita de forma não verificada; `vegetacao-cartoon` (achado pré-existente de banding) dá REPROVADO; um gabarito trocado por referência errada dá REPROVADO especificamente no eixo do IoU (IoU=0,000); a deriva do manifesto pega nos dois sentidos (op sem doc E doc sem op) quando plantada, e volta a ficar limpa quando desfeita. `npm test` 201 (sem teste novo — `criar.mjs` é orquestração de peças já provadas isoladamente, como `porteiro.mjs`/`gabarito.mjs`; a prova é rodar de ponta a ponta, o mesmo padrão dos outros CLIs de bancada), typecheck 0, `executar`/`auditar`/`porteiro` sem mudança de veredito em nenhuma peça, `jogar` ready=true. Jóias `render.js`/`som.js` diff-vazio. **Com P7 fechado, a tese do playground (D-113) está completa:** vocabulário (P1-P6) + forma como número (P5) + laço único de IA (P7) — o "83%" fica impossível por construção. Faltam P8 (edição restante) e P9 (interface), nenhuma bloqueia a IA criar hoje. Próximo: a régua de pronto pede a moto do Tron de ponta a ponta — usar o `criar` pra modelar e verificar, não mais roteiro de tooling.
- **D-129** · 2026-07-25 · Seleção semântica uniforme para atributos e espelho — o segundo gargalo medido do TETO deixa de falhar calado. A 2ª corrida registrou 6.374 ids de face literais (64% de `moto.js`) e provou que `pincel`/`liso`/`material`/`solido`/`parte`/`espelha` ignoravam `sel:{grupo|regiao}` sem erro, embora `rotaciona`/`transladar` já tivessem essa linguagem. A correção não cria outra semântica: `resolverSelecao` é a fonte única de `{v,f,grupo,regiao}`. Para VÉRTICE, preserva o contrato antigo (região = vértices dentro da caixa); para FACE, `v` significa faces incidentes e `regiao` somente faces cujos todos os cantos cabem na caixa inclusiva. Campos se unem; `faces:[ids]` continua byte-compatível, mas `faces`+`sel` é ambíguo e grita. Chave desconhecida, grupo/id inexistente, região incompleta/invertida/não-finita e seleção sem alvo agora carregam operação+tipo+causa em `orfaos`, sem alteração parcial. O modo livre do pincel segue por `pontos`, portanto rejeita seleção em vez de fingir que a leu. **Passe adversarial solo:** depois dos gates, uma região que continha só um vértice ainda passava pelo resolvedor com zero faces — seria silêncio para atributo; `resolverAlvosF` ganhou a guarda específica e o teste trava o caso. Provas: 246 testes, incluindo os seis consumidores, grupo/região/vértice, espelho por grupo/região, legado byte-idêntico, órfãos e regressão de rotação/translação. Medição na moto: duas regiões exatas substituem 20 ids de farol + 12 de lanterna (**32 IDs**, **2 linhas físicas**, **0 passos**), com canônico byte-idêntico e 0 órfãos; relatório em `docs/teto-selecao-semantica-relatorio.md`. Limite honesto: seleção semântica não representa conjunto descontínuo/visual arbitrário — nesses casos o literal legado continua correto. Revisão adversarial solo concluída após os gates.

- **D-128** · 2026-07-25 · O TETO MEDIDO, e o que ele escolheu — `transladar` + 2 consertos de contrato + o crítico de `simetria`. **O experimento** (`docs/TETO.md`, consenso de uma revisão a três: ideador + dois modelos): em vez de somar op ou tela, medir onde o vocabulário para, com uma criação de verdade. O dado que motivou tudo: as 13 peças em `PASSOS` eram TODAS fixtures `_`, e as 14 em `construir(ctx)` JS-puro incluíam **todo** o conteúdo real do jogo — a regra 1 do épico chama o `construir` de "fallback que encolhe", e depois de P1→P9 inteiro ele **não havia encolhido um caso**. **A execução:** um agente LIMPO (sem memória da implementação — só repo, skills e docs), enunciado do ideador, 3 ciclos, exclusivamente PASSOS, sem resgate. Regra registrada e cumprida: se ele precisasse de algo que só existe na cabeça de quem implementou, isso CONTA como achado (furo no contrato formal). **Resultado — a peça saiu:** `pecas/moto.js`, 51 passos, 492V/558F, 0 órfãos, manifold 2052/2052 arestas pareadas, 12 cascas com volume>0, determinismo bit-a-bit, round-trip do formato salvo exato, reabre na Oficina, 5 críticos limpos, `porteiro` são, zero geometria em JS. **O achado que decidiu o próximo trabalho: 7 das 25 ops.** `loft`×9 + `espelha`×3 + atributo; as 18 nunca usadas incluem TODAS as outras 8 primitivas — e não por estilo. Verificado no núcleo: **nenhuma primitiva aceita argumento de posição**, só dimensão. O enquadramento mais afiado (meu, medido depois do relatório dele): `moveV`=1 vértice, `moveF`=1 face, `moveA`=1 aresta, `rotaciona`=**malha inteira** — dava pra GIRAR tudo e não dava pra TRANSLADAR nada maior que uma face. Assimetria no vocabulário, não conveniência faltando. **`transladar(sel, d)`** fecha isso: `resolverAlvosV` (existia, provado) + a soma do `moveV` (existia, provado), com o `rotaciona` como molde exato — mesma semântica de `sel`, `d` por PARAM, aditivo, sem pivô, nunca cria id, não consome o bloco. **Deliberadamente SEM `escala`:** o agente nunca reportou precisar escalar uma SELEÇÃO (o bloqueio dele era escalar CONTORNO dentro do `loft` — coisa diferente); bundlar seria somar op não-provada, o vício que o TETO existe pra corrigir. **2 furos de CONTRATO, os dois provados por ele bater neles:** (1) `loft` com caminho simétrico **não** gera malha simétrica — o frame vem de transporte paralelo PROPAGADO, então depende do HISTÓRICO do caminho; só preserva simetria se a tangente for constante. O mecanismo estava documentado (inclusive a troca de `ref` em `|t.y|>0.9`), a CONSEQUÊNCIA não — e é a consequência que um autor precisa. Agora está no cabeçalho da op, com o número medido e o conserto ("modele meia peça e use `espelha`"). (2) **erro meu no D-125:** escrevi a exceção de 1 ULP como sendo do `Math.sin` do `displace`; ela é de `cos`/`sin` em **qualquer** op — todo gerador circular (`cilindro`/`esfera`/`cone`/`lathe`/`loft`) e o `rotaciona`. A moto divergiu 1 ULP num anel de `loft` **sem um único `displace`**; quem seguisse a doc ao pé da letra concluiria "determinismo REPROVADO" numa peça correta. Corrigido com nota explícita de correção. **O crítico `simetria` (métrica nova, categoria "criou mas não percebeu o defeito"):** a moto saiu com 12 de 492 vértices sem par espelhado (desvio máx 4.45e-3) e **NENHUM gate pegava** — nem `auditar`, nem `porteiro`, nem `criar`; sub-visual (4,5 mm em 2,8 m), o olho também não pega. O agente só achou porque escreveu a própria régua. Agora é crítico permanente, **OPT-IN por `meta.simetria`** ('x'/'y'/'z') — peça que não declara é ignorada (árvore não é simétrica e não deve virar ruído): fail-open no que não foi prometido, cobrado no que foi. `meta` passou a ir no ctx dos críticos (aditivo — os 5 existentes ignoram a chave), nos 3 chamadores (`auditar`/`criar`/`benchmark`). **A moto DECLARA `simetria:'x'` e por isso FALHA o `auditar` de propósito** — o enunciado pediu simetria; não declarar esconderia o defeito atrás de um verde, exatamente o "escolher o ângulo favorável" que eu proibi ao agente. **Consequência assumida: a baseline do `auditar` vai de 3 para 4 achados conhecidos** (os 3 de arte pré-existentes + a moto), e consertar a simetria é o primeiro alvo OBJETIVO do desafio de refino — que é a régua que a 1ª corrida não tinha (a única realimentação de forma dela foi olhar o PNG). Prova de que o crítico mede certo: reproduz **exatamente** o número do agente (12 vértices, 4.45e-3) por outro caminho — ele pelo neutro, o crítico pelo mesh pós-`adaptarV3`. Duas implementações, uma medição. **3 bugs no meu próprio trabalho, todos pegos rodando:** os 3 primeiros testes do `transladar` falharam por erro MEU e não da op — comparei o canônico COMPLETO (que inclui o array de órfãos) quando queria só geometria; passei `id: 1` num passo de posição 2 (o `id` é a BASE do bloco, posição×1000); e chamei `manifoldRuim` de um `describe` novo, onde ele não existe (a convenção do arquivo é redefinir por bloco) — a MESMA classe de escopo do D-126. Também errei 2× na verificação do relatório dele: um grep meu casou `a.sel` com `resolverAlvosV` e eu quase "corrigi" o agente dizendo que `espelha` aceita seleção rica (não aceita — lê `a.sel.f` na mão; só `displace` e `rotaciona` usam o helper, **2 das 25**), e antes disso um grep contou 24 das 25 ops na skill `criar-peca` porque `cilindro` mora numa linha combinada. Nos dois casos a checagem salvou, e nos dois o achado do agente era mais forte do que eu quase publiquei. **Prova:** vitest 240 (+10 do `transladar`), typecheck 0, `oficina` 402/402, `executar` replay provado, `porteiro` 7/7, `jogar` ready=true, `auditar` com os 3 pré-existentes + a moto (esperado, acima). Jóias `render.js`/`som.js` diff-VAZIO. **O que NÃO foi pré-agendado:** `toro` (gap real — confirmei que o `lathe` liga só pontos consecutivos, então perfil circular não fecha sem costura aberta ou vértice duplicado), `chanfrar seleção` (real — `chamferBox` é primitiva, não operação), `escala`, e a Aba Desenho. `tuboEntre` foi ANALISADO E DESCARTADO: é o `loft`, que o agente usou 9× com sucesso — verbosidade, não capacidade ausente. Todos entram só se o desafio de REFINO os elegê-los. Próximo: um agente limpo refinando a MESMA moto, com as críticas do ideador como enunciado — refinar é capacidade diferente de criar, e é a 4ª categoria do TETO ("ficou enorme e difícil de editar"), onde a moto foi pior (2.164 ids de face escritos à mão, 42% do arquivo, 26 dos 51 passos só pintura).
- **D-127** · 2026-07-25 · ESPAÇO MATERIAL FEITO — o chip "Material" da barra vira um espaço de verdade (`oficina.html`, "Passo 19"), e com ele o seletor de espaços passa de 2 pra 3 vias. Sozinho, sem subagentes. **Não é um P9x — é o conserto que a investigação do fim do P9 achou**, a pedido do ideador ("clico em cima e nada acontece — ou ela precisa de algum requisito?"). A resposta medida: nem requisito, nem quebrado — **HTML morto**. O `<span class="chip">Material</span>` estava lá desde o 13b sem `id` e sem `addEventListener` (confirmado varrendo os 64 `addEventListener` do arquivo: nenhum o alcança, e não existe handler delegado por texto), então clicar nele *não podia* fazer nada. Enquanto isso a spec (D-73) prometia um espaço de verdade — "Modelar, Material e Animação" — e a funcionalidade JÁ existia, só pendurada no lugar errado: `#blocoCor` (passo 9) e `#blocoMaterial` (12a) apareciam soltos DENTRO do Modelar, com o comentário do HTML admitindo a dívida ("Material segue contextual, como hoje"). Era o pior dos mundos: um chip que promete um espaço, tem a aparência dos que funcionam, e é inerte. **O estado vira 3 vias:** `animLigado` (booleano) → `espaco` (`'modelar'|'material'|'animacao'`), `setEspacoAnim(on)` → `setEspaco(nome)`, 18 pontos de leitura reescritos um a um. **Cor e Material mudam de casa:** o gate `!modelar` virou `espaco !== 'material'` — a mudança de comportamento real da rodada (escolher cor agora exige trocar de espaço), que de quebra desafoga a coluna do Modelar, que tinha acumulado 12 blocos e já havia causado a regressão de layout do P9b (D-124). **O Pincel NÃO foi junto, de propósito:** ele já era independente do espaço (só depende de `pincelLigado`; o clique no chip nunca teve guarda nem contra Animação — só o atalho de teclado tinha) e a spec promete "parâmetros" no espaço Material, não pintura; mover empilharia risco sem necessidade. Fica anotado como ajuste possível se a coluna ainda estiver longa. **Compatibilidade preservada de fora:** `espacoAnim()`/`ligarAnim()`/`animEstado().animLigado` continuam com o MESMO comportamento externo (viram invólucros finos sobre `espaco`; a chave `animLigado` do objeto de estado fica com o nome de sempre) — e `ligarAnim(false)` SEMPRE volta pro Modelar, nunca Material, o caso que quebraria calado numa tradução ingênua. Hooks novos: `espaco()` (a string crua) e `ligarMaterial(b)`. **2 achados — e a diferença desta rodada é que NENHUM dos dois veio de rodar.** **(1) Na investigação, ANTES de codar:** reusar `!animLigado` como "fora de Modelar" teria vazado os blocos exclusivos de Animação pro Material, porque uma negação só não separa 3 estados — `atualizarPainel` escondia `blocoAnim`/`blocoPartes`/`blocoEsqueleto`/`blocoPeso`/`blocoAnims` (+ `blocoChave` + a linha do tempo) com `hidden = modelar`, e sob 3 vias isso os MOSTRARIA em Material. Os pontos que dependiam dessa leitura viraram testes diretos contra `espaco === 'animacao'`; a bancada confirma o resultado, não foi ela que achou o problema. Corolário do mesmo raciocínio: "sair de Animação" passou a ser medido direto (`espaco === 'animacao'` ANTES da troca) em vez de inferido do destino — então a troca DIRETA Animação→Material já reseta o preview certo, sem caso especial (a generalização certa isolou a regra que importa da regra que era só coincidência de o único destino ser Modelar). **(2) Na revisão adversarial do próprio diff, com tudo JÁ verde:** a dica do painel Vértice (`pvDica`) dizia "escolha uma cor pra pintar" com uma face selecionada — e ela vive dentro do `blocoVertice`, que é Modelar-only. Com a Cor mudando de espaço, a dica passou a MENTIR: mandava escolher cor justamente onde o seletor não existe mais. Nada pegaria: é texto, não comportamento (nenhuma asserção lê `pvDica`), e a verificação manual não pegou porque eu estava conferindo o que MUDOU, não o que ficou para trás. Lição registrada: mover uma funcionalidade de lugar não é só mover o bloco — é caçar todo texto que apontava pra ela de onde ela morava. **Prova:** `npm run oficina` inteiro — **402 asserções, 0 falhas** (384 da baseline + as 18 novas do Passo 19). As 4 falhas da primeira rodada completa foram todas ESPERADAS e são a própria prova de que a mudança pegou: eram asserções dos passos 9/12a que selecionavam uma face e esperavam Cor/Material aparecerem **no Modelar** — o comportamento que esta rodada mudou; ajustadas pra entrar no espaço Material antes de afirmar (e voltar depois). Uma delas foi especialmente instrutiva: `(9 várias)` gravou `['solido',{faces:[0..9]}]` em vez do pincel, porque o clique por COORDENADA CRUA nos swatches, com o bloco escondido, acertou o botão "marcar sólido" — a mesmíssima classe de bug de layout do D-124, agora do outro lado (bloco que SOME, não que aparece). O Passo 19 cobre: chip existe/começa apagado/clique REAL acende só ele e muda `espaco()`; Cor/Material escondidos em Modelar e visíveis em Material com o conteúdo batendo `corEfetiva()`/`materialDaFace()` (lido do estado, não hardcoded — D-116); todos os blocos Modelar-only sumindo em Material; tecla E não extrudando fora do Modelar (canon inalterado); pincel ainda ligando em Material; os 3 hooks de compat; `ligarMaterial` ida e volta; a transição direta Animação(tocando)→Material resetando o preview; e ida-e-volta pelos 3 espaços sem mudar 1 byte do canon (D-73: câmera/seleção/objeto ficam). `npm test` 230 (inalterado — não toca núcleo), typecheck 0, `executar` replay provado, `auditar` com os MESMOS 3 achados pré-existentes de sempre (arco/arvore-cartoon/vegetacao-cartoon, nada novo), `porteiro` 7/7, `jogar` ready=true 11fps 0 erros (rodado sob carga — vários gates em paralelo), `mapa`/`docs:toc` em dia. `git diff` de `motor/oficina.js`/`render.js`/`som.js` VAZIO — 4 arquivos mudaram (`oficina.html`, `tools/bancadas/oficina.mjs`, `docs/oficina.md`, `docs/playground.md`), interface pura. Próximo: atualizar as skills `oficina` e `nos-fluxo` (defasadas — roteiro fechado como se ativo, contagens velhas, e um fluxo coder+revisor que não é o praticado há 10 rodadas), e aí a Aba Desenho.
- **D-126** · 2026-07-25 · Playground P9d FEITO — os blocos `#blocoGrupo` e `#blocoRegiao` (`oficina.html`, "Passo 18"), seleção por GRUPO e por REGIÃO generalizada pro espaço Modelar (`resolverAlvosV`/P8a já aceitava os dois no núcleo, D-121; a UI só tinha grupo, parcial, dentro do espaço Animação). Sozinho, sem subagentes. **Diferença de NATUREZA em relação a P9a/b/c:** os três rounds anteriores gravavam PASSOS (criar forma, editar, transformar); estes dois blocos são SÓ SELEÇÃO — populam `selVertices`/`selFaces` do mesmo jeito que um clique normal, nunca tocam PASSOS. Consequência prática, não hipotética: os botões dos blocos Editar (P9b)/Ruído/Transformar (P9c) já funcionam em cima do que Grupo/Região selecionam, de GRAÇA — zero linha mudada neles, provado rodando (a bancada não precisou de nenhuma alteração nos passos 16/17 pra isso valer). **Grupo REUSA em vez de duplicar:** `animCtl.selecionarParte` (que já existia, usado pela lista de Partes em Animação) virou a fonte ÚNICA — o bloco novo em Modelar só chama o MESMO método, não uma cópia. Nomear uma parte continua sendo ação do espaço Animação (D-96 não mudou; o roadmap pedia "seleção", não "autoria", generalizada). **Região é seleção genuinamente NOVA** (não existia UI nem em Animação): 6 campos min/max × x/y/z inclusivos (a mesma lei do núcleo — sem sentinela infinita, os dois lados obrigatórios) + um botão "usar caixa do objeto" que preenche os campos com o bbox ATUAL SÓ NAQUELE CLIQUE — nunca autopreenche sozinho, porque os campos são uma CONSULTA que a pessoa compõe (a mesma distinção já usada pros painéis Ruído/Transformar do P9c: campo-consulta não é campo-retrato, não pode sobrescrever o que foi digitado). **2 achados reais, os dois pegos RODANDO — nenhum por leitura de código:** **(1) escopo.** O hook novo `gruposDisponiveis` chamava `partesNomeadas()` direto — mas essa função vive dentro de um bloco aninhado (o mesmo onde `atualizarPainel` e o resto da autoria de Animação moram) que o objeto `window.__oficina` NÃO enxerga (`window.__oficina` é construído num escopo mais externo, antes/fora daquele bloco — confirmado pelo padrão `if (animCtl)` que os outros hooks já usavam, sinal de que `animCtl`/`malhaCtl` só existem como REFERÊNCIAS tardias ali, não definições locais). `ReferenceError: partesNomeadas is not defined`, só na hora de RODAR o script de verificação manual — a leitura do código não acusava nada, porque textualmente parecia "óbvio" que uma function declaration no mesmo módulo seria visível em todo lugar (hoisting de módulo inteiro, não de bloco — a suposição errada). Consertado trocando pra `animCtl.partes()`, que JÁ tinha o closure certo (é chamado de DENTRO do mesmo bloco aninhado onde `partesNomeadas` mora). **(2) guarda faltando.** Os botões de Grupo (chips) e Região não desabilitavam durante um arrasto — o único par de blocos, desde o P9a, sem essa guarda "um dono por vez" que todo outro botão tem. `selecionarFaces`/`animCtl.selecionarParte` (os métodos JÁ existentes que o bloco Grupo reusa) nunca tiveram guarda de arrasto porque também servem de hook de TESTE (setup direto, sem simular clique) — adicionar a guarda ALI teria mudado infraestrutura compartilhada usada por toda a bancada, fora do escopo desta rodada. Conserto contido: a guarda entrou SÓ no novo hook `selecionarGrupo` (`if (arrasto) return`) e nos botões do DOM (`disabled=!!arrasto`, que já bloqueia o clique real por construção do browser — um botão `disabled` não dispara evento nenhum); `selecionarRegiao` (método genuinamente novo desta rodada) já nasceu com a guarda, como todo método novo desde o P9b. Os dois achados foram pegos e consertados ANTES de escrever a bancada formal (na verificação manual), não depois. **Prova:** `npm run oficina` inteiro — **384 asserções, 0 falhas** (370 da baseline + as 14 novas do Passo 18, zero regressão nos passos 2–17, verde JÁ na primeira rodada completa depois dos dois consertos). O Passo 18 cobre: grupo vazio (placeholder correto); nomear 'topo' pelo fluxo REAL do espaço Animação (chip→seleciona→nomeia→chip) aparecendo no bloco Grupo de Modelar — a prova de ponta a ponta que a generalização funciona de verdade, não só no papel; clique REAL no chip seleciona as faces certas; nome inexistente é no-op; "usar caixa" + "Selecionar região" REAIS com o bbox inteiro pegam TODOS os 8 vértices do cubo-fixture, e o bbox usado na asserção é DERIVADO do canon real, nunca hardcoded (D-116); uma região mais estreita (y≥0.4) pega EXATAMENTE o subconjunto também derivado do canon (não assumido da geometria do cubo); região vazia/inválida tratadas; guarda de arrasto desabilitando no DOM e nos hooks. `npm test` 230 (inalterado — P9d não toca núcleo), typecheck 0, `executar` replay provado, `auditar` com os MESMOS 3 achados pré-existentes de sempre (arco/arvore-cartoon/vegetacao-cartoon, nada novo), `porteiro` 7/7, `jogar` ready=true 37fps 0 erros, `mapa`/`docs:toc` em dia. `git diff` de `motor/oficina.js`/`render.js`/`som.js` VAZIO — só 4 arquivos mudaram (`oficina.html`, `tools/bancadas/oficina.mjs`, `docs/playground.md`, `docs/oficina.md`), interface pura. **Com P9d fechado, o épico P9 core está completo** (P9a-d, todas as ops do playground com botão) — falta só a Aba Desenho (canvas vetor + pintura livre), documentada desde o início como capítulo à parte maior que o resto do P9 somado, não teto de uma rodada. Próximo: a Aba Desenho, ou a moto do Tron (a régua de pronto) usando o vocabulário+interface que já existe de ponta a ponta.
- **D-125** · 2026-07-25 · Playground P9c FEITO — os blocos `#blocoRuido` (op `displace`, P8c/D-122) e `#blocoTransformar` (`espelha`/`rotaciona`, P3/D-116), no NÚCLEO já provados, sem UI até aqui. Sozinho, sem subagentes. **Visibilidade:** os dois aparecem com QUALQUER seleção — vértice(s) OU face(s), o núcleo do `displace`/`rotaciona` aceita qualquer contagem (`resolverAlvosV`), ao contrário do par-EXATO que o bloco Editar (P9b) exige pro `moveA`. Espelhar fica DESABILITADO sem face (a op só aceita `sel.f` — `sel.v` GRITA, doc da op) — o único dos 3 controles com essa restrição extra. **Posição:** os dois nascem por ÚLTIMO no painel Modelar, direto — a mesma lição do P9b (D-124) aplicada de saída, sem precisar redescobrir: nada fica visível ANTES de algo que um clique de coordenada crua dependa. **Depois de espelhar, a seleção pula pras faces NOVAS** (calculadas por id no bloco do passo, o mesmo padrão do `adicionarForma`/P9a) — deixa ver/ajustar o resultado na hora. `Ruído` é o MOLDE do painel Material (campos persistentes + botão aplicar, não deltas que zeram — ao contrário do `dX/dY/dZ` do bloco Editar); `graus` do Rotacionar É um delta relativo e ZERA no sucesso, a mesma lei do passo 16. Guardas: `amplitude`/`pos` além de ±100 RECUSAM inteiro (D4, LIM_VALOR — `graus` fica de FORA do clamp, não é distância de mundo); `amplitude=0`/`graus=0` são no-op (D3). **1 achado real no processo, pego RODANDO o round-trip serialização, não por leitura:** com `displace` na lista de PASSOS, o canônico da PÁGINA (Chromium) e o do NODE não batiam BIT-A-BIT — divergiam na 12ª-13ª casa decimal (~1e-13 a ~7e-13, medido nos 12 vértices). Diagnosticado com um script à parte que reconstrói a MESMA sequência e diffa vértice a vértice antes de mexer no teste (nunca ajustar epsilon sem ver o número real primeiro). **Causa:** `hash3` (o ruído do `displace`, D-122) usa `Math.sin` — o ECMAScript garante bit-exatidão só pra `+`/`-`/`*`/`/`; funções TRANSCENDENTAIS (seno/cosseno) são de IMPLEMENTAÇÃO, então o V8 do Chromium (a página) e o do binário Node (a bancada) — builds distintas do mesmo motor — podem divergir por ULPs. **Não é bug do displace nem desta rodada:** dentro do MESMO engine (o caso real — o jogo OU a ferramenta rodam um motor só, nunca comparados um contra o outro) o resultado é 100% determinístico; a divergência só aparece NESSE cross-check síntético página↔Node, que nenhuma rodada anterior tinha exercido com uma op baseada em `Math.sin` (P8b/chamferBox e as ops de P3/P8a usam só aritmética exata). Nenhuma outra op do playground usa transcendental — cubo/espelha/rotaciona/moveV/etc. seguem BIT-A-BIT em todo o resto da bancada (passos 10/15/16, confirmado ainda idênticos). **Conserto — não no núcleo, no CRITÉRIO de comparação:** a asserção final passou a exigir estrutura EXATA (mesmos ids de vértice/face, mesma contagem V/F/órfãos, winding e atributos de face IDÊNTICOS) + posição de vértice dentro de epsilon 1e-9 — ~4 ordens de grandeza de folga sobre o ruído medido (generosa o bastante pra nunca disparar num engine novo, apertada o bastante pra continuar pegando um bug de verdade, que erraria por muito mais que ULPs). **Prova:** `npm run oficina` inteiro — **370 asserções, 0 falhas** (346 da baseline + as 24 novas do Passo 17, zero regressão nos passos 2–16). O Passo 17 cobre: blocos escondidos sem seleção / visíveis com face ou vértice(s); ruído real (fill+clique) grava `displace` com o valor digitado, os 4 cantos MOVEM dentro de `amplitude` (a fórmula do ruído já é do P8c — aqui só confere que o valor digitado chega inteiro); espelha real grava `espelha`, o Nº de vértices NOVOS é DERIVADO do canon real (weld ou não, nunca hardcoded) e F cresce EXATAMENTE +1; rotaciona real (em face E em vértices) bate uma REIMPLEMENTAÇÃO INDEPENDENTE da fórmula `p'=pivo+R_eixo(graus)·(p−pivo)` do comentário da op — não importada do núcleo, a mesma disciplina do canon-antes+d do passo 16 agora pra rotação; guarda (arrastar 1 de 3 vértices RESSELECIONA sozinho pra 1 — a lei do passo 8 — mas os blocos CONTINUAM visíveis porque 1 ainda é "alguma seleção", diferente do par-exato do P9b; os hooks devolvem `null` mesmo assim); desfazer/refazer; serialização ida-e-volta (com o epsilon documentado acima). `npm test` 230 (inalterado — P9c não toca núcleo), typecheck 0, `executar` replay provado, `auditar` com os MESMOS 3 achados pré-existentes de sempre (arco/arvore-cartoon/vegetacao-cartoon, nada novo), `porteiro` 7/7, `jogar` ready=true 18fps 0 erros, `mapa`/`docs:toc` em dia. `git diff` de `motor/oficina.js`/`render.js`/`som.js` VAZIO — só 4 arquivos mudaram (`oficina.html`, `tools/bancadas/oficina.mjs`, `docs/playground.md`, `docs/oficina.md`), interface pura. Próximo: P9d (seleção por região/grupo generalizada pro espaço Modelar) e a Aba Desenho (canvas vetor + pintura livre) como capítulo à parte, maior que o resto do P9 somado.
- **D-124** · 2026-07-25 · Playground P9b FEITO — o bloco `#blocoEditar` (`oficina.html`, "Passo 16"), os primeiros botões pras 4 ops de edição do P8a (`moveF`/`moveA`/`vira`/`apagaFace`, D-121, núcleo intocado). Sozinho, sem subagentes. **O despacho é pelo FORMATO da seleção**, sem seletor de modo à parte: face ATIVA → Mover grava `moveF` + Inverter/Apagar ficam disponíveis (a mesma convenção do handle de extrude — "a seta extruda só a ativa" mesmo com várias marcadas); EXATAMENTE 2 vértices selecionados → Mover grava `moveA` nas duas pontas. **`moveA` não precisou de hit-test de aresta novo nenhum:** o par-de-2 que o Shift+clique do passo 8 já monta SERVE, porque o `moveA` do núcleo não exige que as duas pontas estejam de fato ligadas por uma face (doc da op, D-121) — o playground.md apontava isso como "nasce aqui" e nasceu por REUSO, não por feature nova. `dX/dY/dZ` é um delta RELATIVO somado — diferente do painel Vértice (alvo absoluto, só faz sentido pra 1 vértice) — guardado como o valor exato do passo 6: `[0,0,0]` não grava passo fantasma (D3), componente além de ±100 é RECUSADO inteiro, não clampado (D4). **2 achados no processo, os dois pegos RODANDO a bancada de verdade, não por leitura — a mesma lição D-116 se repetindo duas vezes na mesma rodada:** **(1) colisão de nome silenciosa.** O método de despacho nasceu com o nome `mover(d)` — mas `malhaCtl` já tinha um `mover(cx,cy,ctrl)` desde o passo 4 (o rastreio de arrasto de vértice a cada `pointermove`); dois métodos com a MESMA chave num objeto literal, o segundo (mais abaixo no arquivo) vence silenciosamente — `malhaCtl.mover` nunca foi o meu. `moverSelecao([0.05,0,0])` devolvia `undefined` sem erro nenhum (o método de arrasto só tem `if (!arrasto) return;`), e só a comparação `PASSOS antes === PASSOS depois` no script de verificação manual acusou que nada tinha sido gravado. Renomeado pra `moverSelecao` (chave única, conferida por grep). **(2) layout empurrando um teste ALHEIO, achado só na 2ª rodada da bancada completa.** `blocoEditar`, posicionado entre Vértice e Cor, fica visível sempre que uma face está ativa — inclusive durante "(9 várias)" (passo 9, pintar 3 faces), que nunca teve nada a ver com esta rodada. O bloco novo empurrava `#pcPresets` (os swatches de cor) pra BAIXO da área visível do painel rolável (`#props`, `overflow:auto`); o clique do teste usa COORDENADA CRUA (`getBoundingClientRect`+`page.mouse.click`, não `page.click`, que rola sozinho) e errava o alvo — 2 falhas silenciosas e determinísticas, reproduzidas 2× seguidas com o diff presente. **Antes de aceitar como regressão, testei a hipótese alternativa (flake pré-existente) por MEDIÇÃO, não suposição:** `git stash` das minhas mudanças + `npm run oficina` na baseline limpa → 0 falhas, BANCADA OK — confirmado que a causa era mesmo o bloco novo. Conserto: `blocoEditar` movido pro FIM do painel Modelar (depois de Colisão), a única posição onde ficar visível nunca empurra outro bloco pra fora da viewport (o único botão depois dele, `btSolido`, usa `page.click`, que rola sozinho — imune por construção). Rodada seguinte: limpa. **Prova:** `npm run oficina` inteiro — **346 asserções, 0 falhas** (320 da baseline + as 26 novas do Passo 16, zero regressão nos passos 2–15). O Passo 16 cobre: bloco oculto sem seleção / visível com face ou par-de-2; clique REAL (fill+botão, não só hook) grava `moveF`/`vira`/`apagaFace`/`moveA` com o valor DIGITADO; os cantos/pontas de fato SOMARAM o delta (canon ANTES+d == canon DEPOIS, computado do estado real, nunca hardcoded à mão — D-116); o `vs` pós-`vira` é o `.slice().reverse()` exato do de antes; `apagaFace` tira a face do canon (F−1) mantendo os vértices (V igual) e zera a seleção; guarda de arrasto (arrastar um vértice do par RESSELECIONA sozinho — a lei do passo 8 — escondendo o bloco; mesmo assim os 3 hooks devolvem `null` durante o arrasto, a prova real do guard); desfazer/refazer; serialização ida-e-volta idêntica. `npm test` 230 (inalterado — P9b não toca núcleo), typecheck 0, `executar` replay provado, `auditar` com os MESMOS 3 achados pré-existentes de sempre (arco/arvore-cartoon/vegetacao-cartoon, nada novo), `porteiro` 7/7, `jogar` ready=true 15fps 0 erros (rodado sob carga pesada — vários gates em paralelo — número mais baixo que o usual, ainda ready), `mapa`/`docs:toc` em dia. `git diff` de `motor/oficina.js`/`render.js`/`som.js` VAZIO — só 4 arquivos mudaram (`oficina.html`, `tools/bancadas/oficina.mjs`, `docs/playground.md`, `docs/oficina.md`), interface pura. Próximo: P9c (painel do `displace` + botões `espelha`/`rotaciona`), P9d (seleção por região/grupo generalizada), e a Aba Desenho como capítulo à parte, maior que o resto do P9 somado.
- **D-123** · 2026-07-25 · Playground P9a FEITO — "Adicionar forma", o primeiro botão de criação da Oficina (`oficina.html`, "Passo 15"), SEM tocar o núcleo. Sozinho, sem subagentes. **Achado da investigação, ANTES de codar (define o tamanho do épico P9 inteiro):** nenhuma op do playground (P1-P8) tinha QUALQUER botão até aqui — nem `espelha`/`rotaciona`, que existem desde o P3 — e não existia NENHUM jeito de criar geometria do zero pela UI (só editar uma peça já escrita à mão; abrir `_vazio.js` no editor era beco sem saída, sem botão pra colocar o 1º vértice). Achado técnico que baratea o resto do P9: `serializarPeca`/desfazer/refazer já são 100% GENÉRICOS — não sabem nome de op nenhum, não precisaram de UMA linha de mudança pras ops novas (confirmado rodando). **O bloco `#blocoAdicionar`** — o ÚNICO do painel sempre visível em Modelar SEM depender de seleção nenhuma — lista as 6 primitivas de parâmetro ESCALAR (`cubo`/`cilindro`/`esfera`/`cone`/`plano`/`chamferBox`; `lathe`/`loft`/`inflate` ficam de fora de propósito — pedem perfil/contorno, um array de pontos, não campo escalar — isso é a Aba Desenho, ainda placeholder). Escolher o tipo reconstrói os campos (tabela `FORMAS`, só apresentação — o núcleo já valida/clampa cada argumento sozinho); o botão empurra `[tipo,{...campos}]` no fim de PASSOS e seleciona as faces novas (calculadas por id no bloco do passo — `[base,base+BLOCO)` — nunca hardcoded por tipo, funciona pra QUALQUER primitiva igual). Numeração POSICIONAL (`baseDoPasso`) garante que adicionar formas em sequência NUNCA colide de id, não importa quantas já existem — confirmado, não só assumido. Nasce sempre na ORIGEM; reposicionar é o vértice/gizmo já existentes (arrastar), decisão consciente de NÃO inventar um campo de posição na criação — escopo contido. Parâmetro inválido (ex.: `chanfro` fora da faixa do `chamferBox`) GRITA — o passo ENTRA em PASSOS mas nasce sem face nenhuma — e a dica do painel avisa em vez de falhar silencioso; a geometria já existente fica intacta (provado). **Achado no processo (pego pela PRÓPRIA bancada rodando, não hipotético):** o teste do guard-de-arrasto ("um dono por vez", o botão não funciona com uma edição em voo) soltava o mouse numa posição DESLOCADA do ponto de partida — passou do limiar de clique e gravou um `moveV` de verdade, um passo a mais que eu não previa; os 3 testes seguintes (undo/redo/serialização) todos FALHARAM em cadeia por causa desse ÚNICO passo fantasma (a contagem de PASSOS ficou 1 a mais do que o teste esperava). Não era bug na feature — era bug no MEU teste; a prova: `adicionarForma()` já devolvia `null` e o botão já ficava `disabled` corretamente durante o arrasto, só a limpeza (soltar o mouse) que tinha efeito colateral não-intencional. Conserto: soltar de VOLTA na posição original (deslocamento líquido ~0, abaixo do limiar) — `soltar()` não grava nada, e capturar o estado ANTES de soltar, não depois. É a MESMA lição de sempre (D-116, "prove por medição") aplicada ao PRÓPRIO teste, não só à op: rodar de verdade achou um erro que a leitura do código não tinha achado. **Prova:** verificação manual em browser real ANTES de escrever teste formal (screenshot conferido: as 6 formas renderizam, se sobrepõem na origem como documentado, sem erro de página); depois a bancada `tools/bancadas/oficina.mjs` ganhou a seção "Passo 15" (17 asserções): bloco visível só em Modelar sem seleção e escondido em Animação; `<select>` bate `tiposForma()`; trocar tipo reconstrói campos batendo `camposForma(tipo)`; clique REAL (select+fill+botão, não só o hook) grava o valor DIGITADO; a forma nasce com as próprias faces selecionadas (72 faces de uma esfera aneis=9×lados=8, ativa=71); 6 formas em sequência com o canônico da PÁGINA batendo BIT-A-BIT contra `nucleo()` rodado em NODE na MESMA lista lida da página (não recalculada à mão); guarda de arrasto; parâmetro inválido não corrompe a geometria existente; desfazer/refazer; serialização ida-e-volta idêntica (inclusive o órfão do chamferBox inválido). **`npm run oficina` inteiro: 320 asserções, 0 falhas** (passos 2 a 14b intactos — zero regressão — + as 17 novas do passo 15). `git diff` de `motor/oficina.js`/`render.js`/`som.js` VAZIO — só 4 arquivos mudaram (`oficina.html`, `tools/bancadas/oficina.mjs`, 2 docs), nenhum toca núcleo ou jóia. Próximo: P9b (botões `moveF`/`moveA`/`vira`/`apagaFace` — `moveA` precisa de seleção de ARESTA, que ainda não existe na UI), P9c (painel do `displace` + botões `espelha`/`rotaciona`), P9d (seleção por região/grupo generalizada), e a Aba Desenho (canvas vetor + pintura livre) como capítulo à parte, maior que o resto do P9 somado.
- **D-122** · 2026-07-25 · Playground P8b+P8c FEITO — `chamferBox` (primitiva nova) e `displace` (ruído seedado) no NÚCLEO da Oficina, SEM interface. Sozinho, sem subagentes. **Com esta rodada, P8 fecha por completo** — as seis peças soltas do roteiro (moveF/moveA/vira/apagaFace do P8a + estas duas) estão todas prontas; falta só P9 (interface) e a régua de pronto (moto do Tron), nenhuma bloqueia a IA criar hoje. **`chamferBox`** — o `cubo` com CANTOS E ARESTAS chanfrados (corte flat, não arredonda — a mesma classe honesta do "lathe só reto por enquanto"). Fórmula fechada como cubo/esfera/cone, SEM parâmetro TOPO: a contagem é SEMPRE 24 vértices/26 faces (bem abaixo do bloco — sem guarda de overflow, não tem como estourar). **A decisão de método quase saiu errada, e foi pega ANTES de escrever a op de verdade:** a 1ª derivação à mão puxava o vértice de cada canto por UM eixo só ("truncagem" — corta a quina com 1 plano); rodei a característica de Euler num script fora do repo pra conferir e V−E+F ≠ 2 — a malha não fechava. A fórmula certa é CANTELAÇÃO: cada uma das 3 faces que se encontram num canto encolhe por conta própria (o vértice "da face X" mora nela — coordenada X no valor cheio do canto — as outras duas encolhidas `chanfro` pra dentro); refiz a derivação, a incidência por vértice bateu grau 4 (1 face original + 2 retângulos de aresta + 1 triângulo de canto), Euler fechou (24−48+26=2) — só então o código real entrou em `motor/oficina.js`. Winding não tem giro único (ao contrário do cilindro/esfera — sem simetria rotacional aqui): decidido por AUTO-ORIENTAÇÃO (cada face nasce numa ordem fixa e se corrige contra o centro real da caixa via Newell, em vez de uma tabela de sinal por canto decorada à mão — mais fácil de errar decorando do que deixar a própria geometria decidir), verificado nas 26 faces por teste, não numa amostra. Validade: `chanfro` `>0` e `<min(larg/2,prof/2,alt/2)` — medido o LIMITE EXATO antes de travar o teste de fronteira (c=0,499999 limpo; c=0,5 já degenera, normal zerada e 16 arestas soltas; c=0,500001 quebra de vez, 32). **`displace`** — desloca uma seleção (`resolverAlvosV`, reuso do P8a) ao longo da NORMAL MÉDIA (Newell das faces atuais que tocam o vértice, medida antes de mover qualquer uma) por ruído de valor 3D SEEDADO (`hash3`+`ruido3`, novo utilitário do núcleo — hash barato por seno, a mesma classe de determinismo do resto do núcleo, interpolado por smoothstep nos 8 cantos do reticulado; devolve [0,1), remapeado pra [−amplitude,+amplitude]). **FORMATO SALVO:** a fórmula do ruído é o que faz a mesma `semente` reproduzir o mesmo relevo sempre — documentado como tal (mudar a fórmula reformaria toda peça que usa `displace`, a mesma classe de "nunca muda depois de shipar" da numeração). Vértice sem NENHUMA face (`apagaFace` deixou solto) GRITA — sem normal pra seguir, nunca desloca às cegas. Id-estável puro (não cria/apaga nada): o manifold de uma malha já fechada sobrevive por cima do relevo. Peça-exemplo `pecas/_pedra.js`: as duas juntas — chamferBox de base + displace por cima — dão pedra lascada sem precisar de `inflate` (que já cobria "corpo largura≠altura", mas sai BLOCKY; aqui as faces continuam PLANAS, só com relevo). **Prova por medição, ANTES de escrever a op de verdade — não só depois (o método vindo se consolidando desde o D-116):** 3 scripts descartáveis fora do repo, nesta ordem — (1) a característica de Euler pegou a derivação errada da topologia antes de qualquer código real no núcleo; (2) a topologia certa (cantelação + auto-orientação) reverificada em geometrias variadas (não-cúbica, achatada, bem assimétrica) — manifold 0, as 26 normais pra fora, volume sempre positivo e sempre MENOR que a caixa reta; a fronteira exata do `chanfro` válido varrida ponto a ponto; (3) o ruído amostrado 200 mil vezes pra faixa [0,1), suavidade (passo de 0,001 muda o valor em ~0,0013 — não é ruído branco), sensibilidade à semente, determinismo, e exatidão no canto do reticulado. Depois, no vitest: numeração id-a-id derivada da fórmula documentada e medida (não recontada); manifold+winding+volume em 4 geometrias dentro do núcleo real; fronteira exata replicada como teste permanente; magnitude do deslocamento do `displace` recomputada byte-a-byte contra a fórmula (não só comparada consigo mesma); vértice sem face grita e fica parado; seleção parcial só afeta o alvo; composição com inflate/rotaciona/espelha sem NaN e manifold preservado; determinismo bit-a-bit nos dois. **230 testes (+12 desde o P8a), typecheck 0.** `executar` replay provado; `auditar` com os MESMOS 3 achados pré-existentes de sempre (arco/arvore-cartoon/vegetacao-cartoon — byte-idênticos à baseline, nenhum novo); `npm run criar -- _pedra` **APROVADO** (núcleo 24V/26F/0 órfãos, manifesto em dia com a skill `criar-peca` — confirmado numa mudança REAL, não só o teste sintético do P7 —, auditar limpo, porteiro com frame são; render conferido a olho: cantos chanfrados visíveis como facetas distintas no modo `geo=normais`, sem buraco nem winding invertido); `porteiro` 7/7; `jogar` ready=true, 31fps, 0 erros. Jóias `render.js`/`som.js` diff-vazio. Próximo: P9 (interface) ou a moto do Tron — o vocabulário do playground está completo (P1-P8).
- **D-121** · 2026-07-25 · Playground P8a FEITO — `moveF`/`moveA`/`vira`/`apagaFace` + seleção por região/grupo no NÚCLEO da Oficina, SEM interface. Sozinho, sem subagentes. P8 é seis peças soltas (não uma numeração salva nova compartilhada); esta rodada fecha quatro ops de edição id-estável (nenhuma cria id, nenhuma renumera — a mesma classe do `moveV`/`extruda`/`mescla`) mais a extensão de seleção; ficam pro P8b/P8c: `chamferBox` (primitiva nova) e `displace` (ruído com semente). **`moveF`** (face, `d`): move TODOS os cantos pelo mesmo delta, ADITIVO; um canto compartilhado com outra face move JUNTO (comportamento normal de "mover face" num editor — pra não afetar vizinho, usa `extruda` antes). **`moveA`** (`a`,`b`,`d`): move as duas pontas de uma aresta, açúcar sobre dois `moveV` — NÃO exige conectividade real entre `a`/`b` (mover dois vértices juntos nunca corrompe, então checar conectividade só atrapalharia um uso legítimo). **`vira`** (face): inverte o winding revertendo `f.vs`. **`apagaFace`** (face): remove a face de `st.F`; os vértices CONTINUAM (buraco de propósito). Todas as quatro: alvo inexistente GRITA, nunca corrompe. **Seleção por região/grupo:** `resolverAlvosV`, um helper NOVO e COMPARTILHADO, generaliza o `sel` do `rotaciona` (que já existia desde o P3) com `{regiao:{min,max}}` (caixa delimitadora inclusiva — os dois lados OBRIGATÓRIOS, escolha de design: `Infinity` como sentinela de "sem limite" NÃO dá, porque o `st.num` já recusa não-finito por lei desde o P5/D-118 — achei esse conflito DURANTE o desenho, antes de escrever uma linha de código, simulando mentalmente contra a regra já existente) e `{grupo:'nome'}` (as faces daquele `f.parte` — REUSA a nomeação que a op `parte` do passo 13a/D-95 já registra, não inventa um 2º jeito de agrupar). O `rotaciona` foi REFATORADO pra usar o helper em vez da lógica inline — reproduz `{v}`/`{f}` byte-a-byte (141 testes preexistentes continuam batendo sem alteração, prova de que o refactor não mudou comportamento). **2 achados no processo, os dois consertados/documentados, nenhum bloqueante:** **(1)** `moveF` lia e escrevia a MESMA posição dentro do laço sobre `f.vs` — se uma face tivesse um canto REPETIDO (bowtie), o delta aplicaria 2× nessa iteração; investigado e confirmado INALCANÇÁVEL hoje (a `mescla` já remove toda face bowtie, D2/D-86) mas blindado mesmo assim (`new Set(f.vs)` no laço) — defesa barata contra um risco latente, mesmo sem caminho de exploração conhecido. **(2)** `vira` numa face JÁ consistente com as vizinhas DESALINHA o pareamento de aresta do teste de manifold (medido: 4 arestas soltas ao virar o topo já-correto de um cubo) — não é bug, é a natureza de uma ferramenta cirúrgica (o uso responsável é o oposto: consertar uma face que já nasceu de costas, e aí virar RESTAURA o pareamento — provado virando duas vezes e voltando a manifold limpo); documentado no comentário da op E travado por teste dedicado, não só descoberto e escondido. **Prova (por medição, todos os eixos, antes de confiar):** cada op testada isoladamente (moveF move o topo mantendo a base intacta; moveA move as duas pontas e ainda move a válida quando a outra é inexistente; vira inverte o sinal do Newell sem mudar V/F; apagaFace remove só a face, vértices sobrevivem); `resolverAlvosV` testada com regiao válida/incompleta e grupo válido/inexistente, cada grita correto; composição com extruda/mescla/espelha/rotaciona/pincel/liso/solido/parte — sem NaN/órfão em NENHUM caso, manifold intacto nos que deveriam continuar fechados (excluindo os dois casos que abrem/desalinham o mesh DE PROPÓSITO, documentados como tal, não escondidos atrás de uma asserção que mascarasse o achado); determinismo bit-a-bit; o guard de não-finito do P5 (D-118) se propaga automaticamente (herdado de `st.vec`, sem código extra). `npm test` 218 (+17 desde o P7), typecheck 0, `criar`/`executar`/`auditar`/`porteiro` sem mudança de veredito em nenhuma peça — e o cruzamento do manifesto (D-120) confirmou a doc em dia com o núcleo numa mudança REAL (não só o teste sintético do P7), a primeira prova de uso genuíno da ferramenta. Jóias `render.js`/`som.js` diff-vazio. Próximo: P8b (`chamferBox`) e P8c (`displace`), ou a moto do Tron se o ideador preferir testar o vocabulário atual primeiro.

- **D-141** · 2026-07-29 · **O fonte do 2D migrado pro `nos-mentes`, e o `nos` enxugado de 20 branches para 1.** A limpeza de branches quase apagou o 2D de vez, e a checagem que evitou isso vale mais que a limpeza. **O que se descobriu, medindo:** (1) a `main` do `nos` tem **zero** commits tocando `site/`/`engine/`/`world/` — ela tem raiz DIFERENTE das branches antigas (`git merge-base` retorna "no merge base"), então a remoção física da D-111 não deixou o 2D "no histórico", como o `CLAUDE.md` afirmava; (2) o `nos-mentes` tinha só o RETRATO jogável — 92 commits no histórico completo, **nenhum deles jamais tocou um `.ts`**, nem `engine/`, nem `site/`, nem `scripts/`; a `docs/clareira/` são 39 PNGs + 3 JSONs + 2 JS + 1 HTML + 1 CSS, e a `docs/miragem/` são 3 HTML. Ou seja: o código-fonte d'O Coração — a engine TypeScript com schema/validador/gerador procedural, o cliente, o tick, o `tools/art-mcp` de pixel art, os sprites de origem, os 6 agents da era 2D — existia num lugar só, **as cinco branches antigas do `nos`**, que estavam prestes a ser apagadas. **Feito:** as cinco foram empurradas com histórico COMPLETO para o `nos-mentes` como `era-2d/colaborador4` (333 commits), `era-2d/vila-ruinas` (281), `era-2d/tree3d-leaves` (200), `era-2d/git-power-tools` (193) e `era-2d/time-review` (185) — SHAs conferidos um a um contra a origem. Foram as CINCO, não só a mais completa: são variantes e guardar todas custa zero. Detalhe operacional que quase truncou tudo: o clone de trabalho era RASO (`--depth 1`); empurrar dali mandaria histórico cortado, então foi preciso `fetch --unshallow` antes. Depois disso o `nos` ficou com 1 branch (`main`) — as outras 19 apagadas pelo ideador, porque o proxy de git desta sessão devolve **403 em qualquer deleção de ref** e as ferramentas do GitHub aqui têm `create_branch` mas não têm delete. **Lição de método, e é a mesma da D-138:** o `CLAUDE.md` afirmava que o 2D "vive no histórico do git" e a afirmação era FALSA havia meses — ninguém tinha conferido porque ninguém tinha precisado. Documento que promete preservação só vale se alguém já tentou recuperar. Frase corrigida para apontar onde o 2D realmente está.
- **D-140** · 2026-07-28 · **A Fase 4 já tinha sido rodada numa branch parada; resgatada — e com ela uma classe de defeito que nenhum gate vê: o NOME que mente.** Ao varrer branches antes de uma limpeza, `wip/fase4-drone-inspecao-refino` (3 commits, 27/07) tinha a corrida de criação + refino da Fase 4 já feita e nunca mesclada. A peça `drone-inspecao.js` vem pra `main` como **espécime, veredito PARCIAL** — não como peça publicada. Tecnicamente sã: 92→93 PASSOS, 20 origens estruturais, 22 partes nomeadas, 228V/174F, **0 órfãos e 0 referências literais a id**, replay provado. O refino editou a peça existente sem regenerar (pás +41%, hubs −29%, câmera avançada, pouso rebaixado), preservando origens, nomes e parâmetros — que é o critério de saída da Fase 4. PARCIAL porque `npm run auditar -- drone-inspecao` REPROVA: o `detector-de-banding` acusa faixa chapada e o `distancia-paleta` acusa desvio sutil. Isso fica ESCRITO no cabeçalho da peça — uma peça que reprova no gate e mora na `main` só é aceitável enquanto o motivo estiver dito; sem isso, vira ruído que ensina a ignorar o auditor. **O achado que justifica a decisão está no commit do MEIO da branch, não na peça:** a 1ª versão declarava `['parte', {nome:'rotoresDianteiros', sel:{grupo:'palaDianteiroDireitoA'}}]` — um nome agregado, no plural, que resolvia **UMA pá** —, e o alias `detalhesLaranja` pegava só a face `topo` de cada cubo em vez do cubo. Os nomes MENTIAM sobre o que continham, e **nenhum gate viu**: zero órfãos, tudo verde. Só apareceu porque alguém foi contar à mão quantas faces cada nome resolvia (de "6 termos" para "56 seletores"). **Classe de defeito nova, registrada:** todos os gates de hoje checam se a referência EXISTE; nenhum checa se o nome SIGNIFICA o que diz. É a mesma família do achado do `tudo` na D-139 (ligação tardia, alcance mudando em silêncio) — falha por semântica, não por integridade. Não se constrói gate agora, por método: a Fase 4 é quem mede se isso acontece de novo, e falha observada escolhe o trabalho. Corrigido também o cabeçalho da peça, que sobreviveu ao conserto ainda mandando usar `grupo:'rotoresDianteiros'` — os agregados são ALIASES, não `parte`. **Confirmação independente:** a corrida achou sozinha, em 27/07, o mesmo buraco que a Fase 3.5 achou em 28/07 — "o motor não publica `origemId` para `cilindro`" é o mesmo defeito que o `lathe` sem `sel.origem` (D-139). Duas corridas cegas uma à outra apontando o mesmo lugar é o sinal mais forte que a fase produziu de qual é o próximo trabalho. **Varredura das demais branches, para a limpeza:** `wip/moto-proveniencia-loft` é a branch que a D-132 já decidiu não mesclar; onze branches `wip/` estão a 0 commits da `main` (já mescladas); cinco branches grandes (`colaborador4`, `claude/vila-ruinas`, `claude/tree3d-leaves`, `claude/git-power-tools`, `claude/time-review-12r780`, de 18–19/07) são da era do 2D, removida fisicamente na D-111 — nada aproveitável pro Atelier, e o histórico delas continua no git.
- **D-139** · 2026-07-28 · **Fase 3.5 concluída — as três lacunas de endereçamento fechadas, e a medição apontou o próximo eixo.** A fase nasceu de uma MEDIÇÃO sobre `pecas/moto.js`: 6.512 ids escritos à mão, **98% deles dentro de progressões aritméticas de ≥4 termos**, com os passos batendo exatamente nos valores de `TOPO` da peça. A causa não era desleixo — o `loft` emite face com `id = base + faixa*lados + lado` (uma grade 2D achatada num índice), a IA conhecia a grade e a única forma de dizer o que queria era fazer a aritmética na mão. Decomposto: **(a) 77,6%** sem forma de dizer "tudo" (só `espelha` aceitava seleção ausente); **(b) 18,6%** sem forma de dizer "alternado" (o gate `detector-de-banding` exige tom alternado); **(c) 3,8%** grade de dois eixos com `sel.origem` endereçando um só (`faixa` obrigatória). As Fases 2/3 **não podiam** revelar isso: suas fixtures testam geradores isolados com poucas faixas, e as três causas só doem em peça real com atributos por cima. **Rodada A** — `faixa` (loft) e `face` (cubo) viram OPCIONAIS, com a semântica que `lado` já tinha (ausente = todos): abre `{lado:3}` = a COLUNA e `{}` = a origem inteira, sem sintaxe nova. Aditivo por construção — `faixa` ausente GRITAVA antes. **Rodada B** — `sel:{tudo:true}`, num ponto único (`resolverSelecao`), herdado por toda op que aceita `sel`. **Rejeitada explicitamente a alternativa "seleção ausente = tudo"** (que o `espelha` já faz): ela destruiria o fail-closed, porque um erro de digitação (`fases:` no lugar de `faces:`) passaria a pintar a peça inteira em silêncio. Só o literal `true` é aceito; `sel` ausente continua gritando. Não se implementou "o resto"/`exceto` — os PASSOS já resolvem por ordem (pinta tudo, repinta as exceções depois). **Rodada C** — cada eixo numérico de `sel.origem` aceita um FILTRO DE PROGRESSÃO `{passo,fase}` (`k % passo === fase`); `{passo:2}` sem `fase` GRITA, não assume 0. Escolhido sobre um `alternado:true` porque a progressão é o mesmo mecanismo que a IA já executava na mão. **Armadilha registrada:** paridade sobre índice só vale onde a conectividade é regular — é o caso dos eixos de `sel.origem`, e só ali; não estender a `sel.f` nem a ids globais. **Prova Zero** (`tools/bancadas/gabarito-selecao.mjs`, no CI): constrói todas as peças com `PASSOS` e compara o neutro canônico byte a byte contra gabarito gravado ANTES da primeira mudança de núcleo — 15/15 idênticas nas três rodadas. **Achados da revisão adversarial (Rodada B, obrigatória por mexer em formato salvo):** nenhum furo de silêncio — a única entrada nova aceita é `{tudo:true}`, confirmado por ataque com `false/0/1/''/'sim'/{}/[]/NaN/null/undefined`. Mas dois achados valiosos: **(1)** `tudo` é seletor de LIGAÇÃO TARDIA — resolve no momento do PASSO, não na peça final; inserir geometria ANTES alarga o alcance dele com ZERO órfão, e é assim que a IA edita peça. Correto (é a regra de `sel` ausente), mas o nome sugere o contrário e `sel` é formato salvo, logo irreversível: travado por dois testes e documentado. **(2)** A Prova Zero é CEGA a esse afrouxamento — o revisor implementou a alternativa rejeitada e o gabarito passou verde; nenhuma peça publicada usa op de face com `sel` ausente, então ele não pode ver a diferença. **O teste-trava é a única guarda dessa decisão — não apagar confiando no gabarito.** **Rodada D, a medição com agente limpo** (o resultado é o produto, não a peça): `_galho.js` (loft) **160 → 0 ids**, byte-idêntico; `caixa-ferramentas.js`, peça NOVA criada do zero, **0 ids**; `_torno.js` (lathe) 300 → 192, travado. **O que a medição revelou, e é mais importante que o que ela confirmou:** o agente que criou a peça nova não escreveu id nenhum, mas travou em duas coisas de outra natureza — **(i)** o bloco de ids não é escolhido, é `posição-do-passo × 1000`; ele numerou por tipo de peça (corpo=1000, tampa=2000), levou **234 órfãos em cascata** e teve que recontar a posição de cada primitiva à mão, de novo a cada passo inserido antes; **(ii)** não existe posicionamento RELATIVO ("encosta a tampa no topo do corpo") — toda primitiva nasce presa à origem e um campo de passo aceita um número ou UM nome de PARAM, nunca uma expressão entre dois. Isso é o eixo de EXPRESSAR, e a medição diz que ele é maior que o de ENDEREÇAR que esta fase fechou. **Três lacunas menores confirmadas:** `lathe` não registra `sel.origem` (só `loft`/`cubo`), embora seja a mesma família de gerador; não há INTERVALO de faixa (`{de,ate}`), só paridade — é por isso que o `_galho` foi de 4 linhas de `pincel` para 12; e `pesar` é a única op que ainda não aceita `sel`. Nenhuma dessas vira trabalho por catálogo: entram na fila pela falha observada, com a Fase 4 escolhendo a ordem.
- **D-138** · 2026-07-28 · **Documentação repartida em três tempos verbais — `uso/`, `rumo/`, `historico/` — com gate de referência.** O `docs/` tinha 25 arquivos numa pasta só, misturando o que vale hoje, o que se pretende e o que já se decidiu; o `oficina.md` sozinho tinha 2.001 linhas e abria com "Nada disso está construído ainda" sobre um sistema construído. Critério da separação: **`uso/`** é o que a IA precisa saber pra USAR a ferramenta (verificável contra o código), **`rumo/`** é o que ainda não existe (nada citável como se existisse), **`historico/`** é imutável — consulta-se pra saber POR QUE algo é como é. O `oficina.md` foi repartido nos três destinos por status e continua como índice, porque 71 referências apontam pra ele. **A reorganização revelou a deriva que motivava ela:** 10 referências mortas (8 apontando pra `docs/legado/` desde a D-111, em silêncio), a op `pesar` implementada desde a D-97 sem linha na tabela de ops, e o `RECURSOS.md` — o índice declarado "de TUDO" — sem 11 dos 25 docs. **Trancado por ferramenta, não por vigilância:** `tools/mapa/links.mjs` (no CI) exige que todo caminho `docs/<...>.md` citado em arquivo rastreado resolva — zona VIVA exige caminho exato, zona HISTÓRICA aceita resolução por nome, porque reescrever caminho em histórico seria editar registro —, e exige o MANIFESTO: todo `.md` sob `docs/` citado pelo `RECURSOS.md`. `tools/mapa/fatiar.mjs` provou que nada se perdeu no corte, comparando por MULTISET (contagem), não por conjunto — comparação por conjunto não veria perder 9 de 10 repetições de uma linha. **Achado de método, registrado porque quase passou:** a regra geral "pasta citada cobre o conteúdo" foi escrita e QUEBROU DUAS VEZES, com o gate reportando "ok" nas duas — primeiro o padrão de pasta casava o prefixo de qualquer arquivo citado (`docs/rumo/PLANO.md` isentava `docs/rumo/` inteira), depois, já exigindo pasta sozinha, o `RECURSOS.md` usa `docs/uso/`, `docs/rumo/` e `docs/historico/` como TÍTULO de seção e as três se isentavam. Substituída por uma lista EXPLÍCITA (`PASTAS_BLOCO`), que não expande sozinha. Lição que vale além deste gate: **gate verde só é prova depois de plantado um defeito e visto ele ficar vermelho.**
- **D-137** · 2026-07-27 · Fixture de invalidação por `apagaFace` aprovada e Fase 3 concluída. Uma parte apagada deixa de resolver explicitamente: seu nome nunca aponta para vizinha, face posterior ou outro objeto; um nome composto falha inteiro se algum termo foi removido. O caminho destrutivo aceita somente seleção integralmente válida que resolve exatamente uma face. Não foi preciso criar histórico, lista de removidos ou resolvedor novo; `face:id` legado permanece inalterado. A sintaxe estrutural continua **experimental**. Próximo passo: Fase 4, uma peça média criada do zero por agente limpo, ainda sem moto nem interface.
- **D-136** · 2026-07-27 · Primeira fixture da Fase 3 aprovada: `espelha` pode criar uma origem estrutural nova, derivada de uma fonte estrutural direta, sem confundir original e cópia. A relação original→cópia é reconstruída no replay em registro efêmero, sem IDs globais escondidos; a saída estrutural é atômica, isto é, nasce inteira ou não nasce quando qualquer cópia esperada é inviável. O comportamento legado de `espelha` permanece inalterado. A sintaxe continua **experimental** e a Fase 3 segue em andamento. Próximo experimento único: invalidação explícita ao apagar uma parte nomeada, provando que seu nome não passa a apontar para outra parte. Ainda sem moto ou interface.
- **D-135** · 2026-07-26 · Fixture do cubo aprovada e Fase 2 concluída. Cubo e loft usam a mesma base de identidade: `origemId` único no objeto, contrato local reexecutável e aliases diretos ou compostos sem IDs globais escondidos. Declarações duplicadas são detectadas antes de qualquer PASSO, portanto a identidade já nasce ambígua e nenhum alias ou `sel.origem` a escolhe parcialmente; uma nova forma adere registrando seu contrato e publicando seus dados locais, sem alterar o resolvedor central. A sintaxe `ALIASES`/`sel.origem` permanece **experimental**, não é formato definitivo e só será avaliada novamente pelos testes da Fase 3. Próximo experimento: espelhamento, ainda sem moto ou interface.
- **D-134** · 2026-07-26 · Primeira fixture da Fase 2 aprovada, sem concluir a fase. A prova mínima em `loft` confirmou aliases diretos e multi-origem pela API pública: alcançam `executar(...)` e `colisaoDe(...)`, sobrevivem à inserção de geometria anterior e a transformação sem topologia, e não aceitam listas de IDs globais escondidas. A sintaxe experimental `ALIASES` continua restrita à fixture e não está aprovada como formato definitivo; a prova cobre somente `loft`. A Fase 2 segue em andamento: o próximo experimento é aplicar o mesmo modelo a uma primitiva simples, ainda sem moto, interface ou operações topológicas.
- **D-133** · 2026-07-26 · Fase 1 concluída: arquitetura híbrida como hipótese restrita da Fase 2. A identidade estrutural proposta é origem estável + coordenada local reexecutável; aliases são únicos no objeto, diretos, reexecutáveis e podem compor várias origens, sem armazenar IDs globais nem formar cadeias na primeira prova. O PASSO atual `parte`/`f.parte` permanece legado separado; transformação sem topologia preserva origem, enquanto operação topológica cria origem nova, declara relação derivada explicitamente ou invalida a referência — nunca herda identidade por coincidência de IDs. A sintaxe ilustrativa de `docs/arquitetura-identidade-estavel.md` NÃO está aprovada e nenhuma implementação ampla foi autorizada. Próximo passo único: a fixture mínima daquele documento, com dois lofts, alias direto e multi-origem, inserção anterior, transformação sem topologia, duplicatas e invalidação explícita.
- **D-132** · 2026-07-26 · Encerrar a moto antiga e desenhar a identidade estável antes de nova implementação. A moto em `main` fica congelada como espécime histórico e teste de regressão: ela provou criação nativa, revelou o teto de refinamento e não será mais migrada nem refinada. A tentativa mínima de proveniência local resolveu somente 12 dos 6.374 IDs literais e não representa as demais listas; por isso a branch que remove apenas esses 12 IDs não será mesclada, e migrações específicas da moto são interrompidas. [`PLANO.md`](PLANO.md) passa a ser a sequência vigente: antes de implementar, desenhar objetos, origens, partes, subpartes, nomes e composição de seleções estáveis; depois validar a arquitetura em fixtures pequenas e peças novas. Isso evita converter a moto em regra de produto e preserva a compatibilidade legada como restrição, não como arquitetura nova.
- **D-131** · 2026-07-26 · Proveniência local de `loft` como fixture mínima de formato salvo — `sel:{origem:{op:'loft',id,faixa,lado?}}` seleciona somente faces LATERAIS, com `faixa` e `lado` zero-based. Um `loft` que a queira declara `origemId` inteiro estável; `nucleo` reconstrói `st.origens` a cada replay, portanto a informação não toca a malha canônica nem muda PASSOS antigos. A identidade posicional `id` do passo continua sendo o bloco de IDs; a separação é obrigatória porque inserir geometria antes desloca esse bloco. Fixture medida: literal `[8..11]` e origem da faixa 2 são byte-idênticos; ao inserir um cubo antes, a origem ainda pinta `[1008..1011]`, enquanto o literal antigo grita até ser recalculado. Chaves desconhecidas, origem inexistente/ambígua, faixa/lado fora do limite e vazio gritam. Limite deliberado: só `loft`, sem tampas; a generalização para outras primitivas exige provar qual coordenada local é estável. Revisão adversarial solo após gates.

- **D-130** · 2026-07-25 · TETO, 3ª corrida: refinar a moto existente por significado, sem regenerá-la — preservados os 58 passos da 2ª corrida; só quatro dimensões existentes mudaram (punhos do garfo/braço e seção do para-lama) e 11 passos foram anexados para o par de painéis entre garfo e corpo e a crista contínua rabeta–tanque. A seleção semântica entrou onde representa a peça: `painelDianteiro` (60 faces) e `cristaTanqueRabeta` (48) são nomeados uma vez por seleção literal exata, depois material/pintura/liso usam `sel.grupo` em seis atribuições. Passe adversarial do próprio enunciado: a caixa candidata ao espelho selecionava 40 faces — 30 do painel + 10 antigas (`2122`, `2123`, `5003`, `20007`, `21030..21035`) — e foi rejeitada; espelho fica literal, porque economizar IDs ali deformaria o significado. Medido: 1376/1492 → 1470/1600 V/F, caixa e colisão inalteradas, simetria x segue 0 sem par, 0 órfãos, `criar` aprovado (seis críticos limpos; porteiro 397 cores, dominante 49%, luma 249). Relatório em `docs/teto-moto-refino-3-relatorio.md`; forma permanece JULGAMENTO DO IDEADOR sem gabarito.

## Decisões ativas — em detalhe (era v3, D-55→)

- **D-55 · 2026-07-17 · Rumo v2/v3 + A OFICINA (ambiente padrão de criação).** Decisões do ideador: (a) branch **`v2`** criada preservando o jogo atual (CPU raycaster) — rollback/manutenção; a **`main` desenvolve o v3 (GPU)**, publicado em paralelo em `/fps/v3/` até a paridade (o `/fps/` v2 segue no ar durante o port). (b) **A OFICINA**: espaço isolado e PADRONIZADO pra criar objetos/texturas/animações (sons/reflexos na fila) em `prototipos/procedural/v3/` — `motor/` (mat4/tex/geo/render: sol+sombra PCF, luz de céu, névoa, partículas, grama, ?res) + `pecas/` (contrato: `meta` + `construir(ctx) -> {lotes, animar?}`; animação por matriz/lote) + `visor.html` (?peca=nome). REGRA DE OURO: o visor usa o MESMO motor que o cliente v3 — o que fica bom na oficina fica bom no jogo. Peças: `casa-toras` (migrada do gpu-beauty, idêntica) e `_modelo` (template: cubo texturizado girando/flutuando — prova o contrato inteiro). Bancada `npm run peca -- <nome>` (olhar-peca.mjs: server estático p/ ES modules + 3 ângulos). Limites honestos no README do v3: sem som/reflexo/câmera andável no visor ainda; o port do mundo é o trabalho que segue.
- **D-56 · 2026-07-17 · O plano da potência: objetivo final + arsenal de ferramentas (docs/FERRAMENTAS.md).** Pedido do ideador ("ferramentas a níveis alienígenas" + "precisamos de um objetivo final"). Varredura por 5 agentes paralelos sobre o arsenal real (bancadas, arte, motor v3, som, fluxo) → ~60 propostas, sintetizadas em ~30 itens com prioridade. **Objetivo final: "da frase ao mundo no ar em uma sessão — sem passo cego e sem regressão"** (a Oficina vira A Fábrica — lore aplicada ao próprio desenvolvimento). Potência definida em 4 eixos: ciclo em segundos, visão total, alavancagem 10×, gates automáticos. Ordem de construção travada: (1) porteiro-visual+diff, (2) bancada-viva+contato+filme, (3) porta-mundo+cena+câmera, (4) perf-gpu+orçamento+paridade, (5) materiais+paleta-estendida. O plano vive em docs/FERRAMENTAS.md; cada item vira rodada na vez dele.
- **D-57 · 2026-07-18 · O gargalo é o briefing, não a ferramenta + escopo do v3 travado (linguagem + prune).** Debate curto com o ideador que reposicionou tudo. **(0) A virada de chave:** ele intuía uma ferramenta que "automatizasse muito" meu trabalho e não achava a palavra; a palavra que ele mesmo achou foi **"nós combinarmos informações"**. Conclusão aceita pelos dois: **o gargalo nunca foram minhas mãos (as ferramentas) — é a informação/briefing entre nós.** Informação rica + ferramenta mediana > ferramenta divina + informação pobre (é o que o MCP-pro-Blender dele ensina na marra: MCP tira teto, não esculpe; "bom pra IA" ≈ bom pra artesão humano — brief claro, referência, restrição, jeito de ver). **Metade do trato é minha:** eu parava de pensar no 1º substantivo (disse "casa" → fabriquei cabana de toras por 6 rodadas sem NUNCA perguntar *que arquitetura o NÓS tem* — a cabana caiu direto no furo de informação). **Trato firmado:** ele fala mais claro; **eu extraio o brief ANTES de construir qualquer coisa (nem que seja 3 perguntas), não corro com o palpite.** E respostas mais diretas a partir daqui. **(a) Linguagem das estruturas do NÓS = sci-fi decaído: concreto tomado por mato, ruína de tecnologia antiga sobre a natureza.** A natureza cartoon da v2 já tinha estilo; as estruturas eram "bloco de Wolfenstein" porque ninguém nunca definiu o idioma delas — agora tem. Toda estrutura v3 mira esse alvo (a cabana de toras foi teste de motor, NÃO é o tema). **(b) Prune de escopo do v3** (o ideador: "fui ambicioso pensando que teriam mais pessoas e mais mundos além de mim"). Como o v3 é port do zero, "remover" é limpo: **não apago nada do v2 (fica congelado como registro) — só NÃO porto.** FICA: mundo explorável/construível + **economia** (é a única coisa transferível entre mundos, então o encanamento dela fica junto — **₱**, **ledger** = arquivo de saldos+transações que torna o ₱ real, **identidade** = conta GitHub do jogador = "nome de personagem", o outro transferível). SAI do v3: outros NPCs, criaturas no mapa (animais entram DEPOIS, não agora), bancadas de craft no mundo, e a **narrativa** (história do Coração, ficção de federação/muitos-mundos) — cortar narrativa ≠ cortar o encanamento econômico. **(c) Um único NPC: o mago-guia.** Jogador conversa pra tirar dúvida; a gente "dá conhecimento" a ele. Implementação decidida: **base de conhecimento git-native (grátis, sem cota, determinística)** — pergunta via issue → Action busca/responde; cobre "como jogo?/onde fica X?/lore". LLM ao vivo (GitHub Models, grátis-com-limite, chamável de Action) fica como fallback OPCIONAL futuro — **não dependemos de cota**. Esclarecido pro ideador: **Copilot é assistente de código (pago, no editor), não serve de NPC; e "a IA que usamos" são DUAS** — eu (Claude), o construtor, rodo fora do jogo; a IA do mago seria outra, em runtime. **(d) IA ambiente falando de tempos em tempos: documentar que é possível + deixar exemplo, mas NÃO ligar** (só o mago). Escopo do v3, martelo batido: **mundo sci-fi decaído + mago-guia + economia (₱/ledger/identidade); sem outros NPCs, sem criaturas, sem craft, sem narrativa.**
- **D-58 · 2026-07-18 · O chão v3 nasce (ilha-chao) + o bug do hash2 — primeira vitória do senso crítico [cpu].** Port do chão decidido em debate: **só chão por ora**; ilha flutuante mantida; **sem carreiro**; a borda da v2 "parecia margem de rio" → o conserto é a ilha ser um SÓLIDO com queda visível (vão de ar até o mar de nuvens = altura); **sem a parte de baixo** (pedido: o jogador nunca a vê — fica só uma fita de terra de 2.4u pra não ler papel); ilha NA ESCALA V2 (grade 64×64 → R=28, ~56u de diâmetro). Peça `pecas/ilha-chao.js`: grama 64px/unidade (receita genGrassTile: manchas+pinceladas+touceiras+flores, acalmada pra não virar confete a distância), lago com PRAIA de areia, fita de terra com normal inclinada pro céu (não vira breu na sombra), mar de nuvens 4 tons. Motor ganhou opções de PAISAGEM no contrato: `palco:false` (a peça É o chão), `particulas:false` (pólen em paisagem lia como enxame), `fog:[início,alcance]` e `far` próprios, `camera:{e,r}` sugerida (a bancada só passa e/r quando pedidos). **O BUG DOURADO: `hash2` do tex.js usava `>>` (shift com sinal) + multiply estourando float — TODO ruído do v3 saía espremido em [0,0.5)** (nuvem 100% numa cor só, dither pobre — provável causa do "dither hachurado" da madeira em D-54f). Achado MEDINDO, não olhando: histograma da textura em Node (100% cor 46) → estatística do fbm (máx 0.44) → fix com `Math.imul` + `>>>` → faixa cheia [0.11,0.86]. É a tese do D-56 provada: o screenshot enganava ("deve ser o fog"), o número não. Segunda pegada medida: o fog padrão ESMAGAVA o mar de nuvens (A/B com fog recuado provou); terceira: nuvem com repeat de 73u caía inteira dentro de um blob (→24u). No ar em `/fps/v3/visor.html?peca=ilha-chao`. Limites honestos: água/nuvem paradas (animação e reflexo virão), grama ainda meio salpicada ao longe, pontinhos pretos de aliasing na borda distante.
- **D-59 · 2026-07-18 · Port das ÁRVORES da V2 pro v3 (growTree extraído fiel) + 2 consertos de motor.** O ideador: "vamos portar árvores, pega uma imagem da V2 como base". Base = `carreiro` (npm run olhar): copa fofa em camadas (sombra teal→topo amarelo-verde, estilo BotW), contorno de tinta, tronco com casca+raiz, franja de grama. Port: o `growTree` (L-system, ~415 linhas) + dependências (SPECIES/ramps, mulberry32, **hash2/vnoise DA V2**, BAYER, clamp) **extraídos LITERALMENTE** de nos-fps.html pra `motor/arvore.js` via script (scratchpad/extrai-arvore.mjs), NÃO redigitados. Decisão-chave: manter a hash2 buggy da v2 (meia-faixa) LOCAL ao módulo — o gerador foi afinado com ela; a hash2 nova (D-58) mudaria a distribuição e fugiria da referência. Peça `arvore.js`: cada árvore = **cruz de 2 billboards** perpendiculares (volume de qualquer ângulo ao andar em volta, não gira chapada), normal pra CIMA (luz uniforme preserva o sombreado BAKED do sprite). 3 espécies provadas (carvalho/florida/bétula), paletas certas (cerejeira rosa, etc.). **2 bugs de motor consertados no caminho:** (1) **sombra retangular** — o passe de profundidade ignorava alfa, então sprite recortado jogava sombra de CAIXA; agora o depthProg amostra a textura e faz `discard` no alfa (cutout shadows pra toda folhagem futura). (2) **textura PRETA** — árvore é 166×218 = NPOT, e WebGL1 com REPEAT em NPOT rende preto; `glTex` agora auto-CLAMP em NPOT (billboard usa UV 0..1, não precisa REPEAT). No ar em `/fps/v3/visor.html?peca=arvore`. Próximos: frames de vento (growTree aceita windAmt -1/0/1), plantar árvores na ilha-chao, LOD/impostor pra floresta densa.
- **D-60 · 2026-07-18 · Ferramentas de senso crítico [cpu] CONSTRUÍDAS e MEDIDAS por benchmark (o pedido: "descobrir quais ajudam, iterar, listar o que não ajuda").** Aplicação da tese do D-56 às próprias ferramentas: "será que ajuda?" virou NÚMERO. **Método:** benchmark = peças reais (arvore3d, ilha-chao, casa-toras) × defeitos PLANTADOS (18 mutações em 5 domínios) + controles limpos → cada ferramenta pontuada por pegar o defeito do seu domínio (recall) sem alarme falso em limpo/outro-domínio (precision) = F1. **Base técnica-chave:** `sandbox.mjs` roda o `construir()` das peças em NODE PURO (canvas-stub cobre texCanvas/bufToCanvas) — malhas + pixels de textura sem browser, em ms, determinístico e paralelizável. **Construção:** lint-de-malha + porteiro inline (eu); as 4 de textura (distancia-paleta, detector-de-seam, detector-de-banding, contador-de-pixels-orfaos) por WORKFLOW de 4 agentes em worktrees isolados, cada um iterando contra o benchmark. **Resultado — todas gateiam o defeito REAL: NÚCLEO F1=1.00, ZERO falso-alarme nas 4 peças de arte de verdade.** distancia-paleta usa CIEDE2000 puro + allowlist da madeira aprovada (D-54f, lida de casa-toras.js); detector-de-seam distingue costura injetada (borda chapada + salto) de textura-que-só-não-ladrilha (billboard/água) sem metadado; etc. **Round ADVERSARIAL (versão sutil de cada defeito) revelou o PISO honesto:** seam/banding perdem o sutil (0/3), paleta perde desvio de 1-tom (a fronteira paleta↔banding numa faixa chapada off-palette é REAL — forçar a paleta a pegar isso a faz morder o banding; documentado como piso, não re-tunado), órfãos dá 1 FP sob ruído pesado. lint-de-malha é exato (—). **Nenhuma foi dud** → a "listinha do que não ajuda" virou "limites honestos por ferramenta" (onde NÃO confiar). **Empacotado (2º pedido):** `npm run auditar <peca>` (gate [cpu] numa peça real, exit≠0 em achado) + `npm run porteiro <peca>` (gate de render: pageerror/__ready/frame degenerado, decoder PNG próprio via zlib) + `npm run bench` (placar núcleo/adversarial). Viraram a **skill `/auditar-peca`** (`.claude/skills/`) com a tabela de pisos — o senso crítico entra no meu loop antes de commitar peça, com a regra "todo julgamento cita ≥1 número". Prova viva da tese: um `[cpu]` (não o olho) pegou o bug do `hash2` no D-58.
- **D-61 · 2026-07-18 · Resolução de textura MEDIDA (32/64/128px) + o alicerce jogável do v3 (câmera livre, som, tiers de gráfico, menu de pausa/configurações).** Começou como dúvida do ideador ("se fizermos 32/64/128/256/512px como presets, pesaria demais?") e virou dois entregáveis. **(a) O experimento (mesmo método do D-44, agora no v3):** a MESMA árvore (`arvore3d`) renderizada com textura em 32/64/128/256/512px, câmera idêntica, no render 640×360 (o alvo real, D-45) — inclusive colada no tronco (pior caso). **Resultado: 32→64px melhora claramente; 64→128px é refinamento pequeno mas real; 128→256→512px são PIXEL A PIXEL IDÊNTICAS** — o buffer não tem informação de tela pra mostrar o excedente. Teto confirmado ≈128px, batendo com o D-44 (engine diferente, mesma física). **Decisão de produto do ideador: presets Baixo=32/Médio=64/Alto=128** — 256/512 descartados por dado, não palpite. **(b) O pedido cresceu**: o ideador queria os MESMOS 3 níveis pra iluminação/sombra/partículas + volume de ambiente/passos, dentro de um menu de pausa com "continuar/configurações", sub-janela com voltar/✕ e 4 abas (som padrão/gráficos/controles/idioma). Como NENHUM desses sistemas existia no v3 (o motor só tinha câmera em ÓRBITA, sem input, sem som, sem tiers de luz/sombra/partícula — só a Oficina, um visor de peça isolada), a pergunta de escopo foi feita ANTES de construir (D-57): v2 (que já tem tudo isso, D-49) ou v3 (que exigia construir o alicerce primeiro)? **O ideador escolheu v3** — assumindo o escopo maior. **O que foi construído:** `motor/render.js` ganhou câmera LIVRE (`setCam(pos,yaw,pitch)`, com hook `antesDoQuadro(dt,T)` por quadro) ao lado da órbita existente, e 3 tiers reais (não cosméticos): **sombra** 0=desligada (pula os draw calls do passe de profundidade, ganho de fps medido e real: ~43fps vs ~20 Médio vs ~10 Alto/2048px no software daqui — Alto anotado como PESADO, tratar como experimental até medir em aparelho de verdade) /1=1024(atual)/2=2048; **luz** 0=só ambiente (zera o termo direcional, visivelmente mais chapado — confirmado por screenshot) /1=sol+sombra(atual)/2=+rebote falso barato (`amb += ground·bounce·max(0,-N.y)`, sem GI de verdade); **partículas** = contagem direta (80/320/800, densidade visivelmente diferente). `motor/input.js` (novo): WASD+pointer-lock no desktop, joystick touch **portando FIEL as 3 correções pagas caro na v2** (D-47/48/49: dono vivo não é roubado, dono fantasma é destronado por toque novo, watchdog por quadro, rede touchend/cancel) — reinventar teria repetido os mesmos bugs. `motor/som.js` (novo): porta o D-40/41 (vento constante + água por proximidade da lagoa) e SINTETIZA passos (thump grave filtrado, jitter por passo, cadência por movimento) — dois volumes independentes, mudos em 0. `pecas/arvore3d.js` ganhou `ctx.TS` (reaproveitando a convenção que `casa-toras.js` JÁ usava — GT=16·TS, em vez de inventar um campo paralelo) e `ctx.seed` (a peça era 100% determinística; sem seed, "plantar" só clonava a mesma árvore). `render.js#carregar()` ganhou DEDUPE de mesh/textura por referência — plantar a mesma árvore em N posições sobe UMA textura pra GPU, não N (a arquitetura de "pool de variantes" do próprio experimento de resolução, aplicada). **`jogo.html`** (novo): ilha-chao + 12 árvores plantadas (4 variantes por seed) — placeholder EXPLÍCITO, não o "plantar árvores" definitivo (densidade/espécie ainda em aberto) — com o jogador andando de verdade (sem colisão real, só um raio-guarda MAXR=25 pra não sair da ilha) e o menu completo. **QA real pegou 2 bugs**, não hipotéticos: (1) o spawn padrão nasceu quase DENTRO de uma árvore (não chequei a lista de plantio contra o próprio spawn) — corrigido; (2) `.cantoBtn` (botões voltar/✕) perdiam pra `.painel > button` na especificidade CSS (1 classe+1 elemento > 1 classe só) e viravam uma barra larga sobreposta — corrigido subindo a especificidade (`.painel .cantoBtn`). Screenshot ANTES/DEPOIS confirmou os dois. **Bancada nova `npm run jogar`** (`tools/bancadas/jogar.mjs`, irmã do `olhar-peca`): screenshot de `jogo.html` com overrides de câmera (`?cam=x,z,yaw,pitch`) e tier (`?ts=&sombra=&luz=&particulas=`) — sem isso, os 2 bugs acima seriam invisíveis até alguém jogar de verdade. **Limites honestos**: sem colisão real (D-53 não portado); `ilha-chao.js` não respeita `ctx.TS` ainda (só a árvore); sombra Alto pesada no software; o scatter de árvores é placeholder. `jogo.html` NÃO substitui `/fps/` (v2) — é o primeiro alicerce do v3 ficar jogável, não o cliente pronto.
- **D-62 · 2026-07-18 · Vila-ruína transplantada da tree3d-leaves pra v2 (main) + árvores 3D; NPCs/máquinas/monólitos fora; arco/portal SEM gambiarra de profundidade + peça `arco` v3 de geometria real.** Sessão de convergência na **main** (o ideador quer tudo unificado ali): ele apagou as branches `v2`/`v1`/`cidade-coracao` depois que confirmei que a main já carrega a v2 mais atual (o `nos-fps.html` da main é byte-a-byte o da branch `v2`). **(a) O transplante da vila.** A branch `claude/tree3d-leaves` tinha ruínas e árvores mais bonitas que a v2 da main, mas sem chafariz. Plano (evoluiu por vários "tenho ideia melhor" do ideador, cada um enxugando): na vila da main **manter chafariz + arco + portal**, **remover as oficinas velhas + Santuário + a casa-protótipo (D-53)**, e — dos elementos da tree3d-leaves — **NÃO trazer** a Torre da Crônica nem a sujeira espalhada fora da vila. A ideia inicial de trazer **monólitos + A Cabeça enterrada** foi revista: os monólitos liam **chapados/espichados** ("tem monólito no meio do carreiro e dentro do arco") → decisão final **remover os monólitos por completo** (CITY vazia) e **tirar todos os NPCs** (Habitantes/`falas.json`, as **máquinas** = billboards de oficina, e os **Nativos**) — o mundo fica sci-fi decaído sem povo por ora (o mago-guia do D-57 entra depois). Execução por **par de agentes opus (coder + revisor) em worktree isolado** numa branch-cópia da main, pro ideador seguir trabalhando comigo na main em paralelo; o revisor confirmou **zero regressão** (som/menu/joystick/otimização/segmento intactos) antes do merge. **(b) A gambiarra de profundidade REMOVIDA.** O arco e o portal da v2 fingiam volume com `b.depth` (D-50): até **40 fatias** de billboard empilhadas por coluna = overdraw puro ("isso pesa no processamento de verdade", o ideador flagrou). Antes de mexer na v2, **reconstruí o arco como GEOMETRIA REAL no v3** (peça `pecas/arco.js`, no ar em `visor.html?peca=arco`): pilares = **caixas de 4 faces** de verdade (`caixaUV`, com plinto+capitel), arco abatido = **faixa extrudada** com frente/trás/intradorso/extradorso reais (nenhuma fatia), UV **ancorado ao tamanho da face** (não estica em face alta — lição do monólito), com dois consertos achados sob os desenhos do ideador: a **fresta triangular no arranque** (o intradorso agora é RETO sobre o pilar de `Ain→Aout` e só sobe no vão) e o **sinal x da normal do intradorso** invertido. Aí, na v2, **removi o `depth` do arco e do portal** → viraram billboards orientados limpos, sem gambiarra (foto ANTES/DEPOIS confirmou a silhueta preservada). O **lado escuro** que sobra numa face de profundidade do v3 é a **iluminação hemisférica** (faces pra baixo não pegam sol + pegam o ambiente escuro do chão), não bug de textura — o ideador viu e aceitou ("deixa assim"); o v3 ainda não tem GI/rebote (o v2 fingia com GI assada, D-36). **Limites honestos:** a peça `arco` v3 está pronta mas **ainda não integrada a nenhum mundo**; o portal pode ganhar o mesmo tratamento de caixa real do v3 quando for a vez; a "casca de 4 planos" tentada na v2 pra tapar o lado do arco **falhou** (o raycaster da v2 só desenha billboards chapados + paredes, não caixas — revertida) e virou a lição registrada: **caixa de verdade é no v3**, a v2 fica com billboard sem profundidade falsa.
- **D-62b · 2026-07-18 · Copa das árvores da v2 SEM CORTE — a "folhagem cortada" era buffer estreito, não resolução (MEDIDO).** O ideador: "tem um monte de árvore na v2 com folhagem cortada, não sei se uma é cópia da outra e todas saíram erradas". **Debatido com número antes de tocar (D-56).** A árvore da v2 é `growTree3D`+`renderTreeView3D` (`tree3d-core.js`, transplante da tree3d-leaves), não o `growTree` 2D (esse hoje só alimenta arbustos). Medição no gerador REAL (`scratchpad/medir-corte.mjs` — carrega o core num sandbox CJS porque o repo é `type:module` e `require(esm)` vinha vazio): 8 espécies × 8 vistas, render num buffer gigante pra achar a bbox verdadeira da copa e comparar com a janela fixa **166×218**. **Resultado que corrigiu minha hipótese (eu apostara em resolução):** `renderTreeView3D` centra a copa em `cx0=W/2` e o `put()` descarta o que passa de `W`; as **2 espécies mais largas — carvalho (+5/4px) e copado (+8/6px)** — estouravam os ±83px de meia-largura e saíam **fatiadas num corte vertical reto** no lado da copa. As outras 6 cabiam. O topo **nunca** cortava (só os lados). **A intuição de "é cópia" do ideador estava literalmente certa:** existem só **6 sprites** de árvore (1 por espécie, seed fixa) reusados no mapa inteiro → as 2 bugadas repetem em ~**1/3** da floresta, idênticas ("um monte"). **Fix:** alargar o buffer **166→186** (`T3_W = round(104·TREE_RES)+20`; +10px de cada lado, cobre os 8px do copado com folga). **Mesmo `sizeMul`** → mesma nitidez; e como o billboard usa `sw = sh·(texW/texH)` com a árvore centrada, a margem transparente **não muda o tamanho nem a posição no mundo** (verificado no código: linhas 3393-3394 e 3332). Re-medido: **0 corte nas 8 espécies**; screenshot do `carreiro` confirma copas inteiras e arredondadas dos dois lados. Custo: +109KB por variante (8 vistas RGBA). É a tese do senso crítico de novo: o olho dizia "resolução", o número disse "buffer estreito".
- **D-63 · 2026-07-19 · Virada CARTOON das árvores do v3 + 3 recursos toon no motor + o carimbo plantável.** Sessão longa de iteração de arte com o ideador que **fixou a linguagem visual das árvores do v3** e deixou o motor com toon de verdade. **(0) O caminho:** partiu do defeito "folhagem cortada" (D-62b) e virou exploração de folhagem no mostruário `_arvformas` (5 formas: oval, larga, pinheiro, cerejeira, copada). O ideador foi puxando a textura pro LIMPO rodada a rodada — tirou o mosqueado repetido (grade 8×8 → cachos por hash + fase de UV por blob), tirou o amarelado (rampa só de verdes), reduziu tons até **cor única chapada**. Aí pediu "traços que acompanham a deformação"; primeira tentativa (escurecer os vales por UV.v) foi **revertida** — ele queria **curvas de cacho** de desenho ("‿"), não gradiente. Segunda: virei a copa em vários lóbulos separados — **errado** (ele quis dizer curvas seguindo os caroços da MESMA copa larga, não bolas soltas; revertido). Terceira, com uma **referência** de arbusto cartoon que ele mandou: o alvo ficou claro — **massa conectada bombada + contorno grosso uniforme + cel + brilhos**. **(1) O pinheiro (D-62/63):** cone liso → **saias empilhadas em escada** (silhueta clássica de conífera) + agulha; depois virou **verde-escuro chapado em faixas por nível** (topo claro/corpo/rebordo escuro) pra casar com o cartoon. Elenco de alturas em `_pinheiros` (mudinha/padrão/alto/espigão/largo). **(2) Os 3 recursos TOON no motor (`render.js`), todos POR-LOTE e gated (0 = peça não muda):** **(a) contorno de CASCA invertida** (`outline`): infla a malha na normal, pinta chapado, desenha só as faces de trás (cullFace FRONT) ANTES da cena → sobra só na silhueta = contorno firme e uniforme, ao contrário do rim fresnel (fino/some na luz) que existia; **(b) cor de contorno por-lote** (`outlineInk`): cerejeira ganha contorno ameixa, verdes verde-escuro; **(c) cel-shading** (`toon`): quantiza a luz do sol em 3 degraus duros. **Achado do ideador que guiou o cel:** um brilho PINTADO na textura furaria a lógica (apareceria de noite/no contraluz) — então o brilho tem que vir da LUZ. O cel resolve: a faixa clara É o brilho e segue o sol (`sunL` quantizado × `uSunCol`), some sozinho no escuro. **(3) A decisão de estilo:** duas linguagens (cartoon limpa vs mosqueada) não convivem na mesma floresta — o ideador escolheu **tudo cartoon**. Convertidas oval/larga/pinheiro/cerejeira/copada; o estilo mosqueado **guardado** como arte alternativa em `pecas/_arvformas-mosqueado.js` (abrível no visor). **(4) O PORT (o pedido "usar as árvores"):** `motor/arvore-cartoon.js` — `criarArvores(ctx)` monta as texturas UMA vez e devolve `construir(especie, seed)` com a árvore na ORIGEM; `pecas/arvore-cartoon.js` prova o carimbo plantando **36 instâncias de um pool de 10 variantes** (espécie×seed×porte×giro) por matriz, reusando malha/textura por referência (dedupe do `carregar` → só o pool sobe pra GPU). **Explicitamente NÃO integrado à ilha/jogo.html ainda** (pedido do ideador: "só não coloca na ilha em si ainda"). **Limites honestos:** o passe de contorno DOBRA os draw calls da árvore (~18fps pra 36 árvores no renderizador de software daqui; GPU real é outra história, mas floresta grande vai pedir LOD/impostor); `npm run auditar arvore-cartoon` acusa "banding" nas faixas chapadas do pinheiro — é o **limite honesto do detector-de-banding** (D-60), que não distingue faixa cel proposital de degradê preguiçoso: aqui é intencional, não defeito; o `_arvformas`/`_pinheiros` seguem scratch (mostruário), a peça de verdade é `arvore-cartoon`.
- **D-64 · 2026-07-19 · Vegetação cartoon plantável (arbusto + flor + tufo) + ritmo de vento POR-LOTE no motor (`windF`).** Extensão natural do D-63 pro rasteiro: `pecas/vegetacao-cartoon.js` planta arbustos (massa bombada estilo copa), flores (haste + corola chapada) e tufos de grama, tudo no mesmo cartoon (contorno + cel). No motor, o vento ganhou **ritmo por-lote** (`windF` multiplica a frequência): grama balança rápido, flor média, arbusto e árvore lentos (0.9) — a cena inteira respira em camadas em vez de todo mundo no mesmo metrônomo. Registrado em retrospecto (commits `d62f0ab`/`10aab6a`).
- **D-65 · 2026-07-19 · A bancada ganha OLHO DE FORMA + a regra: FORMA quem conclui é o ideador.** A sessão da árvore 'raiz' expôs um padrão ruim repetido: eu declarava "ficou bom" e o ideador via defeito — até ele nomear a causa: **"nossa ferramenta de modelação está péssima, você não consegue analisar e entender que isso está ruim… ela não corta os teus erros"** (e pediu: sem conclusões precipitadas). Diagnóstico: o screenshot texturizado de 640px em 3 ângulos **esconde defeito de FORMA** — a casca disfarça emenda, faceta e junção seca; eu julgava com o instrumento errado e errava pro otimista (a tese do D-56/D-60 de novo, agora pra geometria). Construído (aprovado a+b+c+d): **(a)** no motor, `uDebug` no FS da cena — modo 1 = **normais como cor** (`N*0.5+0.5`; superfícies que se encontram mal SALTAM de cor), modo 2 = **flat** (cinza + luz, sem textura; silhueta/volume nus); visor aceita `?debug=normais|flat`; **(b)** na bancada, `npm run peca -- X --geo=normais|flat --giro=N --res=1400` (o viewport acompanha `--res` → close nítido; `--giro` roda N ângulos, defeito que só aparece de um lado); **(c)** tudo documentado na skill `auditar-peca`; **(d)** a **regra de comportamento** na mesma skill: (1) NUNCA dizer sozinho "ficou bom/fechamos/resolvido" sobre forma; (2) rodar a visão de geometria ANTES de mostrar e **apontar os defeitos que eu vejo**; (3) entregar + listar o que ainda está ruim — o "bonito" é do ideador. **Vitória imediata, dupla:** a visão revelou que o garfo da 'raiz' é facetado/abrupto (tubos de 6 lados × loft de 24, salto de normal) — exatamente o que o ideador via e eu não; e matou em 1 render o experimento do loft de duas pontas (lia como VASO, não galhos) antes de eu vendê-lo como sucesso.
- **D-66 · 2026-07-19 · Elenco fecha com seca/frondosa/raiz — copa fundida por SDF, "pé vira tronco" e a forquilha em penumbra.** Sessão longa de arte com o ideador, 3 espécies novas no carimbo (`ESPECIES` = 8). **(1) 'seca'** (árvore morta): esqueleto recursivo `galhoSeca` — tubos hexagonais afunilados com parallel-transport (fase contínua, sem torção), tampas nas pontas, filhos EMBUTIDOS na ponta do pai com colar flarado (1.5) — a sobreposição esconde a junção galho↔galho; sem contorno cartoon (o "preto" que o ideador estranhou ERA a casca invertida — removida pra seca/raiz). **(2) 'frondosa'**: a seca + copa que SEGUE os galhos — pontas do terço de cima aglomeradas (greedy, min 0.64) em lóbulos **fundidos por SDF + smin** (`superficieSDF`, surface-nets com normais suaves recalculadas; K=0.14 fino = conexões "quase não conectadas", pedido dele), UV triplanar, verde CHAPADO (`VERDE_FLAT` — as curvas "‿" quebravam na superfície fundida, removidas a pedido) e contorno fino 0.03. **(3) 'raiz'** (ref. de cartoon com raízes entrando na terra): iterada com ele — ranhuras finas/escuras/RETAS só do tronco pra cima, paleta unificada tronco==raiz (`woodBody` 21/22, ranhura 20 marrom-escuro), sombra de aterramento na base. O problema central era a **DIVISÃO raiz↔tronco**; resposta = **"pé vira tronco"**: `troncoRaiz` é um loft ÚNICO `[y, raio, amplitudeDedo]` das pontas das raízes (y=-0.55) ao topo (1.45), perfil CÔNCAVO agressivo (abre rápido só perto do chão — curva, não cone; desenho do ideador), 5 dedos `cos(5a)`; textura `BARK_SECA` mapeada por ALTURA (`vAlt(y)`: sombra→raiz lisa→tronco ranhurado) **flui sem emenda porque é a mesma superfície**. Galhos = `galhoSeca` brotando do topo do loft (envolvem a ponta, `vAltMode` continua o V pela altura). **Caminhos REJEITADOS (não repetir):** fusão SDF da árvore inteira (ficou redonda/encaroçada + textura triplanar distorcida — "terrível"), teia na forquilha (z-fight dupla-face), colar 2.1 ("horrível"), loft de duas pontas (vira vaso, D-65). **A junção loft↔galhos que sobrou** é atacada por **jogo de cor**: faixa de PENUMBRA na `BARK_SECA` na altura da forquilha (v≈0.46–0.61, dither 20/21→corpo) — como loft E galhos compartilham o MESMO `vAlt`, os dois escurecem JUNTOS e a linha de interseção fica dentro de área escura contínua, onde o contraste some. **Limites honestos:** a penumbra mascara COR, não SILHUETA — na visão de geometria (D-65) o garfo segue facetado (6×24 lados, salto de normal) e há um nó duro onde os colares dos galhos se cruzam; o mostruário `arvore-cartoon` agora só empurra lote com malha (seca sem copa / raiz sem trunk deixavam lotes vazios); o banding do PINE segue acusado (by design, D-63).
- **D-67 · 2026-07-20 · A 'raiz' muda de abordagem: tronco separado + pé de elefante integrado NA GEOMETRIA (zero emenda de verdade).** Continuação direta do D-66: o ideador pediu **"jogo de cor e textura pra disfarçar"** a divisão raiz↔tronco, mas ao testar a penumbra ele viu o problema de outro ângulo: **"eu quero uma textura igual para as duas partes... não aceito ter textura diferente"**. Diagnóstico: a `BARK_RAIZ` tinha listras de dois tons (`woodBody` 21/22) que batiam em fases diferentes no UV da raiz (30 lados) vs do tronco (6 lados) — a "diferença de cor" que ele via não era iluminação, era a MESMA textura amostrada diferente. **Fix 1 — textura genuinamente uniforme:** `BARK_RAIZ` virou UM tom só (sem listra), mapeada por altura; ainda sobrou um DEGRAU DE BRILHO na emenda (pé com normal enviesada pra cima vs tronco com normal radial) — igualado pra normal radial pura nos dois, sombreado passou a fluir contínuo. **Fix 2 — a forma em si não batia:** o ideador notou que "a raiz tem mais subdivisões que o tronco, o tronco fica mais quadrado" — raiz em 30 lados (redonda) contra tronco em 6 (hexagonal/facetado) SEMPRE geraria salto de forma, textura igual ou não. `galhoSeca` ganhou nº de lados parametrizável; raiz e tronco foram pra 15 lados iguais (múltiplo de 5, os dedos caem nos vértices = estrela nítida). **Fix 3 — a virada real:** "e se apagarmos o pé de elefante e reproduzir ele direto na base do tronco?" — `galhoSeca` ganhou um modo `pe` que PREPENDE as anilhas do pé (perfil `[dy, escalaRaio, ampDedo]`) abaixo da primeira anilha do tubo, **compartilhando a mesma anilha de solda** (mesmo frame `u0/w0`, mesmo raio) → uma superfície ÚNICA do dedo mais fundo da raiz até a ponta do galho mais alto, SEM sobreposição nem costura de verdade (confirmado na visão de geometria, D-65: o pé flui pro tronco sem lipe). `baseRaiz`/`BARK_RAIZ`-com-sombra-pintada foram descartados. **Iteração fina com o ideador:** testou 10 lados (menos redondo) — **rejeitado** ("ta pior"), voltou pra 15; testou 3 raízes largas/grossas em vez de 5 — **rejeitado** ("ta muito pior"), revertido; **início dos galhos afinado** (r0 0.16→0.105, colar 1.5→1.25) — os galhos estufavam mais grosso que o topo do cerne, ficou desproporcional. **Limite honesto:** a emenda geométrica pé↔tronco continua existindo (é um degrau na SILHUETA onde os dedos da raiz encontram o tubo do tronco, visível de perto), mas agora é a MESMA superfície contínua com a MESMA textura — o que o ideador rejeitava (textura/cor diferente) foi eliminado por completo, o que ele aceita resolver depois ("se ficar uma linha divisória eu resolvo") é só forma, não mais textura.
- **D-68 · 2026-07-20 · Merge da branch `colaborador4` pra main (41 commits: câmera, colisão, HUD, gráficos, áudio, ferramentas).** O ideador: "Colaborador 4 subiu bastante coisa que teríamos que trazer pra main... dá uma lida no que temos na branch dele". A branch divergiu em D-62 (antes da virada cartoon D-63/64/65/66) e trouxe **jogabilidade** enquanto esta sessão trazia **arte/porte v3** — trilhas paralelas, sem saber uma da outra. **O que veio:** câmera (inversão horizontal corrigida, shift corre, bug do Esc — dois handlers brigando + pointer-lock engolindo o evento —, retomada da trava sem race condition); **colisão real** (ilha usava círculo fixo de raio 25 contra uma borda orgânica que varia 23.6–26.8, folga medida errada em até 1.4u em 38% das direções — trocado por `raioEm(x,z)`; árvores colidem via `colisao` no `meta` da peça, cilindro do tronco, resposta por empurrão, e peça sem `colisao` simplesmente não colide); **HUD** (coordenadas + protocolo de setores A1-H8 portado do `COMUNICACAO.md` da v2, tecla I = etiquetas de ID via um `visor.projetar` novo, tecla M = mapa grande em canvas 2D separado, aba HUD escolhe canto do minimapa); **gráficos** (resolução até 3440, suavização linear/SSAA-2×, reconstrução tipo-FSR compacta, preset "Dinâmico" que mede janelas de tempo de quadro e sobe degrau — tudo aplicável AO VIVO via `visor.aplicarTiers`; TAA foi implementado e removido — "embaçava demais"); investigação registrada do "fantasma" ao girar — **não é bug do motor**, é persistência de tela + comportamento do olho (descartada acumulação temporal, ritmo de quadro, descompasso de monitor, resolução/filtro, ritmo do mouse, cada um com medição; o teste que fechou foi um quadrado HTML ancorado numa árvore que fantasma IGUAL ao tronco, e quem desenha ele é o navegador, não o WebGL); **áudio** (`motor/som.js` reescrito: passos/água/vento viram síntese por EVENTO — grãos, glissandos, envelopes — em vez de ruído filtrado contínuo, que "vira chiado" e não convence o ouvido); **7 bancadas corrigidas pro Windows** (`await import(caminho-cru)` estourava no ESM, faltava `pathToFileURL`); e o **projeto extenso da Oficina** (editor de objetos in-game, `docs/oficina.md`) com decisões de motor tomadas ANTES de codar: migrar pra WebGL2 primeiro (destrava instanciamento nativo, depth-texture real, MRT), formato de vértice ganha cor + peso de osso NA MESMA passada de migração (mudar duas vezes paga a migração duas vezes), `draw()` aceita tipo de primitiva (`gl.drawArrays(modo, ...)`, hoje fixo em TRIANGLES — trava ponto/linha pra gizmo/grade), materiais por parâmetro-por-lote (não grafo de nós), three.js avaliado e RECUSADO (nada do que se quer é impossível no motor próprio, e o que o three tem de único — carregar `.glb` de terceiro — é o que o protocolo de mundos federados do NÓS explicitamente evita). **O único conflito real foi `render.js`** (o único arquivo que as duas trilhas tocaram): a branch dele nunca viu o contorno cartoon/cel-shading/vento-por-lote/visão-de-geometria (D-63/64/65) que nasceram DEPOIS que ele bifurcou. Mesclado à mão — os dois lados sobrevivem: `WIND`/`uToon`/`uDebug`/programa `outline` (meu) convivem com `aa`/`recon`/refactor de FBO pra `let`/`projetar()`/`aplicarTiers()`/`depurar()` (dele); a função `draw()` interna ganhou os 3 argumentos que os dois lados pediam (`prg, aL, comExtras`); `docs/oficina.md` teve conflito add/add (a main já tinha um cherry-pick antigo do início do doc) — resolvido pegando a versão dele inteira, que é a evolução natural do mesmo documento (perguntas em aberto viraram "solução" com detalhe). Ajuste авulso: `visor.html` usava `visor.info` como valor — virou função no merge (`aplicarTiers` precisa recalcular) — corrigido pra `visor.info()`. **Verificação pós-merge (não só "compilou"):** `auditar` (crítica cpu) idêntico byte-a-byte pré/pós merge nas peças desta sessão (não tocam render.js, então não deveriam mudar — confirmado); `porteiro` 7/7; `jogo.html` carregado via Playwright com **zero erros de página** em tiers baixo/médio/alto (`ts/sombra/luz/particulas`), o painel de configurações de gráfico completo (o que chama `aplicarTiers`) renderiza e é clicável; teclas O (cilindros de colisão via `depurar`)/I (etiquetas via `projetar`)/M (mapa) exercidas sem erro de runtime; a visão de geometria D-65 (`--geo=normais|flat`) re-testada **idêntica em pixel** a antes do merge. **Armadilha própria durante o trabalho (registrada pra não repetir):** um `git stash`/`git stash pop` no MEIO de um merge não commitado (pra comparar `auditar` antes/depois) apagou o `MERGE_HEAD` — o `git commit` seguinte virou um commit comum de 1 pai só, sem o histórico do colaborador4 anexado, apesar do conteúdo em disco estar certo. Detectado comparando `git diff origin/main HEAD --stat` (só mostrava 1 arquivo — óbvio demais pra ser o merge de 2870 linhas) antes de empurrar; corrigido reconstruindo o commit de merge à mão via `git commit-tree` com os 2 pais certos (`origin/main` + a ponta da `colaborador4`), sem perder nenhum byte do trabalho já verificado. **Lição:** nunca `git stash` com merge em progresso — comparar estado pré-merge por outro caminho (branch/worktree separado, não stash).
- **D-69 · 2026-07-20 · README com 3 botões (Jogar V1/V2/V3) + a lacuna de deploy do V3 fechada.** O ideador pediu organizar os botões de jogar do README ("tá meio bagunçado"): existia um link único pro FPS repetido em dois lugares + o mapa 2D só numa parentética, e V3 nem aparecia. Antes de prometer um botão, investiguei ONDE cada versão mora de fato: `/` (raiz) = V1 (`site/`, mapa 2D), `/fps/` = V2 (`prototipos/fps/nos-fps.html`, inlined pelo `build-fps.mjs`), `/fps/v3/` = V3 — **já existe como rota de build** (`build-fps.mjs` copia `prototipos/procedural/v3/` inteiro, comentário original: "D-55, v3 + A OFICINA em /fps/v3/"), só nunca foi anunciada. **O achado que quase virou um botão quebrado:** o `pages.yml` só redesploya em push que toca `site/**`/`world/**`/`assets/**`/`engine/types.ts` — meço e o V3 publicado estava **57 commits atrás** (do D-62b, 2026-07-18 — antes de TODA a virada cartoon D-63…68). O próprio `build-fps.mjs` já documentava essa lacuna desde 2026-07-17 como "gap #1" com o fix escrito e nunca aplicado ("precisa do ideador, workflow edits são gated pro coder"). Perguntei antes de mexer (é literalmente a regra escrita no próprio arquivo) — o ideador escolheu a opção recomendada: **corrigir + publicar agora**. Fix: `prototipos/fps/**` nos `paths` do `pages.yml` (o caminho mais amplo que o comentário já recomendava, cobre V2 e V3 juntos, não só V3). Build local confirmado (`site/dist/fps/v3/jogo.html` existe) antes de empurrar — deploy de verdade é ação visível, não coisa pra testar só depois. **O gap #2 permanece** (documentado, não mexido): commits do tick são feitos com `GITHUB_TOKEN` do bot, e por design do GitHub um push de `GITHUB_TOKEN` não dispara outro workflow — só push humano publica, batida sozinha nunca. README: os 3 botões agora têm um rótulo do que cada um É (não só "V1/V2/V3" pelado) e uma frase que reforça a identidade central — é o MESMO mundo, três janelas, não três jogos.
- **D-70 · 2026-07-20 · O MAPA do repositório — índice gerado, com gate de frescor no CI.** Pedido do ideador: um índice/árvore do NÓS com um resumo por arquivo, "hookeado com commit" pra nunca ficar desatualizado. O instinto (medo de doc que apodrece) estava certo; o mecanismo proposto (escrever índice à mão + hook que barra push) resolvia pelo lado difícil — índice à mão é a doc que MAIS apodrece (todo arquivo novo/renomeado/apagado mexe nele), e "o index mudou neste commit?" é gate fraco (presença ≠ correção). Invertido: **o resumo mora no PRÓPRIO arquivo** (primeiro comentário de cabeçalho; H1 nos .md — disciplina que o repo já seguia em ~85% dos arquivos), e `tools/mapa/mapa.mjs` **gera** `docs/MAPA.md` como projeção (`npm run mapa`). O gate vira **check de frescor estilo lockfile**: `npm run mapa:check` no CI regenera e falha se o commitado divergir OU se qualquer arquivo-fonte estiver sem cabeçalho. Hook local descartado de propósito: hooks não são clonados, o bot do tick não roda hook, e `--no-verify` fura — CI é o gate confiável; e os dois atritos temidos se dissolvem sozinhos (o tick só muda `world/heart.json`, que não altera a árvore de arquivos mapeada; push de bot nem dispara CI — gap #2). Sem timestamp no arquivo gerado de propósito: saída determinística do estado do repo, senão o check nunca bateria. Pra nascer com o gate verde, os 26 arquivos sem cabeçalho ganharam um (11 testes de engine, HTMLs de cliente/QA, `vitest.config`, `player.ts`, `data.js` — e o `build-data.mjs` que REGENERA o `data.js` passou a emitir o cabeçalho junto, senão a próxima regeneração apagaria o resumo; achado, aliás, pelo próprio mapa recém-gerado, que listou um `build-data.mjs` que o grep inicial não tinha visto). Extrator com 3 sabores de comentário + frontmatter (`description:` dos agents vale mais que H1) + junta até 3 linhas do cabeçalho (senão resumo multi-linha corta no meio). O gerado saiu de 182→183 arquivos mapeados e `scratchpad/` entrou no `.gitignore` (o `git add -A` da rodada quase commitou lixo de sessão — pego no review do stage, regra de sempre). Vitest 368/368 + typecheck verdes com os cabeçalhos novos.
- **D-71 · 2026-07-20 · O papel do robô (CI/Actions), fechado — pra não re-debater.** Rodada longa de conversa com o ideador explorando "o que mais dá pra tirar do robô" (logs→conserto? motor gráfico? compilar shader?), fechada numa seção própria (`FERRAMENTAS.md` §7). **O que ele é:** computador de nuvem que liga sozinho (push/horário/botão), roda a lista que escrevemos, desliga — conferidor + forno (pré-cozinhar: o padrão `bake-gi` virou regra geral — conta pesada roda uma vez no robô e vira arquivo; o jogo só lê pronto) + relógio (o tick já é isso). **O que ele nunca é:** motor ao vivo (o jogo roda no aparelho do jogador; o robô não está lá — nenhum truque muda isso) e consertador autônomo (analisar e SUGERIR sim; mudar código sozinho, proibido). **As ferramentas (SUGESTÕES, nada no roadmap — cada uma espera dor real; o que é DECISÃO são só o papel e os limites acima):** ronda-da-oficina nasce junto com o núcleo da Oficina (porteiro em toda peça + replay 2× determinístico + órfão grita + exemplos executáveis do contrato — o braço automático do portão de regressão do túnel); sentinela-de-custo = perf comparativo peça-contra-peça no robô (honesto: compara bem, não crava fps de celular específico); diff-visual com baseline rodando a cada push; **shaders: o robô NÃO compila** (compilação é da GPU de cada jogador, resultado não-portátil — diferente da luz, que vira arquivo universal) — ele valida e pré-monta o texto; o **aquecimento de shaders no carregamento entra como OPÇÃO ligável/desligável nas configurações do jogo** (decisão do ideador), relevante quando o espaço Material multiplicar shaders; logs do jogo→análise fica possível-fora-do-roadmap (não existe canal de coleta). **3 sacadas novas** que a conversa não tinha visto: painel-de-botões (workflow_dispatch + artifacts — o ideador dispara bancada do navegador/celular e recebe os PNGs na página da rodada, sem terminal e sem coder), PR-com-olhos (PR que toca peça ganha comentário automático com render antes|depois — revisão visual, e é o que a federação por PR precisa pra colaborador de fora), ronda-profunda (push = gates rápidos; madrugada/semanal = varredura de perf completa + folha-de-contato de tudo + bench dos críticos). **Guard-rail que veio do medo do ideador** ("tenho medo dele ficar disparando coisa inútil"): só entra checagem que a gente entende; uma por vez; checagem nova começa AVISANDO e só vira reprovação calibrada; desligar = apagar a linha.
- **D-72 · 2026-07-20 · Reciclagem de commits do tick — não agora; o jeito seguro é horizonte-squash, nunca deletar avulso.** Medo do ideador: o tique commita `heart.json` a cada batida e isso pode chegar a milhões de commits. **A conta:** à cadência proposta (1 tick/hora ≈ 8.760/ano), 1 milhão fica a ~114 anos; mesmo a 1 tick/minuto seriam ~2 anos, e a v1 é horária. E o que degrada não é o NÚMERO de commits, é o TAMANHO do repo + tempo de clone — e o `heart.json` é minúsculo, então cresce devagar e o sintoma (clone lento) é gradual, sem penhasco. **A ideia do ideador** (um "sintetizador" que marca commits, resume num doc e apaga o commit): a parte de RESUMIR num doc é boa e segura; a parte de APAGAR não — git não deleta um commit do meio sem reescrever TODO o histórico depois dele (cada commit aponta pro pai por hash; muda um, mudam todos os seguintes), e reescrever histórico quebra todo fork/clone — fatal num projeto federado (mesma lógica da "tatuagem" da versão no envelope). **Detalhe que ajuda:** o identificador que ele queria **já existe** — os commits do tick já nascem marcados na mensagem (`tick: batida #N`), então um futuro reciclador já saberia achá-los sem marca nova. **Conclusão:** não construir reciclador agora (dor distante, e a versão agressiva é perigosa). SE um dia o clone doer, o caminho seguro é um **horizonte-squash** deliberado e anunciado — espremer só o histórico ANTIGO, que ninguém usa ao vivo, num commit-base + um doc-resumo — não um deletador automático por commit. Dor primeiro, como tudo.
- **D-73 · 2026-07-20 · Oficina — rodada de interface e funções (o ideador aprovou pedindo recomendação onde não tinha domínio).** Saída das pendências levantadas na reorganização do doc. **Interface:** mesmo esqueleto em todas as telas — cena/canvas central, painel de propriedades à direita (o único que troca por espaço), barra de modos no topo, status embaixo; aprender uma tela = saber todas. **Modelagem** (além da tabela): valor exato (digitar, não só arrastar), espelho/simetria, duplicar, medir, esconder/isolar; e a que mais serve à IA — **selecionar por critério** (`normal.y > 0.7`, material, tamanho), porque a IA raciocina sobre propriedade, não sobre clique (vira operação `['selecionar', {onde}]`). **Pintura:** cor LIVRE (roda + RGB), **sem trava de paleta** — decisão do ideador, porque a arte ainda não está fechada numa paleta (a trava viria depois, opcional); resolução em pixels + **densidade de texel** (o "DPI" real do 3D, evita borrão em objeto grande); pincéis na ordem duro→macio→texturizado→esfumar, mais shift-reta, conta-gotas, balde, simetria de pintura, gradiente, estabilizador de traço, preview 3D ao vivo (camadas ficam pra depois). **Som:** dois níveis, não um — blocos pequenos (oscilador/filtro/envelope) pra liberdade + presets maiores (vento/passo/bolha) pra facilidade, os presets feitos com os blocos por baixo (catálogo exato marcado `[PENDENTE]` até construir). **Animação:** gatilho = `modo:'uma-vez'` disparado por `tocar()` do código do jogo; dirigido-por-estado = `entrada:'velocidade'` amarra o tempo a um valor do jogo — os dois na seção `ANIMACOES`, sem tocar geometria. Tudo inserido no `oficina.md` (passo 3 da reorganização): decisões como decisão, catálogo de som como `[PENDENTE]`.
- **D-74 · 2026-07-20 · Música, voz e cutscenes — a direção (`docs/AUDIO_E_CENAS.md`).** Terreno novo, fora do planejamento até aqui. **Música:** sequência-como-dado (notas num arquivo pequeno tocadas por sintetizador de código) + generativa — camada por cima do sistema de som. **Voz:** falas = sistema de diálogo em texto (zero-arquivo); voz falada = blips sintetizados por personagem (estilo Animal Crossing/Undertale), com a voz embutida do navegador (API de fala) como alternativa robótica, e voz gravada só como exceção. **Cutscenes:** roteiro/linha-do-tempo executado ao vivo pelo motor (mover câmera, tocar animação/fala/música), NÃO vídeo (arquivo enorme e não-diffável) — é o integrador que junta câmera+animação+diálogo+música, vem depois deles. **Teto de qualidade:** áudio em código não é preso a chiptune — FM, modelagem física (instrumento real a partir de matemática), reverb/efeitos por código e AudioWorklet sobem muito; a única fronteira são samples (arquivo de áudio). **POSSIBILIDADE (não roadmap):** wavetables/ciclo-único como pequenos vetores de dados (poucos KB), zona cinza que sobe mais um degrau. **A preocupação do ideador com retrofit virou decisão:** reservar a ARQUITETURA de som cedo (instrumento = unidade trocável; slot de efeitos no sinal desde o começo) pra que subir a qualidade seja aditivo, não reescrita — mesmo princípio "barato agora, caro depois" do envelope. As técnicas nascem sob necessidade; a estrutura que as acolhe nasce cedo.
- **D-75 · 2026-07-21 · A fundação gráfica, decidida — WebGL 2 como piso, WebGPU reservado na arquitetura.** O ideador perguntou se existe uma fundação de teto mais alto que o WebGL 2 pra adotar AGORA, no começo de tudo, e foi explícito: **não queria tabela de trocas** ("comparativos eu não vou saber lidar muito bem") nem o argumento "você já tem o que precisa" (quase nada foi construído) — queria uma DECISÃO. **A opção de teto mais alto é o WebGPU** (muito mais objeto na tela sem travar; a placa de vídeo pra cálculo pesado de partícula/física). **O fator que decide, concreto e não vago:** hoje ~1/5 a 1/4 dos aparelhos ainda não roda WebGPU (aparelho/sistema mais antigo, Firefox no Android; já tem no Chrome de PC e Android recente, no Safari do iPhone recente, no Firefox de PC) — e "qualquer um entra, PC ou celular, sem instalar" é a alma do NÓS, então excluir um quinto dos jogadores bate de frente com o que o jogo É. **Não foi argumentado por custo afundado** (o motor atual é minúsculo, ~565 linhas/8 shaders — descartável): foi pelo jogador. **Decisão: WebGL 2 como piso universal agora, MAS o motor construído com o renderizador trocável** — uma camada fina separando "o que desenhar" de "com qual tecnologia" — pra o WebGPU ficar RESERVADO na arquitetura; quando o suporte dele chegar perto de universal (logo), plugar é ACRÉSCIMO, não reescrita. Os tempos se encaixam: o teto do WebGPU (milhares de objetos, multidões) só faz falta quando o mundo ficar grande = mais pra frente, que é justo quando o WebGPU vira universal. Mesma jogada do envelope e do instrumento-trocável (D-74): não construir os dois agora, deixar a porta pronta. **A abstração NÃO é construída agora** — com um só renderizador seria prematura; reservá-la no doc basta. A migração WebGL 2 vem primeiro, como troca pura e verificável (mesma saída), ANTES da Oficina, pra gizmo/animação/material não nascerem em cima do que vai mudar. Fontes do estado de suporte: [web.dev](https://web.dev/blog/webgpu-supported-major-browsers), [webo360](https://webo360solutions.com/blog/webgpu-browser-support/).
- **D-76 · 2026-07-21 · A migração pro WebGL 2, feita e verificada — passo 0 da Oficina fechado.** Execução da D-75. Um subagente CODER migrou `prototipos/procedural/v3/motor/render.js` (único arquivo tocado, 42 ins/35 del) num branch isolado: contexto `webgl`→`webgl2` + os 7 programas de shader pra `#version 300 es` (`attribute`→`in`, `varying`→`out`/`in`, `gl_FragColor`→`out vec4 outColor`, `texture2D`→`texture`), com os blocos compartilhados WIND/PACK/VS_QUAD e todo o resto (PCF, empacotamento de profundidade, isPOT/CLAMP-NPOT, tiers de suavização/reconstrução, getAttribLocation) intactos. **Troca pura, PROVADA:** saída BYTE-IDÊNTICA antes/depois nos 20 renders de referência (verificado por `cmp`, não "parece igual") — cobrindo árvore-cartoon (contorno+cel+vento), ilha-chão, casa-toras, arco, geometria normais/flat, jogo com sombra PCF+partículas, e recon+SSAA. Gates verdes (typecheck, 368 testes, mapa, toc, porteiro 4/4, auditar byte-idêntico ao baseline); jogo.html sem erro em baixo/médio/alto. **Sobre o método (D-65, não se auto-certificar):** o coder é o AUTOR; a auditoria independente foi do ORQUESTRADOR — diff lido linha a linha, 3 renders no olho, byte-idêntico re-verificado por conta própria. Um segundo subagente revisor foi dispensado de propósito: prova byte-idêntica não deixa o que um terceiro passe possa achar (rigor ≠ ritual). **Reservas honradas:** o espaço de vértice pra cor/peso-de-osso NÃO foi embutido (fica pro passo 14, aditivo); WebGPU segue reservado no renderizador trocável (D-75). 2 comentários do render.js que a migração deixou factualmente velhos (MSAA "é WebGL2 inalcançável"; "REPEAT exige POT no WebGL1") corrigidos no mesmo fecho. O que a migração destrava — instanciamento nativo, textura de profundidade de verdade, múltiplos alvos de render — fica disponível e é ligado quando a função que precisar dele nascer, não agora.
- **D-77 · 2026-07-21 · O núcleo da Oficina nasceu — passo 1 (estrutura de dados + lista de passos + replay).** Primeiro código da Oficina, ainda sem interface. `prototipos/procedural/v3/motor/oficina.js`: NÚCLEO neutro (vértices únicos numerados + faces apontando pra ids + atributos por face; devolve NÚMEROS, roda headless) + ADAPTADOR v3 (neutro → triângulos soltos do motor; cor por face via textura-amostra + UV, sem inventar atributo de cor no vértice — reservado no passo 0) + `colisaoDe` (só-geometria, encaixa cilindro na malha FINAL). **Identidade posicional por bloco:** o passo de índice i possui os ids [i·1000, i·1000+1000) em espaços separados de vértice e face — a numeração depende só da POSIÇÃO do passo, nunca de PARAMS nem de acaso, então mudar `raio` não renumera e reabrir dá os mesmos ids; mudar `lados` (TOPO) renumera e os passos pendurados viram órfãos que GRITAM. Vocabulário inicial (cubo, cilindro, moveV, extruda, mescla, pincel-face, solido, liso) = o mínimo que prova o modelo E as partes difíceis; o resto da tabela entra nos passos que o usam. Bancada `npm run executar` prova o REPLAY (executa→serializa→re-executa→neutro idêntico); peça `_oficina-toco` renderiza a cadeia inteira núcleo→adaptador→WebGL 2. **O processo provou o valor do revisor adversarial (D-65 na prática):** o coder construiu, e a revisão amistosa (orquestrador: núcleo lido linha a linha, testes lidos, render olhado) achou tudo sólido — mas o passe ADVERSARIAL independente pegou 3 defeitos que os DOIS passaram: **D1 (ALTA)** — tampas do cilindro com winding invertido, normal pra dentro, topo do toco sem sol (o olho não pegou porque a cor tan clara mascarava a luz errada — exatamente o que a regra "não confie no olho pra normal" do D-65 prevê); **D2** — mescla que deixava canto repetido não-consecutivo (bowtie) passava em silêncio, violando a lei "órfão grita, nunca corrompe"; **D3** — cilindro com lados demais estourava o bloco de ids sem aviso. Nenhum tocava o FORMATO SALVO (a fundação irreversível — determinismo, identidade, PARAMS/TOPO, órfão-grita — segurou). Todos consertados antes da main (winding alinhado ao cubo, confirmado por cálculo + render; mescla grita+remove bowtie; assert de overflow) + 3 testes de regressão. 383 testes verdes, replay provado, gates verdes. **Lição de método:** ao contrário do passo 0 (migração byte-idêntica, revisor extra dispensado porque a prova era objetiva), código NOVO de fundação é correção de JULGAMENTO — o passe adversarial não é ritual, é onde mora o bug.
- **D-78 · 2026-07-21 · A câmera do editor — passo 2 (começo da interface).** `prototipos/procedural/v3/oficina.html`: o esqueleto de layout D-73 (cena central, painel de propriedades à direita [placeholder — passo 6], barra de modos no topo, status embaixo) + a CÂMERA DO EDITOR (órbita/pan/zoom) com CURSOR LIVRE (lei do doc: "abrir solta o cursor travado, senão não dá pra clicar em nada" — nenhum `requestPointerLock`). A câmera é da INTERFACE, não do motor: guarda alvo/az/el/dist e dirige o motor por `visor.setCam(pos,yaw,pitch)` no hook `antesDoQuadro`, sem tocar no render.js (golden rule, diff confirma). **Conversão órbita→câmera (o risco "fácil de inverter", igual foi a normal do D1):** pos=alvo+dist·[cos el·sin az, sin el, cos el·cos az], yaw=az+π, pitch=−el — PROVADA por medição, não pelo olho: a bancada projeta o alvo pelo PRÓPRIO motor (`visor.projetar`, prova não-circular) em 7 azimutes = desvio 0.00px do centro, e um ponto fora do eixo varre 261px (orbita de verdade). Bancada `npm run oficina`: carrega/renderiza, cursor livre antes/durante/depois, arrasto move azimute, roda dá zoom, câmera mexe a cena 114226px vs piso 152px (par de controle sem input descarta pólen/vento). Mouse: esquerdo gira, direito/shift move, roda zoom; toque: 1 dedo gira, 2 dedos pinça+arrasta. **Revisor adversarial DISPENSADO de propósito** (lógica do passo 0): o núcleo de risco — a conta da câmera — fechado por medição objetiva, o render.js intacto, e o orquestrador leu o arquivo inteiro; o que sobra é sensação de uso (do ideador) e nuances de UX, não correção. **2 nuances abertas, documentadas pra não apodrecer:** (1) o objeto fica no CENTRO DA JANELA, mas o painel direito cobre parte da direita, então aparece junto do painel, não centrado na área de cena visível — centrar sem reintroduzir "objeto varre arco ao girar" pede PROJEÇÃO DESLOCADA (lens shift), mudança no motor a fazer depois de propósito; (2) o alvo da órbita é fixo [0,0.28,0] (serve pro toco); peça arbitrária deveria AUTO-ENQUADRAR o centro do bounding box ao carregar — acréscimo pequeno pra quando o navegador de peças existir.
- **D-79 · 2026-07-21 · A lente deslocada — o objeto centrado na área útil (fecha a nuance #1 do D-78).** O ideador: "se puder deslocar a lente, não vejo por que manter assim." O objeto ficava no centro da JANELA, então o painel direito o empurrava pra o canto. Conserto = PROJEÇÃO ASSIMÉTRICA (lens shift): desliza a imagem inteira sem girar a câmera, então o alvo da órbita continua sendo o centro do objeto — NÃO reintroduz o "objeto varre arco ao girar" (o modo de falha que o passo 2 evitou). No `render.js`, `setLente(x,y)` guarda um deslocamento em NDC e soma nos elementos de cisalhamento da projeção (`proj[8]/proj[9]`) no frame E no `projetar`. **Opt-in: [0,0] é no-op por construção (somar zero), então jogo e visor ficam byte-idênticos; só a Oficina chama** (porteiro nas peças do jogo confirmou sem regressão). O `oficina.html` calcula o deslocamento dos tamanhos REAIS do painel/barras (recalcula no resize) e centra o objeto no retângulo livre. Medido: o alvo projeta em 376px = centro da área livre (janela 1100 − painel 349), e fica ESTÁVEL ao orbitar (0.00px de variação em 7 azimutes) — a lente centra sem tocar na correção da órbita. 383 testes verdes. A bancada `oficina` passou a medir o invariante certo (alvo ESTÁVEL ao orbitar, agnóstico à lente) + que a lente leva o objeto pra a esquerda ~metade-do-painel. Nuance #2 do D-78 (auto-enquadrar peça arbitrária) segue aberta pra quando o navegador de peças existir.
- **D-80 · 2026-07-21 · A malha aparece — passo 3 (ver vértices e faces sobre a cena).** `oficina.html` ganhou um canvas 2D sobreposto (z-index entre o WebGL e os painéis; `pointer-events:none` — só VER, clicar é o passo 4) que desenha a estrutura editável: importa `nucleo` de `motor/oficina.js` + os PASSOS/PARAMS/TOPO que a peça exporta, e a cada quadro projeta cada vértice por `visor.projetar` (ponto) e liga os cantos de cada face (aresta). Redesenha no hook `antesDoQuadro` LOGO APÓS `aplicarCamera()` — como o `projetar` (ramo freeCam) reconstrói a matriz do estado atual com a MESMA lente do render (D-79), o overlay casa com o quadro 3D sem lag de 1 frame. Aresta em 2 passes (halo escuro + núcleo claro) pra ler sobre grama clara E casca escura. **Provas (bancada, número real):** 19/19 vértices projetados + 62 arestas das 14 faces; orbitando, 19/19 pontos se movem (máx 162px); alinhamento NÃO-circular — detecta o bbox de tela do toco pelos pixels de madeira do render e confirma 19/19 pontos dentro; `pointer-events:none` confirmado e o arrasto do passo 2 segue passando ATRAVÉS do overlay. **Bônus:** a tecla `i` liga as etiquetas de id — o galho sai 1000/1002/1003 com o 1001 AUSENTE (mesclado no 1002), a identidade posicional do passo 1 visível na tela. 17/17 na bancada, 383 testes; render.js/núcleo/peças intactos (só `visor.projetar`, que já existia). Revisor adversarial dispensado (interface, alinhamento provado por pixel). Os passos 1 (dado) + 2 (câmera) + 3 (ver) agora se encontram na tela; segue o passo 4 (selecionar e arrastar um vértice) — o teste da ideia inteira.
- **D-81 · 2026-07-21 · A Oficina EDITA — passo 4, o milestone (selecionar e arrastar um vértice, gravado como operação).** O doc chama os passos 1+4 de "o teste da ideia inteira": arrastar um vértice e o arquivo de passos refazer o objeto igual. Feito, tudo em `oficina.html` (núcleo e motor intactos — só o `moveV`/`executar`/`nucleo`/`projetar` que já existiam). **Interação:** no `pointerdown` do `#c`, ANTES de decidir câmera, hit-test contra os vértices PROJETADOS (o mesmo estado do overlay do passo 3): acertou ≤10px → arrasto de vértice; errou → câmera IDÊNTICA ao passo 2. Overlay segue `pointer-events:none`. **Arrasto 2D→3D:** o vértice segue o cursor no plano da câmera NA PROFUNDIDADE DO PRÓPRIO VÉRTICE (`prof=dot(P−camPos,olhar)`, não a distância do alvo — os vértices do galho extrudado estão noutra profundidade). Provado por MEDIÇÃO (projeta o vértice pós-arrasto vs onde o cursor soltou): 0.04-0.06px em TODOS os extremos (elevação alta, zoom perto/longe, galho fundo) — o adversarial verificou que a base da câmera casa com o `lookAt` do motor e a escala com a derivada da `persp`, correto por construção. **Ciclo:** durante o arrasto, re-executa a lista com um `moveV` TENTATIVO (render ao vivo); ao soltar, se passou de 4px (clique puro só seleciona), grava `['moveV',{v,d}]` (deslocamento total, em mundo). A lista editada re-executada refaz o objeto — determinístico, byte-idêntico na PÁGINA e em NODE à parte (o critério fechado). moveV é aditivo (re-arrastar acumula certo, erro 5e-17). **O passe adversarial se pagou de novo (como no passo 1):** pegou 1 defeito que a revisão amistosa (coder+orquestrador) passou — girar a RODA durante o arrasto zoomava a câmera com a escala do arrasto congelada, gravando um moveV com escala velha (vértice derivava 12-69px, silenciosamente errado, no formato salvo irreversível). Consertado com a guarda `if (arrasto) return` no wheel + teste de regressão (4g) + nit do `setPointerCapture` em try/catch. 383 testes, bancada completa verde. Com o passo 4 a Oficina deixou de ser visualização e virou EDITOR: o ciclo criar→ver→editar→gravar→refazer está fechado. Perf anotada (re-exec por pointermove é barato no toco; objeto grande pedirá caminho incremental).
- **D-82 · 2026-07-21 · Desfazer e refazer — passo 5 (sobre a lista de operações).** Como toda edição é uma operação no FIM de `PASSOS`, o histórico não precisou de sistema à parte: desfazer (Ctrl+Z) tira a última e re-executa, refazer (Ctrl+Y ou Ctrl+Shift+Z) põe de volta — o objeto se refaz da lista (o mesmo `reexec` do passo 4). Tudo em `oficina.html`, núcleo/motor intactos. **Baseline:** ao abrir a peça, trava `baseline = PASSOS.length` (as operações que vieram do ARQUIVO); desfazer nunca passa disso — desfaz só a SESSÃO, não a construção da peça carregada. **Pilha redo**, limpa a cada edição nova (semântica padrão). Provado (bancada, teclas reais do Chromium): desfazer traz o neutro canônico BIT-A-BIT ao de antes da edição (e até o baseline = a peça pristina do arquivo, conferida em Node à parte); refazer, ao de depois; piso do baseline no-op; 3 arrastos↔3 desfaz↔3 refaz idêntico. **A revisão do orquestrador (o passo 4 ensinou a caçar colisão de input no arrasto) pegou 1 defeito:** Ctrl+Z/Y no MEIO de um arrasto de vértice mexia em PASSOS com um moveV tentativo em voo — o commit cairia numa lista encurtada, tangleando o histórico (formato salvo). Mesma classe da brecha da roda; consertado com a guarda `if (arrasto) return false` em desfazer E refazer + teste de regressão (5 guarda). 383 testes, bancada completa verde. Com o passo 5 a Oficina tem edição REVERSÍVEL — o alicerce pra experimentar sem medo antes das ferramentas maiores (gizmo, extruda na tela, materiais).
- **D-83 · 2026-07-21 · Gizmo de eixos + painel — passo 6.** Setas X/Y/Z no vértice selecionado (tamanho constante na tela, `dist/8`), no overlay 2D; hit-test no `pointerdown` na ordem setas→vértice→câmera, com ZONA MORTA na base da seta (o próprio vértice fica pro arrasto livre do passo 4) e TRAVA (`compr<12px` — seta apontando ~pra câmera fica apagada e não arrasta, senão `avanço=mouse/compr` dispara pro infinito). Arrasto TRAVADO usa a fórmula do doc (`avanço=(mouse·dir)/compr`, `d=eixo·avanço`) — o vazamento nos outros 2 eixos é **exatamente 0 por construção** (eixo puro × escalar), não "quase". Reusa a MESMA máquina `arrasto`/`soltar`/`reexec` do passo 4, então as guardas de input-no-arrasto (roda/Ctrl+Z dos passos 4/5) já cobrem o gizmo sem código novo. O painel `#props` deixou de ser placeholder: mostra o vértice selecionado (id + x,y,z) + a caixa do objeto (larg/alt/prof), de LEITURA durante o arrasto ("um dono por vez"); e o **valor exato** (digitar → moveV `d=alvo−atual` no eixo). **Passe adversarial (milestone-class, grava moveV):** veredicto PRONTO PRA MAIN — zero bug de correção, zero corrupção. Provou vazamento 0 e "segue a seta" em 6 câmeras × 3 vértices, a trava sólida (405 observações, 0 violação, near-cliff finito — não dispara pro infinito), o valor-exato robusto (lixo/NaN/no-op/científico) e as colisões de input cobertas (foco no campo não desfaz a malha; roda/Ctrl+Z no arrasto de gizmo ignorados). 1 achado que tocava o formato salvo, consertado antes da main: **D3** — re-digitar o valor JÁ EXIBIDO (3 casas) gravava um moveV fantasma sub-visual (o no-op era 1e-9, o display 1e-3); alinhei o no-op ao display (`toFixed(3)`) + teste (6 valor D3). **Nuances de UX abertas (não bloqueiam, decisão do ideador):** D1 — em ~21% dos ângulos uma seta cobre outro vértice e o clique pega a seta (não corrompe: clique puro = no-op, arrasto grava o vértice certo); D4 — sem limite de sanidade no valor digitado (`1e6` leva o vértice pra longe, mas é undoável). 383 testes, bancada 2-6 verde, motor/núcleo intactos.
- **D-84 · 2026-07-22 · O clique no vértice vence a seta que passa por cima — resolve a nuance D1 do D-83 (passo 6).** A D1: como o `pointerdown` testava a seta do gizmo ANTES do vértice, em ~21% dos ângulos uma seta passava por cima de OUTRO vértice e clicar nele pegava a seta (arrasto travado do selecionado) em vez de selecionar o vértice mirado. Não corrompia (clique puro = no-op), mas atrapalhava a seleção. **Conserto (só `oficina.html`, hit-test do `pointerdown`):** inverteu a ordem pra dar PRECEDÊNCIA ao alvo direto — 1) um vértice sob o cursor que NÃO seja o selecionado vence a seta → arrasto livre (você mirou o ponto, não o cabo); 2) senão a seta → arrasto travado; 3) senão o PRÓPRIO selecionado (a base, na zona morta) → arrasto livre. O caso 3 preserva o arrasto livre na base do vértice selecionado, e a seta segue grabável no resto do cabo, onde não há vértice. **Prova por medição (bancada, teclas reais):** o teste (6 D1) VARRE azimutes até achar uma oclusão real (achou a seta X do #0 cobrindo o #1000), confirma que `hitGizmo` pega naquele ponto e prova que o clique ali agora seleciona o #1000 (não a seta), clique puro sem gravar passo. Revertido o conserto, o teste FALHA (`selecionado 0→0`) — discrimina o bug. Lição virou armadilha na skill `oficina`: alvo clicado direto > handle sobreposto (vale pra todo overlay/handle futuro). `npm test` 383 verde, bancada 2-6 verde; núcleo/motor intactos. Sobra só a D4 (sem clamp no valor digitado, undoável — decisão do ideador).
- **D-85 · 2026-07-22 · Limite de sanidade no campo de valor exato — resolve o D4 do D-83 (passo 6).** O campo que digita a coordenada exata do vértice não tinha teto: um `1e6` era finito, passava, e mandava o vértice pra ~1e6 unidades (undoável, mas atrapalhava). A peça é LOCAL (coordenadas na casa de 1 unidade), então um teto absoluto generoso resolve. **Conserto (só `oficina.html`):** constante `LIM_VALOR = 100` (5-10× a maior peça plausível, mas recusa `1000`+); no `aplicarValor`, `Math.abs(alvoVal) > LIM_VALOR` é RECUSADO com o mesmo tratamento do não-finito — repinta o painel (o campo volta ao valor atual), não grava. **Prova (bancada, evento real):** o teste (6 valor D4) digita `limValor*1000` e afirma vértice + lista intactos; neutralizado o limite, ele FALHA (grava o moveV, PASSOS +1). `npm test` 383, bancada 2-6 verde; núcleo/motor intactos. Com o D1 (D-84) e o D4 fechados, o passo 6 não tem mais nuance aberta.
- **D-86 · 2026-07-22 · Extrudar uma face — passo 7.** A op `extruda` já existia e estava provada no núcleo (`['extruda',{face,dist}]`: cria um anel novo de vértices no bloco do passo, levanta a face-tampa por `dist` na normal, ergue as paredes). O passo 7 é só a INTERFACE (só `oficina.html` + bancada; `motor/oficina.js` e `render.js` INTOCADOS). **Interação:** acrescentei seleção de FACE (vértice XOR face) — `hitFace` faz point-in-polígono (ray-casting par-ímpar, serve pra côncavo) nos cantos PROJETADOS e desempata pela profundidade do CENTROIDE (a face da FRENTE vence na sobreposição); a seleção do corpo é ADIADA pro pointerup pra que orbitar pelo corpo do objeto não deselecione o ativo. A face selecionada ganha UM handle na NORMAL (mesmo estilo/escala/trava do gizmo do passo 6); arrastar extruda com a MESMA fórmula travada (`avanço=(mouse·dir)/compr`), preview ao vivo com um `['extruda',{face,dist}]` TENTATIVO, e ao soltar (passou de `LIMIAR_PX`) grava e limpa o redo. **Reusa a MESMA máquina `arrasto/soltar/reexec`** — então as guardas de input-no-arrasto (roda do passo 4, Ctrl+Z do passo 5) já cobrem o extrude sem código novo (a armadilha nº1 do projeto, resolvida por reuso). A normal do handle TEM que casar com a do núcleo, senão o handle aponta pra um lado e a tampa sobe pra outro: o núcleo não exporta `normalDaFace`, então espelhei o Newell byte-a-byte (conferido: idêntico) e a bancada amarra por medição (o `dist` gravado bate o avanço projetado a 0px). **Verificação (por medição):** bancada com 25 afirmações (Playwright, eventos reais) — hit-test pega a face da frente (`#9 prof 1.833 < #5 2.222`), `dist·compr 46.0px` = cursor `46px` na normal (erro 0px), anel no bloco `10000` (idx·1000), replay página==Node bit-a-bit, undo/redo voltam ao neutro de antes/depois, roda+Ctrl+Z no arrasto ignorados, handle edge-on travado; cada teste mostrado a FALHAR ao neutralizar a checagem-chave. Os dois screenshots confirmam a geometria (handle reto na normal do topo, tampa sobe com paredes limpas, face segue selecionada). **O revisor adversarial dedicado (num subagente) passou de 1h30 sem concluir nem entregar relatório (travou/foi reclamado) — parei de esperar e assumi a prova adversarial da classe CRÍTICA (formato salvo) eu mesmo, em Node puro:** edição composta (extruda→moveV no vértice NOVO→extruda) determinística e aditiva, órfão (`moveV` pra id inexistente) GRITA na lista de órfãos com V/F byte-idênticos (não corrompe), `dist` negativo (inset) não estoura e nasce abaixo da tampa, empilhar 4× põe cada anel no seu bloco, e a lista round-tripada por JSON replaya igual. **Escopo:** só face ÚNICA — extrusão de REGIÃO (várias faces, arestas de borda) é ambição do doc mas exige mudar o núcleo, fica pra um passo próprio. Tecla `E` é atalho opcional (dist padrão 0.2). `npm test` 383, bancada 2-7 verde, typecheck limpo; núcleo/motor intactos. A Oficina agora CRIA geometria nova na tela, não só move a existente.
- **D-87 · 2026-07-22 · Mesclar vértices + ímã — passo 8.** A op `mescla` já existia e estava provada no núcleo (`['mescla',{de,para}]`: some os `de`, faz as faces apontarem pro `para` que mantém a posição, colapsa cantos e apaga face de área-zero; grita no bowtie). Passo 8 é só a INTERFACE (só `oficina.html` + bancada; `motor/oficina.js` e `render.js` INTOCADOS). O doc marca a mescla como "a interação mais delicada do sistema" — ela não muda a FORMA, muda a IDENTIDADE dos vértices no histórico. **Multi-seleção:** `selecionado` virou o ATIVO (último clicado) e uma fonte única `definirSel(ids)` mantém `selVertices` (ordenado) e `selecionado` em sincronia — com 1 id, `selecionado` fica IDÊNTICO ao de antes, então os passos 6/7 não regridem. Shift+clique arma um toggle ADIADO pro pointerup (como o `cliqueFace` do passo 7; Shift+arrasto longo pana); clique normal reseta pra 1; selecionar face limpa os vértices (XOR). **Mesclar:** tecla M (ou botão do painel) com 2+ selecionados grava `['mescla',{de:[não-ativos ORDENADOS],para:ATIVO}]` (merge "no último clicado", estilo Blender; `de` ordenado casa a forma canônica do núcleo), reexec, limpa o redo, seleção vira o `para`. Guardada por `if(arrasto)` (dupla, na tecla e em `mesclar`) e por foco em campo. **Ímã:** um ramo no `mover` LIVRE — `const alvo = ctrl ? imaAlvoEm(cursor, excluir=arrastado) : null; if (alvo) d = posAlvo − arrasto.orig` (`orig` = posição de mundo pré-arrasto, guardada no `iniciar`), então o `moveV` gravado põe o vértice BIT-A-BIT no alvo (não "quase"). Reusa a MESMA máquina `arrasto/mover/soltar`, então as guardas de input-no-arrasto (roda do passo 4, Ctrl+Z do passo 5) já cobrem — **inclusive a colisão nova do passo: o ímã usa Ctrl segurado e o desfazer é Ctrl+Z; a guarda `if(arrasto) return` do desfazer segura o Ctrl+Z no meio do arrasto-com-ímã** (provado). **Verificação (por medição):** bancada com 35 afirmações (Playwright, eventos/teclas reais) — multi-seleção acumula/reseta, M grava `{de:[3],para:13}` (V 19→18, `para` mantém a posição, faces trocam de→para, seleção vira `para`), replay página==Node bit-a-bit, undo/redo voltam ao neutro de antes/depois, ímã cola com erro **5.55e-17** em mundo (sem Ctrl o gap fica 0.28un), Ctrl+Z e roda no arrasto-com-ímã ignorados, e mesclar cantos adjacentes apaga a face de área-zero quieto (F 14→13, 0 órfãos) sem tocar o resto; cada teste mostrado a FALHAR ao neutralizar a checagem-chave (o coder inclusive achou e consertou um teste de guarda que passava pelo PISO do baseline, não pela guarda). **Prova adversarial da classe CRÍTICA (identidade/formato) feita pelo orquestrador em Node** (de propósito, sem subagente — o revisor-subagente do passo 7 travou passando de 1h30): mescla determinística e INDEPENDE da ordem do `de`, cadeia A→B (moveB) →C sobrevive sem órfão, bowtie (cantos não-adjacentes) remove a face e GRITA, `moveV` pra vértice já-mesclado grita com V/F byte-idênticos (não corrompe), `de===para` é no-op seguro, round-trip JSON fiel. **Escopo:** colar o ímã em FACE (projetar+prender) e o LINT de malha visual (aresta 3+ faces / face invertida) ficam pra um passo próprio — o núcleo já impede corrupção (grita/apaga). `npm test` 383, bancada 2-8 verde, typecheck limpo; núcleo/motor intactos.
- **D-88 · 2026-07-22 · Pintar faces (o pincel modo 'face') — passo 9.** A op `pincel` (modo 'face' seta `f.cor`) e o adaptador `adaptarV3` (que monta um SWATCH — uma cor por valor distinto + UV no centro da célula — e cujo `reexec` recarrega a textura) JÁ existiam e estavam provados. Passo 9 é só a INTERFACE (só `oficina.html` + bancada; `motor/oficina.js` e `render.js` INTOCADOS). **Decisão de escopo (consultei o ideador):** o doc pede "projeção em caixa + textura por objeto" já agora, mas ao ler achei um furo — projeção em caixa faz o TOPO e o FUNDO de um objeto fechado (o toco) caírem no MESMO pedaço da textura, então pintar um pintaria o outro; pra cor por face isso é PIOR que o swatch atual, que dá uma cor por face sem conflito. Como o formato salvo do `pincel` é idêntico dos dois jeitos (nada é jogado fora) e quem precisa de textura espacial é o pincel MACIO, **a projeção em caixa / textura pintável (no formato 'atlas por face', que resolve a sobreposição) foi ADIADA pro passo 11**; passo 9 renderiza pelo swatch. **Interação:** MULTI-seleção de FACE espelhando o passo 8 — fonte única `definirSelFace` mantém `selFaces` (ordenado, ativa por último) e `faceSelecionada` em sincronia (com 1 face idêntico ao passo 7, não regride), Shift+clique com toggle adiado pro pointerup (`cliqueSelFace`, testado DEPOIS do vértice — o alvo-ponto vence a área, D1), o XOR vértice↔face agora imposto nos DOIS sentidos via `definirSel`/`definirSelFace`. O painel ganhou um `<input type=color>`+5 presets que mostram a cor EFETIVA da ativa (read-back, `cor ?? COR_PADRAO`); o `change` (confirmar → 1 passo por cor) e os presets gravam `['pincel',{modo:'face',faces:[ORDENADAS],cor}]`, reexec, limpam o redo. **Guardas:** no-op com `arrasto` em curso (mexer em PASSOS com edição em voo tangleria a lista); no-op quando todas as faces já mostram a cor (null conta como COR_PADRAO — sem passo fantasma sub-visual, D3). `COR_PADRAO='#9a8f80'` é ESPELHADO do núcleo (não exportado, não se toca — como o Newell do passo 7); a bancada prova que a paleta concorda. **Verificação (por medição):** bancada com 26 afirmações (Playwright, `change`/clique reais) — multi-seleção acumula/reseta com XOR, `change` grava e `neutro.F.cor` vira a cor, e **a cor APARECE no render** (a paleta REAL do swatch que sobe pra GPU ganha o hex + probe de pixel do topo: madeira `rgb(174,141,146)` → azul `rgb(70,77,233)`), replay página==Node bit-a-bit, undo/redo, pintar 3 faces de uma vez = 1 passo ordenado, guarda no arrasto, no-op de cor-igual; cada teste mostrado a FALHAR ao neutralizar. Prova adversarial da classe formato feita pelo orquestrador em Node: última-pincelada-vence (determinístico), a cor SOBREVIVE à extruda (a tampa mantém o id) e a parede nova nasce sem cor, `pincel` em face inexistente GRITA sem corromper (V/F == base), `modo:'livre'` ainda GRITA (reservado, o adaptador do passo 11 não existe), composto paint+moveV+paint determinístico + JSON fiel. **Princípio novo, dado pelo ideador e que passa a valer sempre: se uma feature depende de algo que só chega num passo POSTERIOR, ela pode ser transferida pra o passo apropriado** (registrado no `nos-fluxo`). `npm test` 383, bancada 2-9 verde, typecheck limpo; núcleo/motor/adaptador intactos.
- **D-89 · 2026-07-22 · Exportar código + colisão automática — passo 10.** O objeto sai do editor de duas formas, e as ops de base (`solido`, `colisaoDe`) já existiam no núcleo (INTOCADO). **(1) Serialização (o coração, é o FORMATO SALVO):** funções puras em `oficina.html` (`serializarPeca`/`jsVal`/`jsStr`/`jsPasso`) montam a string `.js` a partir de `meta/PARAMS/TOPO/PASSOS`, IDÊNTICA em anatomia ao molde `_oficina-toco.js` — cabeçalho de comentário GERADO (uma peça exportada e commitada passa no `mapa:check`), `import { executar, colisaoDe }`, os três `export const`, `meta` com `colisao: colisaoDe(PASSOS, PARAMS, TOPO)` (a CHAMADA literal, calculada no load — não o valor), e `construir`. **Número via `String(double)`, NÃO `toFixed`** — reabre EXATO; arredondar quebraria o bit-a-bit (o `toFixed(3)` foi o que o coder neutralizou pra provar que o teste discrimina). Strings escapadas (`jsStr`). A peça exportada, salva e RE-IMPORTADA, dá `neutroCanonico` bit-a-bit igual ao editor DEPOIS de editar (arrasto+extruda+pincel+solido) — "o arquivo reabre a edição". **(2) Servidor de dev NOVO (`tools/servir.mjs`, 112 linhas):** serve `prototipos/procedural/v3/` com `Cache-Control: no-store` (mata o bug de cache dos módulos ES do `walkthrough_colaborador4`, onde `python -m http.server` prendia a versão velha) + `POST /oficina/salvar {nome,conteudo}` que grava em `pecas/<nome>.js`. **Segurança em duas camadas:** `nomeSeguro` (só `[A-Za-z0-9_-]`, `.js` forçada — barra `../`, `/`, `.`, espaço, símbolo) + o cinto `resolve` e `startsWith(base + sep)` no serving estático, que pega o traversal que a normalização de URL não pega (o `%2e%2e` codificado é decodificado DEPOIS da normalização, mas o caminho resolvido não fica dentro da base → 404). `criarServidor({raiz,pecas})` injetável (a bancada aponta `pecas/` pra um dir temporário, não polui o rastreado). Botão "Salvar peça" na barra de modos: POSTa; sem servidor, **fallback pro download** do `.js` (blob + `<a download>`). **(3) Colisão + sólido:** botão "marcar sólido" (com face[s] selecionada[s], reusa a máquina do passo 9) grava `['solido',{faces:[ordenadas]}]`; o painel roda `colisaoDe` a cada edição e mostra forma/raio/altura/base, com AVISO EM DESTAQUE quando não há face sólida (o doc: senão a copa da árvore vira parede). **Verificação:** bancada com 26 afirmações (Playwright, POST/GET reais, dogfood servindo a Oficina pelo próprio `servir.mjs`) — sólido grava/desfaz/no-op/guarda, colisão bate `colisaoDe`, **ida-e-volta bit-a-bit** (2440 chars, página==Node), servidor grava e o arquivo === conteúdo, 6/6 nomes maliciosos rejeitados sem escapar, `no-store`, fallback download; cada teste mostrado a FALHAR ao neutralizar (a serialização neutralizada pra `toFixed(3)` DIVERGE 2435 vs 2440). **Prova de segurança INDEPENDENTE do orquestrador em Node** (vetores extras além da bancada): `nomeSeguro` rejeita 16 nomes ruins e aceita os bons; GET com `../`, `%2e%2e`, `..%2f`, `/pecas/../../` NÃO vaza um segredo posto fora da raiz (todos 404); POST válido grava e serve de volta. `npm test` 383, bancada 2-10 verde, mapa 195 (o `servir.mjs` com cabeçalho); núcleo/motor/adaptador intactos. Com o passo 10 o ciclo criar→editar→pintar→marcar→EXPORTAR fecha: a Oficina produz peça de verdade, salva no repo.
- **D-90 · 2026-07-22 · Atlas de textura por face — passo 11a (fundação, meia-etapa).** O passo 11 (pincel macio) precisa de uma textura pintável com UV espacial e SEM sobreposição. A projeção em caixa GLOBAL do doc tem um furo — topo (+y) e fundo (−y) de um objeto fechado projetam no MESMO plano XZ, dividindo o mesmo pedaço da textura (medido: IoU 0.94 entre as caixas de #8 e #9 do toco; pintar um pintaria 94% do outro). O ideador escolheu o "pincel completo" (a ferramenta capaz faz o simples também), então a base tem que ser sólida. **Solução (só o `adaptarV3` de `motor/oficina.js` — a PRIMEIRA mudança no núcleo desde o passo 1, e só no ADAPTADOR, que existe pra isso):** cada face ganha uma ILHA própria num atlas quadriculado (`cols=ceil(√N)`, ilha 32px, gutter 2px, ex.: toco 14 faces → 4×4 → 128×128); o UV de cada canto sai por PROJEÇÃO EM CAIXA *daquela* face (eixo dominante da normal → projeta as outras duas coords de mundo → normaliza pela bbox 2D → mapeia pro retângulo interno da ilha). É a projeção em caixa do doc, só que PRIVADA por face — automática, mas sem o furo. **Em 11a a ilha é CHAPADA com a cor da face**, então cada face renderiza a cor cheia IGUAL ao swatch — provado BYTE-A-BYTE: render do toco headless swatch (origin/main) vs atlas (HEAD) = **0 pixels diferentes, PNG idêntico**; e a bancada mostra as 14 ilhas disjuntas (0 pares se intersectam), o furo global (IoU 0.94) sumindo (ilhas separadas), e #8/#9 pintáveis independente (vermelho/azul sem contaminar). **Contido e provado:** `nucleo`/`neutroCanonico`/as ops/`colisaoDe`/`render.js` intactos (diff vazio); `adaptarV3` é importado SÓ por peças da Oficina (grep — o jogo não usa), então o jogo não muda; `neutroCanonico` e o export do passo 10 seguem bit-a-bit (o adaptador não alimenta o neutro nem o arquivo salvo). A saída ganhou `atlas = {W,H,cols,rows,tile,gutter, daFace(id)}` com `daFace→{ilha:{x,y,w,h}, dom, projeta(pontoMundo)→[u,v]}` — a FONTE ÚNICA do UV (o mesh e o pincel do 11b saem dela, nunca divergem). `npm test` 386 (3 testes de atlas novos), typecheck/mapa/toc verdes; bancada 2-10 sem regressão. Mergeado como CHECKPOINT (de-risca a mudança no núcleo antes do 11b). Falta o 11b — o pincel macio (modo 'livre', raio/dureza/degradê) rasterizando pinceladas `{f,a,b}` nas ilhas — pra fechar o passo 11.
- **D-91 · 2026-07-23 · Pincel macio no núcleo — passo 11b (motor, meia-etapa).** Sobre a base do 11a (o atlas por face), o pincel macio precisa de duas coisas no motor, e SÓ no motor (`motor/oficina.js`; `render.js`/`oficina.html` intactos): a operação que grava as pinceladas e o adaptador que as desenha. **A op `pincel` aprendeu `modo:'livre'`:** `['pincel',{modo:'livre',cor,raio,dureza,pontos:[{f,a,b}]}]` — cada ponto vira um DAB na FACE `f` (`f.tinta.push({a,b,cor,raio,dureza})`), com **`{a,b}` FACE-LOCAL** (as coords `s,t` da projeção do atlas em [0,1], o mesmo que gera o UV do mesh — NÃO um texel cru). É isso que faz a tinta ACOMPANHAR a face: um `moveV` depois muda a projeção/o UV, mas o dab segue no mesmo `{a,b}` (provado: após o moveV o UV do canto desliza mas o texel central do dab segue a cor). Ponto com face inexistente GRITA (órfão); modo desconhecido GRITA. O modo 'face' ficou intocado (compat). **`neutroCanonico` inclui a tinta — condicionalmente:** só anexa o campo à linha da face quando ela TEM dab, numa forma fixa `[a,b,cor,raio,dureza]` por dab na ordem de pintura. Assim toda peça sem pincel livre (passos 1..11a inteiros, incl. o toco) canoniza BYTE-idêntico ao de antes — a compat pra trás é estrutural, não um cuidado frágil. **`adaptarV3` rasteriza:** por cima da cor chapada do 11a, cada dab vira um círculo radial macio na ilha da face — falloff `q=dist/raio`; `q≤dureza` núcleo 100% opaco, do núcleo à borda um ombro smoothstep até 0; alpha-over, mais nova por cima; preso na CÉLULA (não vaza pra ilha vizinha — o gutter é a folga). Sem dab → retorna a cor chapada, byte-idêntico ao 11a. **Verificação:** bancada + vitest (393, +7) — dab com degradê medido (centro=cor, +8px=base, meio esmaece), determinismo (canon 2×+JSON), paint-follows-face, órfão, raio/dureza medíveis, preso na célula; cada teste a FALHAR sob neutralização (o guard da compat: append incondicional quebra SÓ o teste da compat na suíte inteira). **Prova adversarial em Node pelo orquestrador:** 11 checagens — compat (toco canoniza sem tinta, linha F=6), determinismo, paint-follows-face, órfão, modo desconhecido, raio. É a 2ª mudança no núcleo desde o passo 1 e a única que mexe no formato salvo (a op nova) — por isso a compat e o determinismo foram os critérios inegociáveis. `npm test` 393, bancada 2-11a sem regressão, typecheck limpo. Mergeado como checkpoint; falta o 11c (a interface: pintar arrastando na superfície, com raycasting → `{f,a,b}`) pra fechar o passo 11.
- **D-92 · 2026-07-23 · Interface do pincel macio — passo 11c (fecha o passo 11).** Com o atlas (11a) e a op livre + rasterização (11b-motor) prontos e provados, o 11c é só a INTERFACE que grava a op a partir do gesto (só `oficina.html`; `render.js`/`motor/oficina.js` byte-idênticos). **Modo pincel:** um chip "Pincel" (grupo "ferramenta" na barra de modos) + tecla `B` ligam o modo; ligado, o cursor vira mira, abre o `#blocoPincel` (cor+presets do passo 9 + sliders de raio e dureza) e um arrasto na superfície PINTA; desligado, tudo segue idêntico (vértice/gizmo/face/câmera intactos, provado sem regressão). **O raycast (a parte difícil, feita certa):** o raio do cursor é o INVERSO EXATO de `visor.projetar` — a mesma `m4.persp` + o MESMO `pj[8]/pj[9] += lente` (a lente assimétrica do D-79) —, por isso o ponto de superfície projeta de volta a **0.00px** do cursor (prova não-circular); acha a face da FRENTE (`hitFace`), intersecta o raio com o PLANO dela → ponto de mundo → `{f,a,b}` face-local pela inversão do retângulo da ilha (`a=(u·W−ilha.x)/ilha.w`). Neutralizar o termo da lente no raycast estoura o round-trip pra 174.7px e `{a,b}` sai da face — o teste discrimina o mecanismo certo. **O gesto:** acumula dabs por distância (6px de tela; ~13 por pincelada) numa op `['pincel',{modo:'livre',cor,raio,dureza,pontos:[{f,a,b}]}]` com preview ao vivo (o `reexec` recomputa o `atlas` por `adaptarV3`), e ao soltar grava (limpa o redo). Reusa `arrasto.pincel` na máquina `arrasto`/`soltar`/`reexec`, então as guardas de input-no-arrasto (roda/Ctrl+Z) já cobrem. Arrasto no vazio (sem face) não grava op vazia; um arrasto que atravessa duas faces grava pontos nas duas num só passo (`[1,9]` — a op já separa por face). **Verificação (bancada, gestos reais):** modo liga/desliga sem regredir o passo 4/7, grava a op na face certa com `{a,b}` batendo o cursor (round-trip 0.00px, `abInMundo`==raycast), a pincelada APARECE (centro madeira→azul, borda não pintada intacta), replay página==Node bit-a-bit, undo/redo, raio/dureza da UI refletem no tamanho da mancha (0.08→16 texels < 0.5→616), guardas cobrem, atravessar faces; cada teste a FALHAR sob neutralização da lente. Screenshot: a mancha azul com degradê no topo do toco. **Nuance:** a borda da mancha sai PIXELADA (a ilha tem 28×28 texels/face) — o que combina com o estilo pixel-art do jogo; se quiser degradê mais liso, é só aumentar o `ATLAS_TILE`. **Com 11a+11b+11c o passo 11 (pincel macio) está COMPLETO:** a Oficina agora pinta degradê livre na superfície, além da cor por face. `npm test` 393, bancada 2-11 verde, typecheck limpo.
